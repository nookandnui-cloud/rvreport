kind: ConfigMap
apiVersion: v1
metadata:
  name: rvreport
  namespace: sysinfra-workloads
data:
  index.html: |
    <!DOCTYPE html>
    <html lang="th">
    <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MFEC VMware Report</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/docx@8.5.0/build/index.umd.js"></script>
    <style>
      :root{
        --ink:#121820;
        --panel:#1A222B;
        --panel-2:#20293380;
        --border:#2C3742;
        --text:#E7ECF2;
        --muted:#8B96A3;
        --ok:#3ED2A0;
        --warn:#F0A857;
        --bad:#E2596B;
        --info:#5AA9E6;
        --mono:'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, monospace;
        --disp:'Space Grotesk', ui-sans-serif, system-ui, sans-serif;
      }
      @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=IBM+Plex+Mono:wght@400;500;600&display=swap');

      *{box-sizing:border-box; margin:0; padding:0;}
      html,body{height:100%;}
      body{
        background:var(--ink);
        color:var(--text);
        font-family:var(--mono);
        font-size:13.5px;
        line-height:1.55;
        -webkit-font-smoothing:antialiased;
      }
      h1,h2,h3,.disp{font-family:var(--disp);}

      a{color:var(--info);}

      /* ---------- layout shell ---------- */
      .shell{ display:flex; min-height:100vh; }

      .rail{
        width:212px; flex:0 0 212px;
        background:#151C24;
        border-right:1px solid var(--border);
        padding:20px 0;
        display:flex; flex-direction:column;
        position:sticky; top:0; height:100vh;
      }
      .rail .brand{
        padding:0 20px 18px 20px;
        border-bottom:1px solid var(--border);
        margin-bottom:14px;
      }
      .rail .brand .mark{
        font-family:var(--disp); font-weight:700; font-size:17px; letter-spacing:.2px;
        color:var(--text);
      }
      .rail .brand .mark span{color:var(--ok);}
      .rail .brand .sub{color:var(--muted); font-size:11px; margin-top:3px;}

      .nav-item{
        display:flex; align-items:center; gap:10px;
        padding:9px 20px;
        color:var(--muted);
        cursor:pointer;
        border-left:2px solid transparent;
        font-size:12.5px;
        transition:background .12s ease, color .12s ease;
      }
      .nav-item .dot{width:6px;height:6px;border-radius:50%;background:var(--border);flex:0 0 auto;}
      .nav-item:hover{ color:var(--text); background:#1B2430; }
      .nav-item.active{ color:var(--text); background:#1B2430; border-left-color:var(--ok); }
      .nav-item.active .dot{background:var(--ok);}
      .nav-item.disabled{ opacity:.35; cursor:default; }
      .nav-item.disabled:hover{ background:transparent; color:var(--muted); }

      .rail-foot{
        margin-top:auto; padding:14px 20px 4px 20px;
        border-top:1px solid var(--border);
        color:var(--muted); font-size:10.5px;
      }

      .main{ flex:1; min-width:0; padding:28px 34px 60px 34px; }

      /* ---------- top status strip ---------- */
      .statusbar{
        display:flex; align-items:center; justify-content:space-between;
        gap:16px; flex-wrap:wrap;
        margin-bottom:26px;
        padding-bottom:16px;
        border-bottom:1px solid var(--border);
      }
      .statusbar .fileinfo{ font-size:12px; color:var(--muted); }
      .statusbar .fileinfo b{ color:var(--text); font-weight:500; }
      .btnrow{ display:flex; gap:8px; }
      button{
        font-family:var(--mono); font-size:12px;
        background:#212B36; color:var(--text);
        border:1px solid var(--border); border-radius:3px;
        padding:8px 14px; cursor:pointer;
      }
      button:hover{ border-color:#3D4A58; background:#26313D; }
      button.primary{ background:var(--ok); color:#0C1712; border-color:var(--ok); font-weight:600; }
      button.primary:hover{ filter:brightness(1.08); }
      button:disabled{ opacity:.4; cursor:not-allowed; }

      /* ---------- upload / empty state ---------- */
      .dropzone{
        margin-top:40px;
        border:1px dashed var(--border);
        border-radius:6px;
        padding:70px 30px;
        text-align:center;
        color:var(--muted);
        transition:border-color .15s ease, background .15s ease;
      }
      .dropzone.drag{ border-color:var(--ok); background:#182019; }
      .dropzone h2{ color:var(--text); font-size:20px; margin-bottom:8px; font-weight:600; }
      .dropzone p{ font-size:12.5px; margin-bottom:22px; }
      .dropzone .hint{ margin-top:18px; font-size:11px; color:#5C6B7A; }
      input[type=file]{ display:none; }

      /* ---------- generic components ---------- */
      .section{ display:none; }
      .section.active{ display:block; }
      .section-title{
        font-size:20px; font-weight:600; margin-bottom:4px;
      }
      .section-desc{ color:var(--muted); font-size:12px; margin-bottom:22px; }

      .grid{ display:grid; gap:14px; }
      .grid.cols-4{ grid-template-columns:repeat(4,1fr); }
      .grid.cols-3{ grid-template-columns:repeat(3,1fr); }
      .grid.cols-2{ grid-template-columns:repeat(2,1fr); }
      @media (max-width:1000px){ .grid.cols-4{grid-template-columns:repeat(2,1fr);} .grid.cols-3{grid-template-columns:repeat(2,1fr);} }

      .stat{
        background:var(--panel);
        border:1px solid var(--border);
        border-left:2px solid var(--border);
        padding:14px 16px;
      }
      .stat .lbl{ color:var(--muted); font-size:11px; text-transform:uppercase; letter-spacing:.4px; }
      .stat .val{ font-family:var(--disp); font-size:26px; font-weight:600; margin-top:6px; }
      .stat .sub{ color:var(--muted); font-size:11px; margin-top:4px; }
      .stat.ok{ border-left-color:var(--ok); }
      .stat.warn{ border-left-color:var(--warn); }
      .stat.bad{ border-left-color:var(--bad); }
      .stat.info{ border-left-color:var(--info); }

      .panel{
        background:var(--panel);
        border:1px solid var(--border);
        padding:18px 20px;
        margin-bottom:16px;
      }
      .panel h3{ font-size:13.5px; font-weight:600; margin-bottom:14px; display:flex; align-items:center; justify-content:space-between; }
      .panel h3 .tag{ font-size:10px; color:var(--muted); font-weight:400; text-transform:uppercase; letter-spacing:.4px; }

      .findings{ list-style:none; }
      .findings li{
        display:flex; gap:10px; align-items:flex-start;
        padding:9px 0; border-top:1px solid var(--border); font-size:12.5px;
      }
      .findings li:first-child{ border-top:none; }
      .findings .sev{ width:8px; height:8px; border-radius:50%; margin-top:4px; flex:0 0 auto; }
      .findings .sev.bad{ background:var(--bad); }
      .findings .sev.warn{ background:var(--warn); }
      .findings .sev.ok{ background:var(--ok); }
      .findings .txt b{ color:var(--text); font-weight:600; }
      .findings .txt{ color:var(--muted); }

      table{ width:100%; border-collapse:collapse; font-size:12px; }
      thead th{
        text-align:left; color:var(--muted); font-weight:500;
        font-size:10.5px; text-transform:uppercase; letter-spacing:.3px;
        padding:6px 10px; border-bottom:1px solid var(--border);
        position:sticky; top:0; background:var(--panel);
      }
      tbody td{ padding:7px 10px; border-bottom:1px solid #232C36; color:var(--text); }
      tbody tr:hover{ background:#20293380; }
      td.num, th.num{ text-align:right; font-variant-numeric:tabular-nums; }
      .table-wrap{ max-height:420px; overflow:auto; border:1px solid var(--border); }
      .table-wrap table thead th{ background:#1A222B; }

      .bar-track{ background:#232C36; height:8px; border-radius:2px; overflow:hidden; }
      .bar-fill{ height:100%; background:var(--ok); }
      .bar-fill.warn{ background:var(--warn); }
      .bar-fill.bad{ background:var(--bad); }

      .pill{ display:inline-block; padding:2px 8px; border-radius:2px; font-size:10.5px; border:1px solid var(--border); color:var(--muted); }
      .pill.ok{ color:var(--ok); border-color:#2A5A47; }
      .pill.warn{ color:var(--warn); border-color:#5A4A2A; }
      .pill.bad{ color:var(--bad); border-color:#5A2A32; }

      .chart-box{ position:relative; height:260px; }
      .chart-box.small{ height:200px; }

      .empty-note{ color:var(--muted); font-size:12px; padding:20px 0; }

      .email-block{ margin-bottom:14px; }
      .email-block .email-head{ display:flex; align-items:center; justify-content:space-between; margin-bottom:6px; }
      .email-block .email-lang{ font-size:11px; color:var(--muted); text-transform:uppercase; letter-spacing:.4px; }
      .email-block textarea{
        width:100%; min-height:170px; resize:vertical;
        background:#151C24; border:1px solid var(--border); color:var(--text);
        font-family:var(--mono); font-size:12px; line-height:1.6; padding:12px;
      }
      .email-copy-btn{ font-size:11px; padding:5px 12px; }
      .email-copy-btn.copied{ background:var(--ok); color:#0C1712; border-color:var(--ok); }

      ::-webkit-scrollbar{ width:8px; height:8px; }
      ::-webkit-scrollbar-thumb{ background:#2C3742; border-radius:4px; }
      ::-webkit-scrollbar-track{ background:transparent; }

      /* ---------- print ---------- */
      @media print{
        .rail, .btnrow, .dropzone, .no-print{ display:none !important; }
        body{ background:#fff; color:#111; font-size:11px; }
        .main{ padding:0; }
        .section{ display:none !important; }
        #sec-template{ display:block !important; }
        .panel, .stat{ background:#fff; border-color:#ccc; }
        thead th{ background:#fff; color:#333; }
        .findings .txt{ color:#333; }
        .statusbar{ border-color:#ccc; }
      }

      /* ---------- MFEC template report ---------- */
      .tpl-report{ background:#fff !important; color:#111 !important; padding:24px; border:1px solid var(--border); font-family:Calibri, Arial, sans-serif; }
      .tpl-report, .tpl-report *{ color:#111 !important; background-color:transparent; text-shadow:none !important; }
      .tpl-report h4.tpl-h1{ font-size:16px; font-weight:700; margin:22px 0 10px 0; border-bottom:2px solid #444; padding-bottom:4px; }
      .tpl-report h4.tpl-h1:first-child{ margin-top:0; }
      .tpl-report h5.tpl-h2{ font-size:13.5px; font-weight:700; margin:16px 0 8px 0; }
      .tpl-report p.tpl-p{ font-size:12px; margin:0 0 8px 0; line-height:1.5; }
      .tpl-report table{ width:100%; border-collapse:collapse; margin-bottom:10px; font-size:11px; background:#fff !important; }
      .tpl-report table th, .tpl-report table td{ border:1px solid #999; padding:5px 8px; text-align:left; background:#fff !important; font-size:11px; text-transform:none !important; position:static !important; letter-spacing:normal !important; }
      .tpl-report table th{ background:#E8E8E8 !important; font-weight:700; }
      .tpl-report table td.num, .tpl-report table th.num{ text-align:right; }
      .tpl-report .tpl-note{ font-size:10.5px; color:#555 !important; font-style:italic; margin:-4px 0 12px 0; }
      .tpl-report i{ color:#555 !important; }
      .tpl-report .tpl-status-normal{ color:#1a7a3c !important; font-weight:600; }
      .tpl-report .tpl-status-warning{ color:#b8860b !important; font-weight:600; }
      .tpl-report .tpl-status-critical{ color:#c0392b !important; font-weight:600; }
      .tpl-report input{ color:#111 !important; background:#fff !important; }
      @media print{ .tpl-report{ border:none; padding:0; } }
    </style>
    </head>
    <body>

    <div class="shell">
      <div class="rail">
        <div class="brand">
          <div class="mark">MFEC <span>VMware Report</span></div>
          <div class="sub">RVTools export analyzer</div>
        </div>
        <div id="nav"></div>
        <div class="rail-foot">ประมวลผลในเบราว์เซอร์ทั้งหมด<br>ไม่มีการอัปโหลดไฟล์ขึ้นเซิร์ฟเวอร์</div>
      </div>

      <div class="main">
        <div class="statusbar">
          <div class="fileinfo" id="fileinfo">ยังไม่ได้โหลดไฟล์ RVTools</div>
          <div class="btnrow">
            <button id="btnReplace" style="display:none;">เปลี่ยนไฟล์</button>
            <button id="btnExportWord" class="no-print" style="display:none;">Export (.docx)</button>
            <button id="btnPrint" class="primary" style="display:none;">Export Report (Print/PDF)</button>
          </div>
        </div>

        <div id="dropzone" class="dropzone">
          <h2>นำเข้าไฟล์ RVTools (.xlsx)</h2>
          <p>ลากไฟล์มาวาง หรือกดปุ่มด้านล่างเพื่อเลือกไฟล์ — ระบบจะสร้างรายงานสรุปให้ทันที</p>
          <button class="primary" id="btnPick">เลือกไฟล์ RVTools</button>
          <input type="file" id="fileInput" accept=".xlsx,.xls">
          <div class="hint">รองรับชีต: vInfo · vHost · vCluster · vDatastore · vSnapshot · vTools · vDisk · vNetwork</div>
        </div>

        <div id="report" style="display:none;">

          <!-- OVERVIEW -->
          <section class="section active" id="sec-overview">
            <div class="section-title">ภาพรวม (Executive Summary)</div>
            <div class="section-desc">สรุปสถานะ Virtual Infrastructure จากไฟล์ที่นำเข้า</div>
            <div class="grid cols-4" id="ov-stats"></div>

            <div class="panel" style="margin-top:16px;">
              <h3>ประเด็นที่ควรตรวจสอบ <span class="tag">Key findings</span></h3>
              <ul class="findings" id="ov-findings"></ul>
            </div>

            <div class="grid cols-2">
              <div class="panel">
                <h3>สถานะเปิด/ปิดเครื่อง <span class="tag">Power state</span></h3>
                <div class="chart-box small"><canvas id="chart-power"></canvas></div>
              </div>
              <div class="panel">
                <h3>ระบบปฏิบัติการยอดนิยม <span class="tag">Top 10 Guest OS</span></h3>
                <div class="chart-box small"><canvas id="chart-os"></canvas></div>
              </div>
            </div>
          </section>

          <!-- COMPUTE -->
          <section class="section" id="sec-compute">
            <div class="section-title">Compute</div>
            <div class="section-desc">การจัดสรร vCPU / vRAM เทียบกับทรัพยากรจริงของ Host</div>
            <div class="grid cols-4" id="cp-stats"></div>
            <div class="panel">
              <h3>สรุปตาม Cluster</h3>
              <div class="table-wrap"><table id="tbl-cluster"></table></div>
            </div>
            <div class="panel">
              <h3>สรุปตาม Host</h3>
              <div class="table-wrap"><table id="tbl-host"></table></div>
            </div>

            <div class="panel">
              <h3>Memory Tiering — Active : Standby Ratio <span class="tag" id="cp-tiering-tag"></span></h3>
              <div class="section-desc" style="margin-bottom:14px;">ประเมินสัดส่วนหน่วยความจำที่ใช้งานจริง (Active) เทียบกับที่ค้าง/เย็น (Standby) ต่อ VM เพื่อดูศักยภาพในการทำ Memory Tiering (เช่น VMware Memory Tiering บน NVMe ตั้งแต่ vSphere 8.0 U3) — VM ที่มี Standby สูงคือตัวเลือกที่ดีสำหรับ tier ไปยัง NVMe/สื่อที่ถูกกว่า</div>
              <div class="grid cols-4" id="cp-tiering-stats"></div>
              <div id="cp-tiering-note" style="font-size:10.5px; color:var(--muted); margin:14px 0 8px 0;"></div>
              <div class="table-wrap"><table id="tbl-tiering"></table></div>
            </div>
          </section>

          <!-- STORAGE -->
          <section class="section" id="sec-storage">
            <div class="section-title">Storage</div>
            <div class="section-desc">การใช้งาน Datastore และพื้นที่คงเหลือ</div>
            <div class="grid cols-4" id="st-stats"></div>
            <div class="panel">
              <h3>Datastore <span class="tag">เรียงตามพื้นที่ว่างน้อยสุด</span></h3>
              <div class="table-wrap"><table id="tbl-datastore"></table></div>
            </div>
          </section>

          <!-- VMs -->
          <section class="section" id="sec-vms">
            <div class="section-title">Virtual Machines</div>
            <div class="section-desc">รายชื่อ VM ที่ใช้ทรัพยากรสูงสุดและรายการทั้งหมด</div>
            <div class="grid cols-3">
              <div class="panel">
                <h3>Top 10 vCPU <span class="tag">RVTools ไม่มี CPU usage ระดับ VM</span></h3>
                <div class="table-wrap"><table id="tbl-top-cpu"></table></div>
              </div>
              <div class="panel">
                <h3>Top 10 Memory Usage</h3>
                <div class="table-wrap"><table id="tbl-top-mem"></table></div>
              </div>
              <div class="panel">
                <h3>Top 10 Storage Usage</h3>
                <div class="table-wrap"><table id="tbl-top-disk"></table></div>
              </div>
            </div>
            <div class="panel">
              <h3>รายการ VM ทั้งหมด <span class="tag" id="vm-count-tag"></span></h3>
              <div class="table-wrap"><table id="tbl-allvms"></table></div>
            </div>
          </section>

          <!-- SNAPSHOTS -->
          <section class="section" id="sec-snapshots">
            <div class="section-title">Snapshots</div>
            <div class="section-desc">Snapshot ที่ค้างอยู่ในระบบและความเสี่ยงจากอายุการเก็บ</div>
            <div class="grid cols-4" id="sn-stats"></div>
            <div class="panel">
              <h3>รายการ Snapshot</h3>
              <div class="table-wrap"><table id="tbl-snapshot"></table></div>
            </div>
          </section>

          <!-- TOOLS -->
          <section class="section" id="sec-tools">
            <div class="section-title">VMware Tools</div>
            <div class="section-desc">สถานะการติดตั้งและเวอร์ชันของ VMware Tools บน VM — แยกระดับความสำคัญระหว่าง "เวอร์ชันเก่า (toolsOld)" กับ "ไม่ได้ติดตั้ง/ไม่ทำงาน" ซึ่งเสี่ยงกว่า</div>
            <div class="grid cols-4" id="tools-stats"></div>
            <div class="grid cols-2">
              <div class="panel">
                <h3>สรุปสถานะ</h3>
                <div class="chart-box"><canvas id="chart-tools"></canvas></div>
              </div>
              <div class="panel">
                <h3>VM ที่ควรตรวจสอบ</h3>
                <div class="table-wrap"><table id="tbl-tools"></table></div>
              </div>
            </div>
          </section>

          <!-- SOFTWARE EXPIRE -->
          <section class="section" id="sec-expire">
            <div class="section-title">Software Expire (End of Support)</div>
            <div class="section-desc">วันสิ้นสุดการซัพพอร์ตของ ESXi, vCenter และระบบปฏิบัติการบน VM ทั้งหมด — อ้างอิงฐานข้อมูลวงจรอายุผลิตภัณฑ์ ณ กันยายน 2569</div>
            <div class="grid cols-4" id="ex-stats"></div>

            <div class="panel">
              <h3>vCenter Server</h3>
              <div id="ex-vcenter"></div>
            </div>

            <div class="panel">
              <h3>ESXi Host</h3>
              <div class="table-wrap"><table id="tbl-esxi"></table></div>
            </div>

            <div class="panel">
              <h3>ระบบปฏิบัติการบน VM <span class="tag" id="ex-os-tag"></span></h3>
              <div class="table-wrap"><table id="tbl-os-eol"></table></div>
            </div>

            <div class="panel no-print" style="border-left:2px solid var(--warn);">
              <div style="color:var(--muted); font-size:11.5px; line-height:1.6;">
                ⚠ วันที่ End of Support อ้างอิงจากฐานข้อมูลวงจรอายุผลิตภัณฑ์สาธารณะ (endoflife.date / Broadcom / Microsoft / Red&nbsp;Hat / Canonical) ที่ปรับปรุงล่าสุดเมื่อกันยายน 2569
                ผู้ผลิตอาจเปลี่ยนแปลงวันที่ได้ โปรดตรวจสอบกับแหล่งข้อมูลทางการอีกครั้งก่อนใช้ประกอบการตัดสินใจด้านความเสี่ยงหรืองบประมาณ
              </div>
            </div>
          </section>

          <!-- CAPACITY SIMULATOR -->
          <section class="section" id="sec-simulator">
            <div class="section-title">Compute Planning</div>
            <div class="section-desc">ปรับจำนวน Host / พื้นที่ Storage / อัตราส่วน vCPU:pCPU เพื่อดูผลกระทบต่อ % การใช้งานทรัพยากรทันที (คำนวนจากข้อมูลจัดสรร/ใช้งานจริงในไฟล์ปัจจุบัน)</div>

            <div class="panel">
              <h3>ปรับพารามิเตอร์การจำลอง</h3>
              <div style="margin-bottom:16px;">
                <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">Cluster ที่ต้องการจำลอง</label>
                <select id="simClusterSelect" style="width:100%; max-width:340px; background:#151C24; border:1px solid var(--border); color:var(--text); padding:8px; font-family:var(--mono); font-size:12px;"></select>
                <div style="font-size:10.5px; color:var(--muted); margin-top:4px;">CPU/Memory จะคำนวณเฉพาะ host และ VM ในคลัสเตอร์ที่เลือก — ส่วน Storage แสดงรวมทุก datastore เสมอ เนื่องจาก RVTools ไม่ระบุความสัมพันธ์ datastore ↔ cluster ไว้ชัดเจน</div>
              </div>
              <div id="simHostChecklistWrap" style="margin-bottom:16px; display:none;">
                <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:6px;">เลือก Host ที่จะรวมในการคำนวณของคลัสเตอร์นี้ (ติ๊กออกเพื่อจำลองย้าย/ถอด host ออก, ติ๊กเพิ่ม host จากคลัสเตอร์อื่นเพื่อจำลองย้ายเข้า)</label>
                <div id="simHostChecklist" style="max-height:180px; overflow:auto; border:1px solid var(--border); padding:8px; font-size:11.5px;"></div>
              </div>
              <div class="grid cols-2">
                <div>
                  <div style="font-size:12px; color:var(--muted); margin-bottom:10px;">Host / Server</div>
                  <div style="display:flex; align-items:center; gap:10px; margin-bottom:10px;">
                    <button id="simHostMinus" style="width:34px; padding:8px 0;">−</button>
                    <div style="flex:1; text-align:center;">
                      <div id="simHostDeltaLabel" style="font-family:var(--disp); font-size:18px; font-weight:600;">+0</div>
                      <div style="font-size:10.5px; color:var(--muted);">จำนวน host ที่เพิ่ม/ลด</div>
                    </div>
                    <button id="simHostPlus" style="width:34px; padding:8px 0;">+</button>
                  </div>
                  <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">Cores ต่อ host ใหม่ (ค่าเฉลี่ยปัจจุบัน)</label>
                  <input id="simCoresPerHost" type="number" min="1" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px; margin-bottom:8px;">
                  <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">RAM ต่อ host ใหม่ (GB)</label>
                  <input id="simRamPerHost" type="number" min="1" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px; margin-bottom:8px;">
                  <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">CPU Ratio (vCPU : pCPU) — อัตราส่วนที่ยอมรับได้ต่อ physical core</label>
                  <input id="simCpuRatio" type="number" min="0.1" step="0.5" value="4" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px;">
                  <div style="font-size:10.5px; color:var(--muted); margin-top:4px;">ใช้คำนวณ Effective CPU Capacity = Physical Cores × Ratio นี้ (ค่าทั่วไปในงาน production ประมาณ 3:1–6:1)</div>
                </div>
                <div>
                  <div style="font-size:12px; color:var(--muted); margin-bottom:10px;">Storage</div>
                  <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">เพิ่ม/ลด Storage Capacity รวม (TB) — ใส่ค่าติดลบเพื่อจำลองการลดพื้นที่</label>
                  <input id="simStorageDeltaTB" type="number" step="0.1" value="0" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px; margin-bottom:12px;">
                  <button id="simReset" style="width:100%;">รีเซ็ตกลับค่าปัจจุบัน</button>
                </div>
              </div>
            </div>

            <div class="grid cols-3">
              <div class="panel">
                <h3>CPU <span class="tag" id="sim-cpu-tag"></span></h3>
                <div class="chart-box small"><canvas id="chart-sim-cpu"></canvas></div>
              </div>
              <div class="panel">
                <h3>Memory <span class="tag" id="sim-mem-tag"></span></h3>
                <div class="chart-box small"><canvas id="chart-sim-mem"></canvas></div>
              </div>
              <div class="panel">
                <h3>Storage <span class="tag" id="sim-storage-tag"></span></h3>
                <div class="chart-box small"><canvas id="chart-sim-storage"></canvas></div>
              </div>
            </div>

            <div class="panel">
              <h3>จำลอง Host ล่ม 1 ตัว (N-1 Failure) <span class="tag" id="sim-n1-tag"></span></h3>
              <div id="sim-n1-note" class="empty-note" style="display:none;"></div>
              <div class="grid cols-2" id="sim-n1-charts">
                <div>
                  <div style="font-size:12px; color:var(--muted); margin-bottom:8px; text-align:center;">CPU หากเสีย host ที่ใหญ่ที่สุด 1 ตัว</div>
                  <div class="chart-box small"><canvas id="chart-sim-n1-cpu"></canvas></div>
                </div>
                <div>
                  <div style="font-size:12px; color:var(--muted); margin-bottom:8px; text-align:center;">Memory หากเสีย host ที่ใหญ่ที่สุด 1 ตัว</div>
                  <div class="chart-box small"><canvas id="chart-sim-n1-mem"></canvas></div>
                </div>
              </div>
              <div style="font-size:10.5px; color:var(--muted); margin-top:10px;">จำลองการนำ host ที่มี core มากที่สุดออกจากคลัสเตอร์ที่เลือก (สถานการณ์เลวร้ายที่สุดตามแนวทาง vSphere HA admission control) แล้วคำนวณ utilization ของ host ที่เหลือ โดยยังคง host ที่เพิ่ม/ลดและ CPU Ratio จากด้านบนไว้</div>
            </div>

            <div class="panel">
              <h3>สรุปเปรียบเทียบ</h3>
              <div class="table-wrap"><table id="tbl-sim-summary"></table></div>
            </div>

            <div class="panel">
              <h3>สรุปสำหรับส่งอีเมลขอ BOM และราคา</h3>
              <div id="sim-email-block"></div>
            </div>
          </section>

          <!-- NEW SIZING -->
          <section class="section" id="sec-sizing">
            <div class="section-title">New Sizing</div>
            <div class="section-desc">ใส่ความต้องการทรัพยากรของ workload ใหม่ เทียบกับสเปกเครื่อง Server ที่จะจัดหา เพื่อดูว่าเพียงพอหรือไม่ (ไม่ผูกกับข้อมูล RVTools ที่โหลดไว้ — ใช้เป็นเครื่องมือคำนวณอิสระ)</div>

            <div class="grid cols-2">
              <div class="panel">
                <h3>ความต้องการ Workload (Requirement)</h3>
                <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">vCPU ที่ต้องการ</label>
                <input id="szReqVcpu" type="number" min="0" value="32" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px; margin-bottom:10px;">
                <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">vMemory ที่ต้องการ (GB)</label>
                <input id="szReqMem" type="number" min="0" value="128" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px; margin-bottom:10px;">
                <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">vDisk ที่ต้องการ (GB)</label>
                <input id="szReqDisk" type="number" min="0" value="2000" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px;">
              </div>
              <div class="panel">
                <h3>ฝั่ง Server ที่จะจัดหา</h3>
                <div style="display:flex; align-items:center; gap:10px; margin-bottom:10px;">
                  <button id="szServerMinus" style="width:34px; padding:8px 0;">−</button>
                  <div style="flex:1; text-align:center;">
                    <div id="szServerCountLabel" style="font-family:var(--disp); font-size:18px; font-weight:600;">3</div>
                    <div style="font-size:10.5px; color:var(--muted);">จำนวนเครื่อง</div>
                  </div>
                  <button id="szServerPlus" style="width:34px; padding:8px 0;">+</button>
                </div>
                <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">CPU Core ต่อเครื่อง</label>
                <input id="szCoresPerServer" type="number" min="1" value="32" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px; margin-bottom:10px;">
                <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">Memory ต่อเครื่อง (GB)</label>
                <input id="szRamPerServer" type="number" min="1" value="256" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px; margin-bottom:10px;">
                <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">Storage รวมที่จัดหา (GB)</label>
                <input id="szStorageTotal" type="number" min="0" value="6000" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px; margin-bottom:10px;">
                <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">CPU Ratio (vCPU : pCPU)</label>
                <input id="szCpuRatio" type="number" min="0.1" step="0.5" value="4" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:7px; font-family:var(--mono); font-size:12px;">
              </div>
            </div>

            <div class="grid cols-3">
              <div class="panel">
                <h3>CPU <span class="tag" id="sz-cpu-tag"></span></h3>
                <div class="chart-box small"><canvas id="chart-sz-cpu"></canvas></div>
              </div>
              <div class="panel">
                <h3>Memory <span class="tag" id="sz-mem-tag"></span></h3>
                <div class="chart-box small"><canvas id="chart-sz-mem"></canvas></div>
              </div>
              <div class="panel">
                <h3>Storage <span class="tag" id="sz-storage-tag"></span></h3>
                <div class="chart-box small"><canvas id="chart-sz-storage"></canvas></div>
              </div>
            </div>

            <div class="panel">
              <h3>จำลอง Server ล่ม 1 เครื่อง (N-1) <span class="tag" id="sz-n1-tag"></span></h3>
              <div id="sz-n1-note" class="empty-note" style="display:none;"></div>
              <div class="grid cols-2" id="sz-n1-charts">
                <div>
                  <div style="font-size:12px; color:var(--muted); margin-bottom:8px; text-align:center;">CPU หากเสีย 1 เครื่อง</div>
                  <div class="chart-box small"><canvas id="chart-sz-n1-cpu"></canvas></div>
                </div>
                <div>
                  <div style="font-size:12px; color:var(--muted); margin-bottom:8px; text-align:center;">Memory หากเสีย 1 เครื่อง</div>
                  <div class="chart-box small"><canvas id="chart-sz-n1-mem"></canvas></div>
                </div>
              </div>
            </div>

            <div class="panel">
              <h3>สรุปเปรียบเทียบ</h3>
              <div class="table-wrap"><table id="tbl-sz-summary"></table></div>
            </div>

            <div class="panel">
              <h3>สรุปสำหรับส่งอีเมลขอ BOM และราคา</h3>
              <div id="sz-email-block"></div>
            </div>
          </section>

          <!-- UPGRADE PLAN -->
          <section class="section" id="sec-upgrade">
            <div class="section-title">Upgrade Plan</div>
            <div class="section-desc">ตรวจสอบเวอร์ชัน/build ปัจจุบันของ ESXi และ vCenter จากไฟล์ RVTools เลือกเวอร์ชันปลายทาง แล้วสร้าง action plan พร้อมตรวจสอบความเข้ากันได้ของฮาร์ดแวร์เบื้องต้น</div>

            <div class="panel">
              <h3>เวอร์ชันปัจจุบัน &amp; เลือกเวอร์ชันปลายทาง</h3>
              <div class="table-wrap"><table id="tbl-upg-current"></table></div>
              <div class="grid cols-2" style="margin-top:14px;">
                <div>
                  <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">ESXi เวอร์ชันปลายทาง</label>
                  <select id="upgEsxiTarget" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:8px; font-family:var(--mono); font-size:12px;"></select>
                </div>
                <div>
                  <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">vCenter เวอร์ชันปลายทาง</label>
                  <select id="upgVcenterTarget" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:8px; font-family:var(--mono); font-size:12px;"></select>
                </div>
              </div>
            </div>

            <div class="panel">
              <h3>ตรวจสอบ Hardware Compatibility เบื้องต้น <span class="tag" id="upg-hw-tag"></span></h3>
              <div class="table-wrap"><table id="tbl-upg-hw"></table></div>
              <div style="font-size:10.5px; color:var(--muted); margin-top:8px;">⚠ เป็นการตรวจสอบเบื้องต้นจากชื่อรุ่น CPU เท่านั้น ไม่ใช่ข้อมูลอย่างเป็นทางการ — ก่อนอัปเกรดจริงต้องตรวจสอบกับ <a href="https://www.vmware.com/resources/compatibility/search.php" target="_blank" rel="noopener">VMware Compatibility Guide (VCG)</a> โดยใช้รุ่นเครื่อง/CPU ที่แน่นอนอีกครั้ง</div>
            </div>

            <div class="panel">
              <h3>Action Plan <span class="tag" id="upg-plan-tag"></span></h3>
              <div class="table-wrap"><table id="tbl-upg-plan"></table></div>
              <div style="font-size:10.5px; color:var(--muted); margin-top:8px;">ประมาณการเวลาเป็นแนวทางเบื้องต้นสำหรับวางแผน Change Management เท่านั้น ระยะเวลาจริงขึ้นกับสภาพแวดล้อม ขนาด workload และ maintenance window ที่มี</div>
            </div>
          </section>

          <!-- DIAGRAM -->
          <section class="section" id="sec-diagram">
            <div class="section-title">Network Diagram</div>
            <div class="section-desc">แผนภาพการเชื่อมต่อของแต่ละ ESXi Host — vSwitch/dvSwitch, Port Group (พร้อม VLAN), และการจับคู่กับ Physical NIC ผ่าน Uplink</div>

            <div class="panel">
              <div class="grid cols-2">
                <div>
                  <label style="font-size:11px; color:var(--muted); display:block; margin-bottom:4px;">เลือก Host</label>
                  <select id="diagHostSelect" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:8px; font-family:var(--mono); font-size:12px;"></select>
                </div>
                <div style="display:flex; align-items:flex-end; gap:16px; font-size:11px; color:var(--muted);">
                  <div><span style="display:inline-block; width:22px; height:2px; background:#5AA9E6; vertical-align:middle;"></span> NIC → Switch (Uplink)</div>
                  <div><span style="display:inline-block; width:22px; height:2px; background:#3ED2A0; vertical-align:middle;"></span> Active (ถ้ามีข้อมูล)</div>
                  <div><span style="display:inline-block; width:22px; height:2px; background:#F0A857; vertical-align:middle; border-top:2px dashed #F0A857;"></span> Standby (ถ้ามีข้อมูล)</div>
                </div>
              </div>
            </div>

            <div class="panel">
              <h3>Diagram <span class="tag" id="diag-host-tag"></span></h3>
              <div id="diag-svg-wrap" style="overflow:auto; background:#0D1218; border:1px solid var(--border); border-radius:4px;"></div>
              <div id="diag-diagnostic-note"></div>
            </div>

            <div class="panel">
              <h3>ตารางประกอบ</h3>
              <div class="grid cols-2">
                <div>
                  <h3 style="font-size:12px;">Physical NIC</h3>
                  <div class="table-wrap"><table id="tbl-diag-nic"></table></div>
                </div>
                <div>
                  <h3 style="font-size:12px;">VMkernel Ports</h3>
                  <div class="table-wrap"><table id="tbl-diag-vmk"></table></div>
                </div>
              </div>
            </div>

            <div class="panel no-print">
              <h3>ข้อมูลดิบสำหรับ Debug <span class="tag">เผื่อ diagram ยังไม่ขึ้นเส้น</span></h3>
              <div id="diag-debug-info" style="font-size:11px; color:var(--muted); font-family:var(--mono); line-height:1.8;"></div>
            </div>
          </section>

          <!-- MFEC TEMPLATE REPORT -->
          <section class="section" id="sec-template">
            <div class="section-title">MFEC Template Report</div>
            <div class="section-desc">จัดรูปแบบข้อมูลตามโครงเทมเพลต Preventive Maintenance Report (ส่วน vCenter/VMware) — ใช้ปุ่ม "Export Report" เพื่อพิมพ์/บันทึกเป็น PDF ในรูปแบบนี้</div>

            <div class="panel no-print">
              <h3>ข้อมูลหัวรายงาน (ใช้ตอน Export เท่านั้น)</h3>
              <div class="grid cols-3">
                <div>
                  <div class="lbl" style="color:var(--muted); font-size:11px; margin-bottom:4px;">ชื่อลูกค้า / Customer</div>
                  <input id="inp-customer" type="text" placeholder="เช่น Krungthai Asset Management" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:8px; font-family:var(--mono); font-size:12px;">
                </div>
                <div>
                  <div class="lbl" style="color:var(--muted); font-size:11px; margin-bottom:4px;">วันที่ตรวจ / PM Date</div>
                  <input id="inp-pmdate" type="text" placeholder="เช่น 18 Nov 2025" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:8px; font-family:var(--mono); font-size:12px;">
                </div>
                <div>
                  <div class="lbl" style="color:var(--muted); font-size:11px; margin-bottom:4px;">ผู้จัดทำ / Prepared by</div>
                  <input id="inp-preparedby" type="text" placeholder="ชื่อผู้จัดทำรายงาน" style="width:100%; background:#151C24; border:1px solid var(--border); color:var(--text); padding:8px; font-family:var(--mono); font-size:12px;">
                </div>
              </div>
            </div>

            <div id="tpl-body"></div>

            <div class="panel no-print">
              <h3>สรุปสำหรับส่งอีเมลถึงลูกค้า</h3>
              <div id="tpl-email-block"></div>
            </div>
          </section>

          <!-- SECURITY AWARENESS -->
          <section class="section" id="sec-security">
            <div class="section-title">Security Awareness</div>
            <div class="section-desc">เทียบเวอร์ชัน/build ของ ESXi และ vCenter ที่ตรวจพบกับฐานข้อมูล VMware Security Advisory (VMSA) พร้อมระดับความรุนแรงและวิธีแก้ไข</div>
            <div class="grid cols-4" id="sec-stats"></div>

            <div class="panel">
              <h3>ช่องโหว่ที่อาจกระทบกับ Environment นี้ <span class="tag" id="sec-count-tag"></span></h3>
              <div id="sec-findings"></div>
            </div>

            <div class="panel">
              <h3>ตรวจสอบ CVE/VMSA ล่าสุดจาก NVD <span class="tag" id="sec-live-tag"></span>
                <button id="btnRefreshCve" class="no-print" style="font-size:11px; padding:4px 10px;">รีเฟรช</button>
              </h3>
              <div id="sec-live"><div class="empty-note">กำลังตรวจสอบข้อมูลล่าสุด...</div></div>
            </div>

            <div class="panel no-print" style="border-left:2px solid var(--warn);">
              <div style="color:var(--muted); font-size:11.5px; line-height:1.6;">
                ⚠ ฐานข้อมูล VMSA หลักในเครื่องมือนี้เป็นรายการที่คัดเลือกเฉพาะ Critical/Important ที่สำคัญ (ปรับปรุงล่าสุด ก.ย. 2569) <b>ไม่ใช่รายการที่ครบถ้วน 100%</b>
                การจับคู่ระดับ host ใช้เวอร์ชันหลัก (major.minor) เสมอ และจะเทียบ <b>เลข build จริง</b> เพิ่มเติมเฉพาะ VMSA ที่เครื่องมือมีเลข build อ้างอิงยืนยันแล้วเท่านั้น (จะระบุไว้ในแต่ละรายการ) — ส่วนที่เหลือต้องเทียบ build เองกับ Response Matrix
                ส่วน "ตรวจสอบ CVE/VMSA ล่าสุดจาก NVD" ด้านบนจะดึงข้อมูลจาก National Vulnerability Database แบบสดตอนนำเข้าไฟล์ เพื่อช่วยแจ้งเตือน CVE ใหม่ที่อาจยังไม่ถูกเพิ่มในฐานข้อมูลหลักของเครื่องมือนี้ (อาจใช้ไม่ได้หากเบราว์เซอร์/เครือข่ายบล็อกการเชื่อมต่อออกนอก)
                โปรดตรวจสอบ Response Matrix ของแต่ละ VMSA บน <a href="https://support.broadcom.com/web/ecx/support-content-notification" target="_blank" rel="noopener">Broadcom Support Portal</a> เพื่อความถูกต้องก่อนตัดสินใจ
              </div>
            </div>
          </section>

        </div>
      </div>
    </div>

    <script>
    (function(){

      /* ===================== nav / sections ===================== */
      var SECTIONS = [
        {id:'overview',  label:'Overview'},
        {id:'compute',   label:'Compute'},
        {id:'storage',   label:'Storage'},
        {id:'vms',       label:'Virtual Machines'},
        {id:'snapshots', label:'Snapshots'},
        {id:'tools',     label:'VMware Tools'},
        {id:'expire',    label:'Software Expire'},
        {id:'security',  label:'Security Awareness'},
        {id:'simulator', label:'Compute Planning'},
        {id:'sizing',    label:'New Sizing'},
        {id:'upgrade',   label:'Upgrade Plan'},
        {id:'diagram',   label:'Diagram'},
        {id:'template',  label:'MFEC Template Report'}
      ];
      var navEl = document.getElementById('nav');
      var loaded = false;

      function renderNav(){
        navEl.innerHTML = '';
        SECTIONS.forEach(function(s, i){
          var el = document.createElement('div');
          el.className = 'nav-item' + (i===0 ? ' active':'') + (!loaded ? ' disabled':'');
          el.innerHTML = '<span class="dot"></span>' + s.label;
          el.dataset.id = s.id;
          el.addEventListener('click', function(){
            if(!loaded) return;
            document.querySelectorAll('.nav-item').forEach(function(n){ n.classList.remove('active'); });
            document.querySelectorAll('.section').forEach(function(n){ n.classList.remove('active'); });
            el.classList.add('active');
            document.getElementById('sec-'+s.id).classList.add('active');
          });
          navEl.appendChild(el);
        });
      }
      renderNav();

      /* ===================== helpers ===================== */
      function norm(s){ return String(s==null?'':s).toLowerCase().replace(/[^a-z0-9]/g,''); }

      function findSheet(wb, candidates){
        var names = wb.SheetNames;
        for(var c=0;c<candidates.length;c++){
          var target = norm(candidates[c]);
          for(var i=0;i<names.length;i++){
            if(norm(names[i]) === target) return wb.Sheets[names[i]];
          }
        }
        for(var c2=0;c2<candidates.length;c2++){
          var target2 = norm(candidates[c2]);
          for(var i2=0;i2<names.length;i2++){
            if(norm(names[i2]).indexOf(target2) !== -1) return wb.Sheets[names[i2]];
          }
        }
        return null;
      }

      function sheetToRows(sheet){
        if(!sheet) return [];
        return XLSX.utils.sheet_to_json(sheet, {defval:null, raw:true});
      }

      // build a row-accessor tolerant to header naming variance
      function findBuildColumn(row){
        var keys = Object.keys(row);
        for(var i=0;i<keys.length;i++){
          if(norm(keys[i]).indexOf('build') !== -1){
            var v = row[keys[i]];
            if(v != null && String(v).trim() !== '') return v;
          }
        }
        return null;
      }
      function extractBuildNumber(str){
        if(str == null) return null;
        var m = String(str).match(/(\d{5,8})/);
        return m ? m[1] : null;
      }

      function col(row, candidates){
        var keys = Object.keys(row);
        for(var c=0;c<candidates.length;c++){
          var target = norm(candidates[c]);
          for(var i=0;i<keys.length;i++){
            if(norm(keys[i]) === target) return row[keys[i]];
          }
        }
        for(var c2=0;c2<candidates.length;c2++){
          var target2 = norm(candidates[c2]);
          for(var i2=0;i2<keys.length;i2++){
            if(norm(keys[i2]).indexOf(target2) !== -1) return row[keys[i2]];
          }
        }
        return null;
      }

      function toBool(v){
        if(v === true) return true;
        if(v === false || v == null) return false;
        var s = String(v).trim().toLowerCase();
        return s === 'true' || s === 'yes' || s === '1' || s === 'y';
      }
      function num(v){ var n = parseFloat(v); return isFinite(n) ? n : 0; }
      function fmt(n, d){ d = d==null?0:d; return Number(n).toLocaleString('en-US', {minimumFractionDigits:d, maximumFractionDigits:d}); }
      function mibToGB(mib){ return num(mib)/1024; }
      function mibToTB(mib){ return num(mib)/1024/1024; }
      function esc(s){ return String(s==null?'':s).replace(/[&<>"]/g, function(c){ return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]; }); }
      function pillClass(pct){ return pct>=90?'bad':(pct>=75?'warn':'ok'); }
      function barClass(pct){ return pct>=90?'bad':(pct>=75?'warn':''); }

      var CHARTS = {};
      function killChart(id){ if(CHARTS[id]){ CHARTS[id].destroy(); delete CHARTS[id]; } }
      var palette = ['#3ED2A0','#5AA9E6','#F0A857','#E2596B','#9B8CF2','#6FCF97','#56CCF2','#F2C94C','#EB5757','#BB6BD9'];

      /* ===================== end-of-support lifecycle database ===================== */
      // Reference snapshot: September 2026 — vendor pages can change these, always re-verify for real risk decisions.
      var VSPHERE_EOL = {
        '5.0':{eogs:'2016-08-24'}, '5.1':{eogs:'2016-08-24'}, '5.5':{eogs:'2018-09-19'},
        '6.0':{eogs:'2020-03-12'}, '6.5':{eogs:'2022-10-15'}, '6.7':{eogs:'2022-10-15'},
        '7.0':{eogs:'2025-10-02'}, '8.0':{eogs:'2027-10-11'},
        '9.0':{eogs:'2027-09-17'}, '9.1':{eogs:'2028-08-12'}
      };
      function versionKey(v){
        var m = String(v==null?'':v).match(/(\d+)\.(\d+)/);
        return m ? (m[1]+'.'+m[2]) : null;
      }

      var OS_EOL_RULES = [
        {re:/windows server 2025/i, label:'Windows Server 2025', eol:'2034-11-13'},
        {re:/windows server 2022/i, label:'Windows Server 2022', eol:'2031-10-14'},
        {re:/windows server 2019/i, label:'Windows Server 2019', eol:'2029-01-09'},
        {re:/windows server 2016/i, label:'Windows Server 2016', eol:'2027-01-12'},
        {re:/windows server 2012\s*r2/i, label:'Windows Server 2012 R2 (ESU)', eol:'2026-10-13'},
        {re:/windows server 2012/i, label:'Windows Server 2012 (ESU)', eol:'2026-10-13'},
        {re:/windows server 2008\s*r2/i, label:'Windows Server 2008 R2', eol:'2020-01-14'},
        {re:/windows server 2008/i, label:'Windows Server 2008', eol:'2020-01-14'},
        {re:/windows server 2003/i, label:'Windows Server 2003', eol:'2015-07-14'},
        {re:/windows\s*11/i, label:'Windows 11', eol:'2026-10-13', approx:true},
        {re:/windows\s*10/i, label:'Windows 10', eol:'2025-10-14'},
        {re:/windows\s*8\.1/i, label:'Windows 8.1', eol:'2023-01-10'},
        {re:/windows\s*8/i, label:'Windows 8', eol:'2016-01-12'},
        {re:/windows\s*7/i, label:'Windows 7', eol:'2020-01-14'},
        {re:/windows xp/i, label:'Windows XP', eol:'2014-04-08'},
        {re:/red hat enterprise linux 10|rhel\s*10/i, label:'RHEL 10', eol:'2035-05-31'},
        {re:/red hat enterprise linux 9|rhel\s*9/i, label:'RHEL 9', eol:'2032-05-31'},
        {re:/red hat enterprise linux 8|rhel\s*8/i, label:'RHEL 8', eol:'2029-05-31'},
        {re:/red hat enterprise linux 7|rhel\s*7/i, label:'RHEL 7', eol:'2024-06-30'},
        {re:/red hat enterprise linux 6|rhel\s*6/i, label:'RHEL 6', eol:'2020-11-30'},
        {re:/red hat enterprise linux 5|rhel\s*5/i, label:'RHEL 5', eol:'2017-03-31'},
        {re:/centos stream 9/i, label:'CentOS Stream 9', eol:'2027-05-31', approx:true},
        {re:/centos stream 8/i, label:'CentOS Stream 8', eol:'2024-05-31'},
        {re:/centos.*8/i, label:'CentOS 8', eol:'2021-12-31'},
        {re:/centos.*7/i, label:'CentOS 7', eol:'2024-06-30'},
        {re:/centos.*6/i, label:'CentOS 6', eol:'2020-11-30'},
        {re:/oracle linux.*9|oracle linux 9/i, label:'Oracle Linux 9', eol:'2032-06-30', approx:true},
        {re:/oracle linux.*8|oracle linux 8/i, label:'Oracle Linux 8', eol:'2029-07-31', approx:true},
        {re:/oracle linux.*7|oracle linux 7/i, label:'Oracle Linux 7', eol:'2024-07-31', approx:true},
        {re:/ubuntu.*24\.04|noble/i, label:'Ubuntu 24.04 LTS', eol:'2029-05-31'},
        {re:/ubuntu.*22\.04|jammy/i, label:'Ubuntu 22.04 LTS', eol:'2027-05-31'},
        {re:/ubuntu.*20\.04|focal/i, label:'Ubuntu 20.04 LTS', eol:'2025-05-31'},
        {re:/ubuntu.*18\.04|bionic/i, label:'Ubuntu 18.04 LTS', eol:'2023-05-31'},
        {re:/ubuntu.*16\.04/i, label:'Ubuntu 16.04 LTS', eol:'2021-04-30'},
        {re:/ubuntu/i, label:'Ubuntu (ไม่ระบุเวอร์ชัน)', eol:null},
        {re:/suse.*15/i, label:'SUSE Linux Enterprise 15', eol:'2028-07-31', approx:true},
        {re:/suse.*12/i, label:'SUSE Linux Enterprise 12', eol:'2024-10-31', approx:true},
        {re:/debian.*12|bookworm/i, label:'Debian 12', eol:'2028-06-30', approx:true},
        {re:/debian.*11|bullseye/i, label:'Debian 11', eol:'2026-08-31', approx:true},
        {re:/debian.*10|buster/i, label:'Debian 10', eol:'2024-06-30'}
      ];
      function findOsEol(osString){
        var s = String(osString||'');
        for(var i=0;i<OS_EOL_RULES.length;i++){
          if(OS_EOL_RULES[i].re.test(s)) return OS_EOL_RULES[i];
        }
        return null;
      }
      function daysUntil(dateStr){
        if(!dateStr) return null;
        var d = new Date(dateStr+'T00:00:00');
        if(isNaN(d)) return null;
        return Math.floor((d.getTime()-Date.now())/86400000);
      }
      function expireStatus(days){
        if(days==null) return {cls:'', label:'ไม่ทราบ'};
        if(days<0) return {cls:'bad', label:'หมดอายุแล้ว'};
        if(days<=180) return {cls:'warn', label:'ใกล้หมดอายุ'};
        return {cls:'ok', label:'ปกติ'};
      }

      /* ===================== VMSA security advisory database (curated, reference snapshot Sep 2026) ===================== */
      var VMSA_DB = [
        {
          id:'VMSA-2026-0006.1', cves:['CVE-2026-59309','CVE-2026-59310'], cvss:9.8, sev:'critical',
          title:'vCenter Directory Service auth bypass + Syslog directory traversal RCE',
          affects:{vcenter:['6.5','6.7','7.0','8.0','9.0','9.1'], esxi:[]},
          fixed:'อัปเดต vCenter เป็นเวอร์ชันล่าสุดตาม Response Matrix ของ VMSA-2026-0006.1 (ไม่มี workaround)',
          itw:false,
          remediation:'แพตช์ vCenter ทันทีตาม Response Matrix — ไม่มี workaround ให้ใช้ระหว่างรอ จำกัดการเข้าถึงเครือข่ายไปยัง vCenter ผ่าน firewall/jump host ระหว่างรอแพตช์'
        },
        {
          id:'VMSA-2026-0006.1', cves:['CVE-2026-47876'], cvss:9.3, sev:'critical',
          title:'ESXi VMXNET3 virtual NIC — VM escape to host',
          affects:{vcenter:[], esxi:['7.0','8.0','9.0']},
          fixed:'อัปเดต ESXi ตาม Response Matrix ของ VMSA-2026-0006.1',
          itw:false,
          remediation:'แพตช์ ESXi ตาม Response Matrix; ตรวจสอบว่ามี VM ใดใช้ VMXNET3 adapter และจำกัดสิทธิ์ administrative ภายใน guest VM เหล่านั้น (ไม่มี workaround อื่น)'
        },
        {
          id:'VMSA-2025-0016', cves:['CVE-2025-41250','CVE-2025-41251'], cvss:8.5, sev:'important',
          title:'vCenter SMTP header injection + NSX username enumeration',
          affects:{vcenter:['7.0','8.0'], esxi:[]},
          fixed:'อัปเดต vCenter/NSX ตาม Response Matrix ของ VMSA-2025-0016',
          itw:false,
          remediation:'อัปเดตตาม Response Matrix; จำกัดสิทธิ์ผู้ใช้ที่สร้าง scheduled task บน vCenter'
        },
        {
          id:'VMSA-2025-0013', cves:['CVE-2025-41236','CVE-2025-41237','CVE-2025-41238','CVE-2025-41239'], cvss:9.3, sev:'critical',
          title:'ESXi VMXNET3 virtual NIC integer-overflow + related device-emulation flaws — VM escape',
          affects:{vcenter:[], esxi:['7.0','8.0']},
          fixed:'อัปเดต ESXi ตาม Response Matrix ของ VMSA-2025-0013 (patch ก.ย. 2568)',
          itw:false,
          remediation:'แพตช์ ESXi โดยเร็ว; จำกัด local administrative privileges บน guest VM เนื่องจากเป็นช่องทางโจมตีหลัก'
        },
        {
          id:'VMSA-2025-0004', cves:['CVE-2025-22224','CVE-2025-22225','CVE-2025-22226'], cvss:9.3, sev:'critical',
          title:'ESXi VMX TOCTOU heap-overflow + arbitrary write + info leak — VM escape',
          affects:{vcenter:[], esxi:['7.0','8.0']},
          fixed:'ESXi 8.0 Update 3d (build 24585383) / 7.0 Update 3s หรือใหม่กว่า — ตรวจสอบ Response Matrix สำหรับสาย 8.0 Update 2',
          fixedBuildEsxi:{'8.0':24585383},
          itw:true,
          remediation:'แพตช์ด่วน — Broadcom ยืนยันมีการโจมตีจริงในธรรมชาติ (active exploitation) ตรวจสอบ log การเข้าถึง VMX process และ isolate host ที่สงสัยว่าถูกบุกรุก'
        },
        {
          id:'VMSA-2024-0019', cves:['CVE-2024-38812','CVE-2024-38813'], cvss:9.8, sev:'critical',
          title:'vCenter DCERPC heap-overflow RCE + privilege escalation (ต้องแพตช์ซ้ำ 2 ครั้ง)',
          affects:{vcenter:['7.0','8.0'], esxi:[]},
          fixed:'vCenter 8.0U3b / 7.0U3s หรือใหม่กว่า (แพตช์ครั้งแรก ก.ย. 2567 ไม่สมบูรณ์ ต้องอัปเดตซ้ำตาม VMSA-2024-0019 revision ล่าสุด)',
          itw:false,
          remediation:'ตรวจสอบว่าอัปเดตเป็นเวอร์ชันล่าสุดของ VMSA-2024-0019 (ไม่ใช่แค่แพตช์แรกที่ไม่สมบูรณ์) และปิดการเข้าถึง vCenter จากเครือข่ายที่ไม่จำเป็น'
        },
        {
          id:'VMSA-2024-0013', cves:['CVE-2024-37085','CVE-2024-37086','CVE-2024-37087'], cvss:6.8, sev:'important',
          title:'ESXi Active Directory auth bypass ("ESX Admins" group) — ถูกใช้โดยกลุ่ม Ransomware',
          affects:{vcenter:[], esxi:['7.0','8.0']},
          fixed:'ESXi 8.0 Update 3 / 7.0 Update 3 หรือใหม่กว่า',
          itw:true,
          remediation:'แพตช์ ESXi; หากไม่ได้ใช้ AD integration ให้ลบการตั้งค่านี้ออก; ตรวจสอบกลุ่ม AD ชื่อ "ESX Admins" ว่าไม่ถูกสร้างใหม่โดยไม่ได้รับอนุญาต — เคยถูกใช้โดยกลุ่ม ransomware (Akira/Storm-0506) เพื่อยึดครอง ESXi ทั้งคลัสเตอร์'
        },
        {
          id:'VMSA-2024-0012', cves:['CVE-2024-37079','CVE-2024-37080','CVE-2024-37081'], cvss:9.8, sev:'critical',
          title:'vCenter DCERPC heap-overflow RCE + local privilege escalation',
          affects:{vcenter:['7.0','8.0'], esxi:[]},
          fixed:'ตรวจสอบ Response Matrix ของ VMSA-2024-0012 (อัปเดตพร้อมกับ VMSA-2024-0013)',
          itw:true,
          remediation:'แพตช์ vCenter โดยด่วน — Broadcom ยืนยันมีการโจมตีจริง จำกัดการเข้าถึงเครือข่ายของ vCenter'
        },
        {
          id:'VMSA-2024-0006', cves:['CVE-2024-22252','CVE-2024-22253'], cvss:9.3, sev:'critical',
          title:'ESXi USB (UHCI/XHCI) use-after-free — VM escape (Pwn2Own)',
          affects:{vcenter:[], esxi:['7.0','8.0']},
          fixed:'ESXi 8.0 Update 2b / 7.0 Update 3p หรือใหม่กว่า',
          itw:false,
          remediation:'แพตช์ ESXi; ปลด USB controller ออกจาก VM ที่ไม่จำเป็นต้องใช้เป็น workaround ชั่วคราว'
        },
        {
          id:'VMSA-2023-0023', cves:['CVE-2023-34048','CVE-2023-34056'], cvss:9.8, sev:'critical',
          title:'vCenter out-of-bounds write ใน DCERPC — ถูกกลุ่ม APT (UNC3886) ใช้โจมตีจริงตั้งแต่ปี 2021',
          affects:{vcenter:['6.7','7.0','8.0'], esxi:[]},
          fixed:'vCenter 8.0U2 / 7.0U3o / 6.7U3u หรือใหม่กว่า',
          itw:true,
          remediation:'แพตช์ทันที — มีรายงานว่าถูกกลุ่ม APT ใช้เจาะระบบมาตั้งแต่ปี 2021 ก่อนที่จะมีการเปิดเผยช่องโหว่นี้; ตรวจสอบ log และ indicators of compromise ย้อนหลังหากยังไม่เคยแพตช์'
        },
        {
          id:'VMSA-2021-0002', cves:['CVE-2021-21985','CVE-2021-21986'], cvss:9.8, sev:'critical',
          title:'vCenter Server vSphere Client (HTML5) plug-in RCE',
          affects:{vcenter:['6.5','6.7','7.0'], esxi:[]},
          fixed:'vCenter 7.0U2b / 6.7U3n / 6.5U3p หรือใหม่กว่า',
          itw:true,
          remediation:'แพตช์ vCenter — ช่องโหว่นี้ถูกสแกน/โจมตีอย่างแพร่หลายทั่วโลกหลังเปิดเผย; หากยังไม่ได้แพตช์ควรถือว่ามีความเสี่ยงสูงมาก'
        }
      ];
      function sevRank(s){ return s==='critical'?3:(s==='important'?2:1); }
      function sevLabel(s){ return s==='critical'?'Critical':(s==='important'?'Important':'Moderate'); }
      function sevClass(s){ return s==='critical'?'bad':(s==='important'?'warn':''); }

      /* ===================== main processing ===================== */
      function processWorkbook(wb, filename){
        var vInfo      = sheetToRows(findSheet(wb, ['vInfo']));
        var vHost      = sheetToRows(findSheet(wb, ['vHost']));
        var vCluster   = sheetToRows(findSheet(wb, ['vCluster']));
        var vDatastore = sheetToRows(findSheet(wb, ['vDatastore','vDatastores']));
        var vSnapshot  = sheetToRows(findSheet(wb, ['vSnapshot']));
        var vTools     = sheetToRows(findSheet(wb, ['vTools']));
        var vMemory    = sheetToRows(findSheet(wb, ['vMemory']));
        var vSwitch    = sheetToRows(findSheet(wb, ['vSwitch']));
        var vPort      = sheetToRows(findSheet(wb, ['vPort','vPortgroup','vPortGroup','vPortG']));
        var dvSwitch   = sheetToRows(findSheet(wb, ['dvSwitch']));
        var dvPort     = sheetToRows(findSheet(wb, ['dvPort','dvPortgroup','dvPortGroup']));
        var vNicPhys   = sheetToRows(findSheet(wb, ['vNIC']));
        var vSC_VMK    = sheetToRows(findSheet(wb, ['vSC_VMK','vSC VMK','vSCVMK','vKernel']));

        // diagnostics: raw sheet names + headers, to help debug column-name mismatches
        var netDiag = {
          allSheetNames: wb.SheetNames.slice(),
          vSwitchHeaders: vSwitch.length ? Object.keys(vSwitch[0]) : null,
          vPortHeaders: vPort.length ? Object.keys(vPort[0]) : null,
          dvSwitchHeaders: dvSwitch.length ? Object.keys(dvSwitch[0]) : null,
          dvPortHeaders: dvPort.length ? Object.keys(dvPort[0]) : null,
          vNicHeaders: vNicPhys.length ? Object.keys(vNicPhys[0]) : null,
          vSCVMKHeaders: vSC_VMK.length ? Object.keys(vSC_VMK[0]) : null
        };

        // ---- per-VM actual memory usage (vMemory tab: "Active" = actively used memory) ----
        var memActiveByVm = {};
        vMemory.forEach(function(r){
          var nm = col(r,['VM']);
          if(!nm) return;
          var active = col(r,['Active']);
          if(active == null) return;
          memActiveByVm[nm] = num(active);
        });
        var hasMemUsageData = Object.keys(memActiveByVm).length > 0;

        // ---- normalize VM rows ----
        var vms = vInfo.map(function(r){
          var vmName = col(r, ['VM']);
          return {
            name: vmName,
            power: String(col(r,['Powerstate','Power State'])||'').toLowerCase(),
            template: toBool(col(r,['Template'])),
            cpus: num(col(r,['CPUs','Num CPU','NumCpu'])),
            memMiB: num(col(r,['Memory','Memory MiB','Memory MB'])),
            memActiveMiB: memActiveByVm[vmName] != null ? memActiveByVm[vmName] : null,
            provMiB: num(col(r,['Provisioned MiB','Provisioned MB','Provisioned'])),
            usedMiB: num(col(r,['In Use MiB','In Use MB','In Use'])),
            os: col(r,['OS according to the VMware Tools','OS according to the configuration file','Guest OS','OS']) || 'ไม่ระบุ',
            cluster: col(r,['Cluster']) || '-',
            host: col(r,['Host']) || '-',
            toolsStatus: col(r,['Tools','Tools status']) || null,
            viSdkApiVersion: col(r,['VI SDK API Version','VI SDK Server Version']),
            viSdkServer: col(r,['VI SDK Server']),
            viSdkServerType: col(r,['VI SDK Server type','VI SDK Server Type']),
            viSdkBuild: (function(){
              var b = col(r,['VI SDK Server Build','VI SDK Build','vCenter Build','vCenter Server Build']);
              if(b == null || String(b).trim()==='') b = findBuildColumn(r);
              if(b == null || String(b).trim()===''){
                b = extractBuildNumber(col(r,['VI SDK API Version','VI SDK Server Version']));
              }
              return b;
            })()
          };
        }).filter(function(v){ return v.name; });

        var realVms = vms.filter(function(v){ return !v.template; });

        // ---- vCenter version: take the first non-empty VI SDK API Version found across vInfo rows ----
        var vCenterApiVersion = null;
        var vCenterServer = null;
        var vCenterBuild = null;
        var vCenterServerType = null;
        for(var i=0;i<vms.length;i++){
          if(vms[i].viSdkApiVersion){ vCenterApiVersion = vms[i].viSdkApiVersion; }
          if(vms[i].viSdkServer){ vCenterServer = vms[i].viSdkServer; }
          if(vms[i].viSdkBuild){ vCenterBuild = vms[i].viSdkBuild; }
          if(vms[i].viSdkServerType){ vCenterServerType = vms[i].viSdkServerType; }
          if(vCenterApiVersion && vCenterServer && vCenterBuild && vCenterServerType) break;
        }
        // "Current version of customer" per request: use VI SDK Server type value when present; fall back to VI SDK API Version.
        // EOL/build matching always uses vCenterApiVersion (needs numeric major.minor form).
        var vCenterDisplayVersion = vCenterServerType || vCenterApiVersion;

        // ---- hosts ----
        var hosts = vHost.map(function(r){
          var cores = num(col(r,['# Cores','NumCores','Cores']));
          var numCpuPkgs = num(col(r,['# CPU','NumCpu']));
          var memMiB = num(col(r,['# Memory','Memory MiB','Memory']));
          return {
            name: col(r,['Host']),
            cluster: col(r,['Cluster']) || '-',
            cpuModel: col(r,['CPU Model']) || '-',
            cores: cores,
            memMiB: memMiB,
            vmCount: num(col(r,['# VMs','NumVMs','VMs'])),
            esxVersion: col(r,['ESX Version','Version','ESXi Version']),
            esxBuild: (function(){
              var b = col(r,['Build','ESX Build','Build Number','Patch Build']);
              if(b == null || String(b).trim()==='') b = findBuildColumn(r);
              if(b == null || String(b).trim()===''){
                b = extractBuildNumber(col(r,['ESX Version','Version','ESXi Version']));
              }
              return b;
            })(),
            cpuUsagePct: col(r,['CPU usage %','CPU Usage %']) != null ? num(col(r,['CPU usage %','CPU Usage %'])) : null,
            memUsagePct: col(r,['Memory usage %','Memory Usage %']) != null ? num(col(r,['Memory usage %','Memory Usage %'])) : null
          };
        }).filter(function(h){ return h.name; });


        // ---- clusters ----
        var clusterMap = {};
        vCluster.forEach(function(r){
          var nm = col(r,['Name']);
          if(!nm) return;
          clusterMap[nm] = {
            name: nm,
            hosts: num(col(r,['NumHosts','# Hosts'])),
            cpuCores: num(col(r,['NumCpuCores','Num Cpu Cores'])),
            memMiB: num(col(r,['TotalMemory','Total Memory']))/ (col(r,['TotalMemory'])>100000000?1024*1024: 1) // some exports store bytes
          };
        });
        // fallback: derive clusters from vHost + vInfo if vCluster sheet absent/incomplete
        var clusterAgg = {};
        function ensureCluster(name){
          if(!clusterAgg[name]) clusterAgg[name] = {name:name, hosts:0, cores:0, memMiB:0, vms:0, vcpu:0, vramMiB:0, cpuUsageSum:0, memUsageSum:0, usageCount:0};
          return clusterAgg[name];
        }
        hosts.forEach(function(h){
          var c = ensureCluster(h.cluster||'-');
          c.hosts += 1; c.cores += h.cores; c.memMiB += h.memMiB;
          if(h.cpuUsagePct!=null || h.memUsagePct!=null){
            c.cpuUsageSum += (h.cpuUsagePct||0); c.memUsageSum += (h.memUsagePct||0); c.usageCount += 1;
          }
        });
        realVms.forEach(function(v){
          var c = ensureCluster(v.cluster||'-');
          c.vms += 1; c.vcpu += v.cpus; c.vramMiB += v.memMiB;
        });

        // ---- datastores ----
        var datastores = vDatastore.map(function(r){
          var capMiB = num(col(r,['Capacity MiB','Capacity MB','Capacity']));
          var freeMiB = num(col(r,['Free MiB','Free MB','Free']));
          var provMiB = num(col(r,['Provisioned MiB','Provisioned MB','Provisioned']));
          var usedMiB = capMiB - freeMiB;
          return {
            name: col(r,['Name']),
            type: col(r,['Type']) || '-',
            capMiB: capMiB, freeMiB: freeMiB, provMiB: provMiB, usedMiB: usedMiB,
            usedPct: capMiB>0 ? (usedMiB/capMiB*100) : 0,
            vmCount: num(col(r,['# VMs','NumVMs']))
          };
        }).filter(function(d){ return d.name; });

        // ---- snapshots ----
        var snapshots = vSnapshot.map(function(r){
          var dt = col(r,['Date / time','Date/Time','Create Time','Created']);
          var d = dt ? new Date(dt) : null;
          var ageDays = (d && !isNaN(d)) ? Math.floor((Date.now()-d.getTime())/86400000) : null;
          return {
            vm: col(r,['VM']),
            name: col(r,['Name']),
            desc: col(r,['Description'])||'',
            date: d,
            ageDays: ageDays,
            sizeMiB: num(col(r,['Size MiB (total)','Size MiB (vmsn)','Size MiB','Size MB (total)','Size MB']))
          };
        }).filter(function(s){ return s.vm; });

        // ---- tools ----
        var toolsRows = vTools.map(function(r){
          return {
            vm: col(r,['VM']),
            status: (col(r,['Tools','Tools status','Status'])||'').toString()
          };
        }).filter(function(t){ return t.vm; });

        // ---- networking: switches, port groups, physical NICs, VMkernel ports ----
        function splitNicList(v){
          if(v == null) return [];
          return String(v).split(/[,;|\/\s]+/).map(function(s){ return s.trim(); }).filter(Boolean);
        }
        function mapSwitchRows(rows, isDistributed){
          return rows.map(function(r){
            return {
              host: col(r,['Host']),
              name: col(r,['Switch']),
              ports: num(col(r,['# Ports','NumPorts'])),
              freePorts: num(col(r,['# Free Ports','Free Ports'])),
              mtu: num(col(r,['MTU'])),
              width: num(col(r,['Width','# NICs'])),
              distributed: !!isDistributed
            };
          }).filter(function(s){ return s.host && s.name; });
        }
        var switches = mapSwitchRows(vSwitch, false).concat(mapSwitchRows(dvSwitch, true));

        function mapPortRows(rows, isDistributed){
          return rows.map(function(r){
            return {
              host: col(r,['Host']),
              switch: col(r,['Switch','vSwitch','SwitchName']),
              name: col(r,['Port Group','PortGroup','Port Group Name']),
              vlan: col(r,['VLAN','VLAN ID']),
              type: col(r,['Type']),
              activeNics: splitNicList(col(r,['Active NIC','Active Nics','Active NICs','Active Uplink','Active Uplinks','Active adapters','NIC Teaming Active'])),
              standbyNics: splitNicList(col(r,['Standby NIC','Standby Nics','Standby NICs','Standby Uplink','Standby Uplinks','Standby adapters','NIC Teaming Standby'])),
              unusedNics: splitNicList(col(r,['Unused NIC','Unused Nics','Unused NICs','Unused Uplink','Unused Uplinks','Unused adapters','NIC Teaming Unused'])),
              distributed: !!isDistributed
            };
          }).filter(function(p){ return p.host && p.name; });
        }
        var portgroups = mapPortRows(vPort, false).concat(mapPortRows(dvPort, true));

        var pnics = vNicPhys.map(function(r){
          return {
            host: col(r,['Host']),
            name: col(r,['Network Device','Device','NIC']),
            driver: col(r,['Driver']),
            speed: col(r,['Speed']),
            mac: col(r,['MAC Address','MAC']),
            switch: col(r,['Switch']),
            uplinkPort: col(r,['Uplink port','Uplink Port','UplinkPort','Uplink'])
          };
        }).filter(function(n){ return n.host && n.name; });

        var vmkernels = vSC_VMK.map(function(r){
          return {
            host: col(r,['Host']),
            portgroup: col(r,['Port Group','PortGroup']),
            switch: col(r,['Switch']),
            device: col(r,['VMkernel Port','Device','Port']),
            ip: col(r,['IP Address']),
            mgmtTraffic: col(r,['Mgmt Traffic']),
            vmotionTraffic: col(r,['vMotion Traffic'])
          };
        }).filter(function(k){ return k.host; });

        return {filename, vms, realVms, hosts, clusterAgg, datastores, snapshots, toolsRows, hasMemUsageData, vCenterApiVersion, vCenterServer, vCenterBuild, vCenterServerType, vCenterDisplayVersion, switches, portgroups, pnics, vmkernels, netDiag};
      }

      /* ===================== renderers ===================== */
      function renderOverview(D){
        var total = D.realVms.length;
        var on = D.realVms.filter(function(v){return v.power.indexOf('on')!==-1;}).length;
        var off = D.realVms.filter(function(v){return v.power.indexOf('off')!==-1;}).length;
        var susp = D.realVms.filter(function(v){return v.power.indexOf('suspend')!==-1;}).length;
        var templates = D.vms.filter(function(v){return v.template;}).length;
        var totalCPU = D.realVms.reduce(function(a,v){return a+v.cpus;},0);
        var totalMemGB = mibToGB(D.realVms.reduce(function(a,v){return a+v.memMiB;},0));
        var totalProvTB = mibToTB(D.datastores.reduce(function(a,v){return a+v.provMiB;},0));
        var totalUsedTB = mibToTB(D.datastores.reduce(function(a,v){return a+v.usedMiB;},0));
        var totalCapTB = mibToTB(D.datastores.reduce(function(a,v){return a+v.capMiB;},0));
        var clusterCount = Object.keys(D.clusterAgg).length;

        var stats = [
          {lbl:'Virtual Machines', val:fmt(total), sub:on+' เปิด · '+off+' ปิด · '+susp+' suspend', cls:'ok'},
          {lbl:'Templates', val:fmt(templates), sub:'VM template ทั้งหมด', cls:''},
          {lbl:'ESXi Hosts', val:fmt(D.hosts.length), sub:clusterCount+' cluster', cls:'info'},
          {lbl:'Datastores', val:fmt(D.datastores.length), sub:fmt(totalCapTB,1)+' TB รวม', cls:'info'},
          {lbl:'vCPU จัดสรร', val:fmt(totalCPU), sub:'รวมทุก VM ที่เปิด/ปิดอยู่', cls:''},
          {lbl:'vRAM จัดสรร', val:fmt(totalMemGB,0)+' GB', sub:'รวมทุก VM', cls:''},
          {lbl:'Storage Provisioned', val:fmt(totalProvTB,2)+' TB', sub:'thin/thick รวมกัน', cls:'warn'},
          {lbl:'Storage Used', val:fmt(totalUsedTB,2)+' TB', sub:'พื้นที่ที่ใช้จริงบน datastore', cls:''}
        ];
        document.getElementById('ov-stats').innerHTML = stats.map(function(s){
          return '<div class="stat '+s.cls+'"><div class="lbl">'+s.lbl+'</div><div class="val">'+s.val+'</div><div class="sub">'+s.sub+'</div></div>';
        }).join('');

        // findings
        var findings = [];
        var lowDS = D.datastores.filter(function(d){return d.usedPct>=90;});
        if(lowDS.length) findings.push({sev:'bad', txt:'<b>'+lowDS.length+' datastore</b> มีพื้นที่ใช้งานเกิน 90% ('+lowDS.slice(0,3).map(function(d){return esc(d.name);}).join(', ')+(lowDS.length>3?' ...':'')+')'});
        var oldSnap = D.snapshots.filter(function(s){return s.ageDays!=null && s.ageDays>30;});
        if(oldSnap.length) findings.push({sev:'warn', txt:'<b>'+oldSnap.length+' snapshot</b> มีอายุมากกว่า 30 วัน ควรพิจารณาลบหรือรวม'});
        if(D.snapshots.length && !oldSnap.length) findings.push({sev:'ok', txt:'พบ snapshot ทั้งหมด '+D.snapshots.length+' รายการ ไม่มีรายการเก่าเกิน 30 วัน'});
        var toolsBad = D.toolsRows.filter(function(t){ var s=norm(t.status); return s.indexOf('notrunning')!==-1||s.indexOf('notinstalled')!==-1; });
        if(toolsBad.length) findings.push({sev:'warn', txt:'<b>'+toolsBad.length+' VM</b> ที่ VMware Tools ไม่ได้ติดตั้งหรือไม่ทำงาน'});
        if(off > 0) findings.push({sev:'warn', txt:'<b>'+off+' VM</b> อยู่ในสถานะปิดเครื่อง — ตรวจสอบว่ายังจำเป็นต้องเก็บไว้หรือไม่ เพื่อคืนทรัพยากร'});
        if(!findings.length) findings.push({sev:'ok', txt:'ไม่พบประเด็นผิดปกติที่เด่นชัดจากข้อมูลที่นำเข้า'});
        document.getElementById('ov-findings').innerHTML = findings.map(function(f){
          return '<li><span class="sev '+f.sev+'"></span><span class="txt">'+f.txt+'</span></li>';
        }).join('');

        // power chart
        killChart('power');
        CHARTS.power = new Chart(document.getElementById('chart-power'), {
          type:'doughnut',
          data:{ labels:['เปิดเครื่อง','ปิดเครื่อง','Suspended'], datasets:[{data:[on,off,susp], backgroundColor:['#3ED2A0','#E2596B','#F0A857'], borderColor:'#1A222B', borderWidth:2}] },
          options:{ plugins:{legend:{position:'bottom', labels:{color:'#8B96A3', font:{size:11}}}}, maintainAspectRatio:false }
        });

        // OS distribution
        var osCount = {};
        D.realVms.forEach(function(v){ var k=String(v.os).trim()||'ไม่ระบุ'; osCount[k]=(osCount[k]||0)+1; });
        var osSorted = Object.keys(osCount).map(function(k){return {k:k,v:osCount[k]};}).sort(function(a,b){return b.v-a.v;}).slice(0,10);
        killChart('os');
        CHARTS.os = new Chart(document.getElementById('chart-os'), {
          type:'bar',
          data:{ labels: osSorted.map(function(o){return o.k.length>26?o.k.slice(0,26)+'…':o.k;}), datasets:[{data:osSorted.map(function(o){return o.v;}), backgroundColor:'#5AA9E6'}] },
          options:{ indexAxis:'y', plugins:{legend:{display:false}}, scales:{x:{ticks:{color:'#8B96A3'}, grid:{color:'#232C36'}}, y:{ticks:{color:'#8B96A3', font:{size:10}}, grid:{display:false}}}, maintainAspectRatio:false }
        });
      }

      function renderCompute(D){
        var totalCores = D.hosts.reduce(function(a,h){return a+h.cores;},0);
        var totalHostMemGB = mibToGB(D.hosts.reduce(function(a,h){return a+h.memMiB;},0));
        var totalVCPU = D.realVms.reduce(function(a,v){return a+v.cpus;},0);
        var totalVRAMGB = mibToGB(D.realVms.reduce(function(a,v){return a+v.memMiB;},0));
        var cpuRatio = totalCores>0 ? (totalVCPU/totalCores) : 0;
        var ramRatio = totalHostMemGB>0 ? (totalVRAMGB/totalHostMemGB) : 0;

        document.getElementById('cp-stats').innerHTML = [
          {lbl:'Physical Cores', val:fmt(totalCores), sub:D.hosts.length+' host'},
          {lbl:'vCPU : pCore', val:fmt(cpuRatio,2)+' : 1', sub: cpuRatio>4 ? 'อัตราส่วนสูง ควรตรวจสอบ' : 'อยู่ในเกณฑ์ปกติ'},
          {lbl:'Physical RAM', val:fmt(totalHostMemGB,0)+' GB', sub:'รวมทุก host'},
          {lbl:'vRAM : pRAM', val:fmt(ramRatio,2)+' : 1', sub: ramRatio>1.5 ? 'จัดสรรเกินทรัพยากรจริง' : 'อยู่ในเกณฑ์ปกติ'}
        ].map(function(s){ return '<div class="stat"><div class="lbl">'+s.lbl+'</div><div class="val">'+s.val+'</div><div class="sub">'+s.sub+'</div></div>'; }).join('');

        var clusters = Object.keys(D.clusterAgg).map(function(k){return D.clusterAgg[k];}).sort(function(a,b){return b.vms-a.vms;});
        document.getElementById('tbl-cluster').innerHTML =
          '<thead><tr><th>Cluster</th><th class="num">Hosts</th><th class="num">VMs</th><th class="num">Physical Cores</th><th class="num">vCPU</th><th class="num">Physical RAM (GB)</th><th class="num">vRAM (GB)</th></tr></thead><tbody>'+
          clusters.map(function(c){
            return '<tr><td>'+esc(c.name)+'</td><td class="num">'+fmt(c.hosts)+'</td><td class="num">'+fmt(c.vms)+'</td><td class="num">'+fmt(c.cores)+'</td><td class="num">'+fmt(c.vcpu)+'</td><td class="num">'+fmt(mibToGB(c.memMiB),0)+'</td><td class="num">'+fmt(mibToGB(c.vramMiB),0)+'</td></tr>';
          }).join('') + '</tbody>';

        var hasHostUsage = D.hosts.some(function(h){ return h.cpuUsagePct!=null || h.memUsagePct!=null; });
        var hostSorted = D.hosts.slice().sort(function(a,b){return b.vmCount-a.vmCount;});
        document.getElementById('tbl-host').innerHTML =
          '<thead><tr><th>Host</th><th>Cluster</th><th>CPU Model</th><th class="num">Cores</th><th class="num">RAM (GB)</th><th class="num">VMs</th>'+
          (hasHostUsage ? '<th class="num">CPU Usage %</th><th class="num">Memory Usage %</th>' : '') +
          '</tr></thead><tbody>'+
          hostSorted.map(function(h){
            var row = '<tr><td>'+esc(h.name)+'</td><td>'+esc(h.cluster)+'</td><td>'+esc(h.cpuModel)+'</td><td class="num">'+fmt(h.cores)+'</td><td class="num">'+fmt(mibToGB(h.memMiB),0)+'</td><td class="num">'+fmt(h.vmCount)+'</td>';
            if(hasHostUsage){
              row += '<td class="num">'+(h.cpuUsagePct!=null?'<span class="pill '+pillClass(h.cpuUsagePct)+'">'+fmt(h.cpuUsagePct,1)+'%</span>':'-')+'</td>';
              row += '<td class="num">'+(h.memUsagePct!=null?'<span class="pill '+pillClass(h.memUsagePct)+'">'+fmt(h.memUsagePct,1)+'%</span>':'-')+'</td>';
            }
            return row+'</tr>';
          }).join('') + '</tbody>';

        if(!hasHostUsage){
          document.getElementById('tbl-host').insertAdjacentHTML('afterend', '<div class="empty-note">หมายเหตุ: ไม่พบข้อมูล CPU/Memory Usage % ของ host ในไฟล์นี้ (ต้องเชื่อมต่อ vCenter แบบสดตอน export)</div>');
        }

        if(!D.hosts.length){
          document.getElementById('tbl-host').parentElement.innerHTML = '<div class="empty-note">ไม่พบชีต vHost ในไฟล์นี้</div>';
        }

        // ---- Memory Tiering: Active vs Standby (cold) memory per VM ----
        if(D.hasMemUsageData){
          var tieringVms = D.realVms.filter(function(v){ return v.memActiveMiB != null; });
          var totalAllocMiB = tieringVms.reduce(function(a,v){return a+v.memMiB;},0);
          var totalActiveMiB = tieringVms.reduce(function(a,v){return a+Math.min(v.memActiveMiB, v.memMiB);},0);
          var totalStandbyMiB = Math.max(0, totalAllocMiB - totalActiveMiB);
          var activePct = totalAllocMiB>0 ? (totalActiveMiB/totalAllocMiB*100) : 0;
          var standbyPct = totalAllocMiB>0 ? (totalStandbyMiB/totalAllocMiB*100) : 0;
          var ratio = totalActiveMiB>0 ? (totalStandbyMiB/totalActiveMiB) : (totalStandbyMiB>0?999:0);

          document.getElementById('cp-tiering-tag').textContent = tieringVms.length+'/'+D.realVms.length+' VM มีข้อมูล';
          document.getElementById('cp-tiering-stats').innerHTML = [
            {lbl:'Active Memory (ใช้งานจริง)', val:fmt(mibToGB(totalActiveMiB),0)+' GB', sub:fmt(activePct,1)+'% ของที่จัดสรร'},
            {lbl:'Standby Memory (เย็น/ค้าง)', val:fmt(mibToGB(totalStandbyMiB),0)+' GB', sub:fmt(standbyPct,1)+'% ของที่จัดสรร', cls:standbyPct>=50?'warn':''},
            {lbl:'Active : Standby Ratio', val:'1 : '+fmt(ratio,2), sub: ratio>1 ? 'Standby มากกว่า Active — ศักยภาพ tiering สูง' : 'Active มากกว่า Standby'},
            {lbl:'ศักยภาพ Memory Tiering', val: standbyPct>=50?'สูง':(standbyPct>=25?'ปานกลาง':'ต่ำ'), sub:'ประเมินจากสัดส่วน Standby รวม', cls: standbyPct>=50?'ok':(standbyPct>=25?'warn':'')}
          ].map(function(s){ return '<div class="stat '+(s.cls||'')+'"><div class="lbl">'+s.lbl+'</div><div class="val">'+s.val+'</div><div class="sub">'+s.sub+'</div></div>'; }).join('');

          var tieringRows = tieringVms.map(function(v){
            var active = Math.min(v.memActiveMiB, v.memMiB);
            var standby = Math.max(0, v.memMiB - active);
            var standbyPctVm = v.memMiB>0 ? (standby/v.memMiB*100) : 0;
            return {name:v.name, allocGB:mibToGB(v.memMiB), activeGB:mibToGB(active), standbyGB:mibToGB(standby), standbyPct:standbyPctVm};
          }).sort(function(a,b){ return b.standbyGB - a.standbyGB; }).slice(0,20);

          document.getElementById('tbl-tiering').innerHTML =
            '<thead><tr><th>VM</th><th class="num">Allocated (GB)</th><th class="num">Active (GB)</th><th class="num">Standby (GB)</th><th>Standby %</th></tr></thead><tbody>'+
            tieringRows.map(function(r){
              return '<tr><td>'+esc(r.name)+'</td><td class="num">'+fmt(r.allocGB,1)+'</td><td class="num">'+fmt(r.activeGB,1)+'</td><td class="num">'+fmt(r.standbyGB,1)+'</td>'+
                '<td><div class="bar-track"><div class="bar-fill" style="width:'+Math.min(r.standbyPct,100)+'%; background:'+(r.standbyPct>=50?'#3ED2A0':'#5AA9E6')+';"></div></div><span class="pill" style="margin-top:4px; display:inline-block;">'+fmt(r.standbyPct,1)+'%</span></td></tr>';
            }).join('') + '</tbody>';
          document.getElementById('cp-tiering-note').textContent = 'แสดง 20 อันดับแรกที่มี Standby Memory สูงสุด (ตัวเลือกที่ดีที่สุดสำหรับ tiering)';
        }else{
          document.getElementById('cp-tiering-tag').textContent = '';
          document.getElementById('cp-tiering-stats').innerHTML = '';
          document.getElementById('cp-tiering-note').textContent = '';
          document.getElementById('tbl-tiering').parentElement.innerHTML = '<div class="empty-note">ไม่พบข้อมูล Memory Active (ชีต vMemory คอลัมน์ "Active") ในไฟล์นี้ — ไม่สามารถประเมิน Memory Tiering ได้</div>';
        }
      }

      function renderStorage(D){
        var totalCap = D.datastores.reduce(function(a,d){return a+d.capMiB;},0);
        var totalUsed = D.datastores.reduce(function(a,d){return a+d.usedMiB;},0);
        var totalProv = D.datastores.reduce(function(a,d){return a+d.provMiB;},0);
        var overPct = totalCap>0 ? (totalProv/totalCap*100) : 0;

        document.getElementById('st-stats').innerHTML = [
          {lbl:'Capacity รวม', val:fmt(mibToTB(totalCap),2)+' TB', sub:D.datastores.length+' datastore'},
          {lbl:'Used รวม', val:fmt(mibToTB(totalUsed),2)+' TB', sub:fmt(totalCap>0?totalUsed/totalCap*100:0,1)+'% ของ capacity'},
          {lbl:'Provisioned รวม', val:fmt(mibToTB(totalProv),2)+' TB', sub:'รวม thin-provision'},
          {lbl:'Oversubscription', val:fmt(overPct,0)+'%', sub: overPct>150 ? 'จัดสรรเกิน capacity มาก' : 'อยู่ในเกณฑ์ทั่วไป'}
        ].map(function(s){ return '<div class="stat"><div class="lbl">'+s.lbl+'</div><div class="val">'+s.val+'</div><div class="sub">'+s.sub+'</div></div>'; }).join('');

        var sorted = D.datastores.slice().sort(function(a,b){return b.usedPct-a.usedPct;});
        document.getElementById('tbl-datastore').innerHTML =
          '<thead><tr><th>Datastore</th><th>Type</th><th class="num">Capacity (GB)</th><th class="num">Used (GB)</th><th class="num">Free (GB)</th><th>Used %</th><th class="num">VMs</th></tr></thead><tbody>'+
          sorted.map(function(d){
            var pct = d.usedPct;
            return '<tr><td>'+esc(d.name)+'</td><td>'+esc(d.type)+'</td><td class="num">'+fmt(mibToGB(d.capMiB),0)+'</td><td class="num">'+fmt(mibToGB(d.usedMiB),0)+'</td><td class="num">'+fmt(mibToGB(d.freeMiB),0)+'</td>'+
              '<td><div class="bar-track"><div class="bar-fill '+barClass(pct)+'" style="width:'+Math.min(pct,100)+'%"></div></div><span class="pill '+pillClass(pct)+'" style="margin-top:4px;display:inline-block;">'+fmt(pct,1)+'%</span></td>'+
              '<td class="num">'+fmt(d.vmCount)+'</td></tr>';
          }).join('') + '</tbody>';

        if(!D.datastores.length){
          document.getElementById('tbl-datastore').parentElement.innerHTML = '<div class="empty-note">ไม่พบชีต vDatastore ในไฟล์นี้</div>';
        }
      }

      function renderVMs(D){
        function topTable(list, field, unit, conv){
          var sorted = list.slice().sort(function(a,b){return (b[field]||0)-(a[field]||0);}).slice(0,10);
          return '<thead><tr><th>VM</th><th class="num">'+unit+'</th></tr></thead><tbody>'+
            sorted.map(function(v){ var val=v[field]||0; return '<tr><td>'+esc(v.name)+'</td><td class="num">'+fmt(conv?conv(val):val,conv?0:0)+'</td></tr>'; }).join('') + '</tbody>';
        }

        // CPU: RVTools does not capture a per-VM CPU usage metric (only allocated vCPU count).
        document.getElementById('tbl-top-cpu').innerHTML = topTable(D.realVms, 'cpus','vCPU (จัดสรร)');

        // Memory: use actual "Active" memory usage from vMemory tab when available, otherwise fall back to allocated.
        if(D.hasMemUsageData){
          var memList = D.realVms.filter(function(v){ return v.memActiveMiB != null; });
          document.getElementById('tbl-top-mem').innerHTML = topTable(memList, 'memActiveMiB','Memory Usage (GB)', mibToGB);
        }else{
          document.getElementById('tbl-top-mem').innerHTML = topTable(D.realVms, 'memMiB','RAM จัดสรร (GB)', mibToGB) +
            '<caption class="empty-note" style="caption-side:bottom; text-align:left; padding:8px 4px;">ไม่พบข้อมูล Memory usage (vMemory: Active) ในไฟล์นี้ แสดงค่าที่จัดสรรแทน</caption>';
        }

        // Storage: use actual "In Use" (used) storage rather than provisioned/allocated.
        document.getElementById('tbl-top-disk').innerHTML = topTable(D.realVms, 'usedMiB','Storage Usage (GB)', mibToGB);

        document.getElementById('vm-count-tag').textContent = D.realVms.length + ' VM';
        var allSorted = D.realVms.slice().sort(function(a,b){ return a.name.localeCompare(b.name); });
        document.getElementById('tbl-allvms').innerHTML =
          '<thead><tr><th>VM</th><th>Power</th><th>OS</th><th>Cluster</th><th>Host</th><th class="num">vCPU</th><th class="num">RAM (GB)</th><th class="num">Storage Used (GB)</th></tr></thead><tbody>'+
          allSorted.map(function(v){
            var pcls = v.power.indexOf('on')!==-1 ? 'ok' : (v.power.indexOf('off')!==-1 ? '' : 'warn');
            return '<tr><td>'+esc(v.name)+'</td><td><span class="pill '+pcls+'">'+esc(v.power||'-')+'</span></td><td>'+esc(v.os)+'</td><td>'+esc(v.cluster)+'</td><td>'+esc(v.host)+'</td><td class="num">'+fmt(v.cpus)+'</td><td class="num">'+fmt(mibToGB(v.memMiB),0)+'</td><td class="num">'+fmt(mibToGB(v.usedMiB),0)+'</td></tr>';
          }).join('') + '</tbody>';
      }

      function renderSnapshots(D){
        var total = D.snapshots.length;
        var totalSize = D.snapshots.reduce(function(a,s){return a+s.sizeMiB;},0);
        var old30 = D.snapshots.filter(function(s){return s.ageDays!=null && s.ageDays>30;}).length;
        var old7 = D.snapshots.filter(function(s){return s.ageDays!=null && s.ageDays>7 && s.ageDays<=30;}).length;

        document.getElementById('sn-stats').innerHTML = [
          {lbl:'Snapshot ทั้งหมด', val:fmt(total), sub:'รายการที่พบในไฟล์'},
          {lbl:'ขนาดรวม', val:fmt(mibToGB(totalSize),1)+' GB', sub:'พื้นที่ที่ snapshot ใช้'},
          {lbl:'อายุ 7–30 วัน', val:fmt(old7), sub:'ควรติดตาม', cls:old7?'warn':''},
          {lbl:'อายุมากกว่า 30 วัน', val:fmt(old30), sub:'ควรพิจารณาลบ', cls:old30?'bad':''}
        ].map(function(s){ return '<div class="stat '+(s.cls||'')+'"><div class="lbl">'+s.lbl+'</div><div class="val">'+s.val+'</div><div class="sub">'+s.sub+'</div></div>'; }).join('');

        var sorted = D.snapshots.slice().sort(function(a,b){ return (b.ageDays||0)-(a.ageDays||0); });
        document.getElementById('tbl-snapshot').innerHTML =
          '<thead><tr><th>VM</th><th>Snapshot</th><th>วันที่สร้าง</th><th class="num">อายุ (วัน)</th><th class="num">ขนาด (GB)</th></tr></thead><tbody>'+
          sorted.map(function(s){
            var cls = s.ageDays==null?'':(s.ageDays>30?'bad':(s.ageDays>7?'warn':'ok'));
            return '<tr><td>'+esc(s.vm)+'</td><td>'+esc(s.name)+'</td><td>'+(s.date&&!isNaN(s.date)?s.date.toISOString().slice(0,10):'-')+'</td><td class="num"><span class="pill '+cls+'">'+(s.ageDays==null?'-':s.ageDays)+'</span></td><td class="num">'+fmt(mibToGB(s.sizeMiB),2)+'</td></tr>';
          }).join('') + '</tbody>';

        if(!D.snapshots.length){
          document.getElementById('tbl-snapshot').parentElement.innerHTML = '<div class="empty-note">ไม่พบ Snapshot ในไฟล์นี้ (หรือไม่มีชีต vSnapshot)</div>';
        }
      }

      function renderTools(D){
        var counts = {};
        D.toolsRows.forEach(function(t){ var k=t.status||'ไม่ระบุ'; counts[k]=(counts[k]||0)+1; });
        var keys = Object.keys(counts);
        // Severity tiers: toolsOk = fine; toolsOld = outdated but running (warn); notrunning/notinstalled = more severe (bad)
        function tier(status){
          var s = norm(status);
          if(s.indexOf('notrunning')!==-1 || s.indexOf('notinstalled')!==-1) return 'bad';
          if(s.indexOf('old')!==-1) return 'warn';
          if(s.indexOf('ok')!==-1) return 'ok';
          return 'unknown';
        }
        var TIER_COLOR = {ok:'#3ED2A0', warn:'#F0A857', bad:'#E2596B', unknown:'#5AA9E6'};
        killChart('tools');
        if(keys.length){
          CHARTS.tools = new Chart(document.getElementById('chart-tools'), {
            type:'doughnut',
            data:{ labels:keys, datasets:[{data:keys.map(function(k){return counts[k];}), backgroundColor:keys.map(function(k){return TIER_COLOR[tier(k)];}), borderColor:'#1A222B', borderWidth:2}] },
            options:{ plugins:{legend:{position:'bottom', labels:{color:'#8B96A3', font:{size:11}}}}, maintainAspectRatio:false }
          });
        }
        var okCount = D.toolsRows.filter(function(t){return tier(t.status)==='ok';}).length;
        var oldCount = D.toolsRows.filter(function(t){return tier(t.status)==='warn';}).length;
        var badCount = D.toolsRows.filter(function(t){return tier(t.status)==='bad';}).length;
        document.getElementById('tools-stats').innerHTML = [
          {lbl:'Tools ปกติ (toolsOk)', val:fmt(okCount), sub:'ทำงานและเป็นเวอร์ชันล่าสุด', cls:'ok'},
          {lbl:'เวอร์ชันเก่า (toolsOld)', val:fmt(oldCount), sub:'ยังทำงานอยู่ แต่ควรอัปเดต — ความเสี่ยงต่ำกว่า', cls:oldCount?'warn':''},
          {lbl:'ไม่ได้ติดตั้ง/ไม่ทำงาน', val:fmt(badCount), sub:'notRunning/notInstalled — ควรแก้ไขก่อน toolsOld', cls:badCount?'bad':''},
          {lbl:'รวม VM ที่มีข้อมูล Tools', val:fmt(D.toolsRows.length), sub:D.realVms.length+' VM ทั้งหมด'}
        ].map(function(s){ return '<div class="stat '+(s.cls||'')+'"><div class="lbl">'+s.lbl+'</div><div class="val">'+s.val+'</div><div class="sub">'+s.sub+'</div></div>'; }).join('');

        var needsAttention = D.toolsRows.filter(function(t){ var tr=tier(t.status); return tr==='bad'||tr==='warn'; })
          .sort(function(a,b){ var ra = tier(a.status)==='bad'?0:1, rb = tier(b.status)==='bad'?0:1; return ra-rb; });
        document.getElementById('tbl-tools').innerHTML =
          '<thead><tr><th>VM</th><th>สถานะ</th><th>ระดับ</th></tr></thead><tbody>'+
          needsAttention.map(function(t){
            var tr = tier(t.status);
            var trLabel = tr==='bad' ? 'ควรแก้ไขก่อน' : (tr==='warn' ? 'ควรอัปเดต' : 'ไม่ทราบ');
            return '<tr><td>'+esc(t.vm)+'</td><td><span class="pill '+tr+'">'+esc(t.status)+'</span></td><td>'+trLabel+'</td></tr>';
          }).join('') + '</tbody>';
        if(!D.toolsRows.length){
          document.querySelector('#sec-tools .chart-box').innerHTML = '<div class="empty-note">ไม่พบชีต vTools ในไฟล์นี้</div>';
          document.getElementById('tbl-tools').parentElement.innerHTML = '<div class="empty-note">ไม่มีข้อมูล</div>';
        } else if(!needsAttention.length){
          document.getElementById('tbl-tools').parentElement.innerHTML = '<div class="empty-note">ทุก VM มี VMware Tools ทำงานปกติ</div>';
        }
      }

      function buildStatusForHost(v, h, branch){
        // Returns {status:'vulnerable'|'patched'|'unknown', note:string}
        var threshold = v.fixedBuildEsxi && v.fixedBuildEsxi[branch];
        var hostBuild = h.esxBuild!=null ? parseInt(h.esxBuild, 10) : null;
        if(threshold && hostBuild && !isNaN(hostBuild)){
          if(hostBuild >= threshold){
            return {status:'patched', note:'build ปัจจุบัน '+hostBuild+' ≥ build ที่แก้ไขแล้ว ('+threshold+')'};
          }
          return {status:'vulnerable', note:'build ปัจจุบัน '+hostBuild+' ต่ำกว่า build ที่แก้ไขแล้ว ('+threshold+') — ยังไม่ได้แพตช์'};
        }
        return {status:'unknown', note: hostBuild ? ('ตรวจพบ build '+hostBuild+' แต่เครื่องมือนี้ยังไม่มีเลข build อ้างอิงสำหรับ VMSA นี้ — เทียบกับ Response Matrix เอง') : 'ไม่พบเลข build ของ host นี้ในไฟล์ RVTools — เทียบเวอร์ชันกับ Response Matrix ด้วยตนเอง'};
      }

      function computeSecurityFindings(D){
        var esxiKeys = D.hosts.map(function(h){ return {host:h, key:versionKey(h.esxVersion)}; });
        var vcKey = D.vCenterApiVersion ? versionKey(D.vCenterApiVersion) : null;
        var findings = [];
        VMSA_DB.forEach(function(v){
          var affectedHostEntries = esxiKeys.filter(function(x){ return x.key && v.affects.esxi.indexOf(x.key)!==-1; })
            .map(function(x){
              var bs = buildStatusForHost(v, x.host, x.key);
              return {host:x.host, buildStatus:bs};
            })
            .filter(function(e){ return e.buildStatus.status !== 'patched'; }); // drop hosts we can confirm are already patched by build number
          var vcAffected = vcKey && v.affects.vcenter.indexOf(vcKey)!==-1;
          if(affectedHostEntries.length || vcAffected){
            findings.push({vmsa:v, affectedHosts:affectedHostEntries, vcAffected:vcAffected});
          }
        });
        findings.sort(function(a,b){
          var r = sevRank(b.vmsa.sev) - sevRank(a.vmsa.sev);
          if(r!==0) return r;
          return b.vmsa.cvss - a.vmsa.cvss;
        });
        return findings;
      }

      function renderSecurity(D){
        var findings = computeSecurityFindings(D);
        var critCount = findings.filter(function(f){return f.vmsa.sev==='critical';}).length;
        var impCount = findings.filter(function(f){return f.vmsa.sev==='important';}).length;
        var itwCount = findings.filter(function(f){return f.vmsa.itw;}).length;

        document.getElementById('sec-stats').innerHTML = [
          {lbl:'ช่องโหว่ที่พบทั้งหมด', val:fmt(findings.length), sub:'จับคู่ตามเวอร์ชัน ESXi/vCenter', cls: findings.length?'warn':'ok'},
          {lbl:'ระดับ Critical', val:fmt(critCount), sub:'ควรแพตช์เร่งด่วนที่สุด', cls: critCount?'bad':'ok'},
          {lbl:'ระดับ Important', val:fmt(impCount), sub:'ควรวางแผนแพตช์', cls: impCount?'warn':'ok'},
          {lbl:'ถูกโจมตีจริงแล้ว (ITW)', val:fmt(itwCount), sub:'Broadcom ยืนยันพบการโจมตีจริง', cls: itwCount?'bad':'ok'}
        ].map(function(s){ return '<div class="stat '+s.cls+'"><div class="lbl">'+s.lbl+'</div><div class="val">'+s.val+'</div><div class="sub">'+s.sub+'</div></div>'; }).join('');

        document.getElementById('sec-count-tag').textContent = findings.length + ' รายการ';

        if(!findings.length){
          document.getElementById('sec-findings').innerHTML = '<div class="empty-note">ไม่พบช่องโหว่ในฐานข้อมูล VMSA ที่คัดเลือกไว้ ที่ตรงกับเวอร์ชัน ESXi/vCenter ที่ตรวจพบในไฟล์นี้ (หรือไม่สามารถระบุเวอร์ชันได้ — ดูแท็บ Software Expire) โปรดตรวจสอบว่ามีการอัปเดตแพตช์สม่ำเสมออยู่แล้ว</div>';
          return;
        }

        var html = findings.map(function(f){
          var v = f.vmsa;
          var targets = [];
          if(f.vcAffected) targets.push('vCenter ('+esc(D.vCenterApiVersion)+(D.vCenterBuild?(', build '+esc(D.vCenterBuild)):'')+')'+' — เทียบระดับ major.minor เท่านั้น (ยังไม่มี build threshold อ้างอิงสำหรับ vCenter ใน VMSA นี้ ในเครื่องมือ — ตรวจ Response Matrix ด้วยเลข build ที่ตรวจพบเทียบเอง)');
          f.affectedHosts.forEach(function(entry){
            var h = entry.host, bs = entry.buildStatus;
            var bsPill = bs.status==='vulnerable' ? '<span class="pill bad">ยืนยันจาก build</span>' : '<span class="pill">ยังไม่ยืนยัน build</span>';
            targets.push('ESXi host: '+esc(h.name)+' ('+esc(h.esxVersion||'-')+(h.esxBuild?(', build '+esc(h.esxBuild)):', ไม่พบ build')+') '+bsPill+' — '+esc(bs.note));
          });
          return '<div class="panel" style="border-left:3px solid var('+(v.sev==='critical'?'--bad':(v.sev==='important'?'--warn':'--info'))+'); margin-bottom:12px;">'+
            '<div style="display:flex; justify-content:space-between; align-items:flex-start; gap:12px; flex-wrap:wrap;">'+
              '<div>'+
                '<div style="font-size:13px; font-weight:600; margin-bottom:2px;">'+esc(v.id)+' — '+esc(v.title)+'</div>'+
                '<div style="color:var(--muted); font-size:11px;">'+v.cves.map(esc).join(', ')+'</div>'+
              '</div>'+
              '<div style="text-align:right; flex:0 0 auto;">'+
                '<span class="pill '+sevClass(v.sev)+'" style="font-size:11px;">'+sevLabel(v.sev)+' · CVSS '+fmt(v.cvss,1)+'</span>'+
                (v.itw?' <span class="pill bad" style="font-size:11px;">Exploited in the wild</span>':'')+
              '</div>'+
            '</div>'+
            '<div style="margin-top:10px; font-size:12px; color:var(--text);"><b>กระทบกับ:</b><br>'+targets.join('<br>')+'</div>'+
            '<div style="margin-top:6px; font-size:12px;"><b>เวอร์ชันที่แก้ไขแล้ว:</b> '+esc(v.fixed)+'</div>'+
            '<div style="margin-top:6px; font-size:12px; color:var(--muted);"><b>วิธีปิดช่องโหว่:</b> '+esc(v.remediation)+'</div>'+
          '</div>';
        }).join('');
        document.getElementById('sec-findings').innerHTML = html;
      }


      function computeExpireData(D){
        var esxiRows = D.hosts.map(function(h){
          var vkey = versionKey(h.esxVersion);
          var info = vkey ? VSPHERE_EOL[vkey] : null;
          var days = info ? daysUntil(info.eogs) : null;
          var st = expireStatus(days);
          return {host:h.name, cluster:h.cluster, version:h.esxVersion||'ไม่ทราบ', vkey:vkey, eogs:info?info.eogs:null, days:days, st:st};
        });
        var vcKey = D.vCenterApiVersion ? versionKey(D.vCenterApiVersion) : null;
        var vcInfo = vcKey ? VSPHERE_EOL[vcKey] : null;
        var vcDays = vcInfo ? daysUntil(vcInfo.eogs) : null;
        var vcSt = expireStatus(vcDays);
        var osResults = D.realVms.map(function(v){
          var m = findOsEol(v.os);
          var days = m ? daysUntil(m.eol) : null;
          var st = m ? (m.eol ? expireStatus(days) : {cls:'', label:'ไม่ทราบเวอร์ชัน'}) : {cls:'', label:'ไม่รู้จัก OS นี้'};
          return {vm:v.name, os:v.os, matched: m ? m.label : null, eol: m ? m.eol : null, days:days, st:st, approx: m && m.approx};
        });
        var recognized = osResults.filter(function(r){ return r.matched && r.eol; });
        var expired = recognized.filter(function(r){ return r.days!=null && r.days<0; });
        var soon = recognized.filter(function(r){ return r.days!=null && r.days>=0 && r.days<=180; });
        var unrecognized = osResults.filter(function(r){ return !r.matched; });
        var esxiExpired = esxiRows.filter(function(r){ return r.days!=null && r.days<0; }).length;
        var esxiSoon = esxiRows.filter(function(r){ return r.days!=null && r.days>=0 && r.days<=180; }).length;
        return {esxiRows:esxiRows, vcInfo:vcInfo, vcDays:vcDays, vcSt:vcSt, osResults:osResults, recognized:recognized, expired:expired, soon:soon, unrecognized:unrecognized, esxiExpired:esxiExpired, esxiSoon:esxiSoon};
      }

      function renderExpire(D){
        var E = computeExpireData(D);
        var esxiRows = E.esxiRows;

        document.getElementById('tbl-esxi').innerHTML = D.hosts.length ?
          '<thead><tr><th>Host</th><th>Cluster</th><th>ESXi Version</th><th>End of General Support</th><th class="num">เหลือ (วัน)</th><th>สถานะ</th></tr></thead><tbody>'+
          esxiRows.slice().sort(function(a,b){ return (a.days==null?1e9:a.days) - (b.days==null?1e9:b.days); }).map(function(r){
            return '<tr><td>'+esc(r.host)+'</td><td>'+esc(r.cluster)+'</td><td>'+esc(r.version)+'</td>'+
              '<td>'+(r.eogs||'ไม่พบในฐานข้อมูล')+'</td>'+
              '<td class="num">'+(r.days==null?'-':fmt(r.days))+'</td>'+
              '<td><span class="pill '+r.st.cls+'">'+r.st.label+'</span></td></tr>';
          }).join('') + '</tbody>'
          : '';
        if(!D.hosts.length){
          document.getElementById('tbl-esxi').parentElement.innerHTML = '<div class="empty-note">ไม่พบชีต vHost ในไฟล์นี้ ไม่สามารถระบุเวอร์ชัน ESXi ได้</div>';
        }

        if(D.vCenterDisplayVersion){
          document.getElementById('ex-vcenter').innerHTML =
            '<div class="grid cols-3">'+
            '<div class="stat"><div class="lbl">vCenter Version <span style="font-weight:400;">(VI SDK Server type)</span></div><div class="val" style="font-size:20px;">'+esc(D.vCenterDisplayVersion)+'</div><div class="sub">'+(D.vCenterBuild?('build '+esc(D.vCenterBuild)):'ตรวจพบจากชีต vInfo (ไม่พบเลข build)')+'</div></div>'+
            '<div class="stat '+E.vcSt.cls+'"><div class="lbl">End of General Support</div><div class="val" style="font-size:20px;">'+(E.vcInfo?E.vcInfo.eogs:'ไม่พบในฐานข้อมูล')+'</div><div class="sub">'+(E.vcDays!=null?('เหลือ '+fmt(E.vcDays)+' วัน'):'-')+'</div></div>'+
            '<div class="stat '+E.vcSt.cls+'"><div class="lbl">สถานะ</div><div class="val" style="font-size:20px;">'+E.vcSt.label+'</div><div class="sub">อ้างอิงนโยบาย 5+2 ปีของ Broadcom/VMware</div></div>'+
            '</div>';
        }else{
          document.getElementById('ex-vcenter').innerHTML = '<div class="empty-note">ไม่พบคอลัมน์ VI SDK Server type / VI SDK API Version ในชีต vInfo — ไม่สามารถระบุเวอร์ชัน vCenter จากไฟล์นี้ได้ โปรดตรวจสอบด้วยตนเองใน vCenter &gt; Help &gt; About</div>';
        }

        document.getElementById('ex-stats').innerHTML = [
          {lbl:'ESXi หมดอายุแล้ว', val:fmt(E.esxiExpired), sub:D.hosts.length+' host ทั้งหมด', cls:E.esxiExpired?'bad':'ok'},
          {lbl:'ESXi ใกล้หมดอายุ (≤180 วัน)', val:fmt(E.esxiSoon), sub:'ควรวางแผนอัปเกรด', cls:E.esxiSoon?'warn':'ok'},
          {lbl:'OS ของ VM ที่หมดอายุแล้ว', val:fmt(E.expired.length), sub:(D.realVms.length)+' VM ทั้งหมด', cls:E.expired.length?'bad':'ok'},
          {lbl:'OS ของ VM ใกล้หมดอายุ', val:fmt(E.soon.length), sub:'ภายใน 180 วัน', cls:E.soon.length?'warn':'ok'}
        ].map(function(s){ return '<div class="stat '+s.cls+'"><div class="lbl">'+s.lbl+'</div><div class="val">'+s.val+'</div><div class="sub">'+s.sub+'</div></div>'; }).join('');

        document.getElementById('ex-os-tag').textContent = E.recognized.length+'/'+E.osResults.length+' ระบุเวอร์ชันได้';

        var sortedOs = E.osResults.slice().sort(function(a,b){
          var da = a.days==null?1e9:a.days, db = b.days==null?1e9:b.days;
          return da-db;
        });
        document.getElementById('tbl-os-eol').innerHTML =
          '<thead><tr><th>VM</th><th>Guest OS (จากไฟล์)</th><th>ระบุเป็น</th><th>End of Support</th><th class="num">เหลือ (วัน)</th><th>สถานะ</th></tr></thead><tbody>'+
          sortedOs.map(function(r){
            return '<tr><td>'+esc(r.vm)+'</td><td>'+esc(r.os)+'</td><td>'+esc(r.matched||'—')+(r.approx?' <span class="pill">โดยประมาณ</span>':'')+'</td>'+
              '<td>'+(r.eol||'-')+'</td><td class="num">'+(r.days==null?'-':fmt(r.days))+'</td>'+
              '<td><span class="pill '+r.st.cls+'">'+r.st.label+'</span></td></tr>';
          }).join('') + '</tbody>';

        if(E.unrecognized.length){
          document.getElementById('tbl-os-eol').insertAdjacentHTML('afterend',
            '<div class="empty-note">'+E.unrecognized.length+' VM มี Guest OS ที่ไม่อยู่ในฐานข้อมูล EOL ของเครื่องมือนี้ (เช่น distro/เวอร์ชันที่พบไม่บ่อย) — ตรวจสอบ EOL ของระบบเหล่านี้ด้วยตนเองจากผู้ผลิต</div>');
        }
      }
      function realVmsCount(D){ return D.realVms.length; }

      function renderEmailBlock(containerId, thaiText, enText){
        var el = document.getElementById(containerId);
        if(!el) return;
        el.innerHTML =
          '<div class="email-block">'+
            '<div class="email-head"><span class="email-lang">ภาษาไทย</span><button class="email-copy-btn" data-target="'+containerId+'-th">คัดลอก</button></div>'+
            '<textarea id="'+containerId+'-th" readonly>'+esc(thaiText)+'</textarea>'+
          '</div>'+
          '<div class="email-block">'+
            '<div class="email-head"><span class="email-lang">English</span><button class="email-copy-btn" data-target="'+containerId+'-en">Copy</button></div>'+
            '<textarea id="'+containerId+'-en" readonly>'+esc(enText)+'</textarea>'+
          '</div>';
        el.querySelectorAll('.email-copy-btn').forEach(function(btn){
          btn.addEventListener('click', function(){
            var ta = document.getElementById(btn.dataset.target);
            ta.select();
            var isEn = btn.dataset.target.indexOf('-en')!==-1;
            var orig = btn.textContent;
            function done(){
              btn.classList.add('copied');
              btn.textContent = isEn ? 'Copied!' : 'คัดลอกแล้ว';
              setTimeout(function(){ btn.classList.remove('copied'); btn.textContent = orig; }, 1500);
            }
            if(navigator.clipboard && navigator.clipboard.writeText){
              navigator.clipboard.writeText(ta.value).then(done).catch(function(){ try{document.execCommand('copy'); done();}catch(e){} });
            }else{
              try{ document.execCommand('copy'); done(); }catch(e){}
            }
          });
        });
      }

      function tplStatusClass(cls){
        if(cls==='bad') return 'tpl-status-critical';
        if(cls==='warn') return 'tpl-status-warning';
        return 'tpl-status-normal';
      }

      function renderTplPie(canvasId, usedPct){
        var d = simPieData(usedPct);
        killChart(canvasId);
        var labels = ['ใช้งาน/จัดสรรแล้ว','ว่าง'];
        var data = [d.used, d.free];
        var colors = [usedPct>=90?'#c0392b':(usedPct>=75?'#b8860b':'#1a7a3c'),'#D8DCE1'];
        if(d.over>0){ labels = ['เกินขีดความสามารถ','ในขอบเขต']; data=[Math.min(d.over,100),100-Math.min(d.over,100)]; colors=['#c0392b','#e8b9b9']; }
        var canvas = document.getElementById(canvasId);
        if(!canvas) return;
        CHARTS[canvasId] = new Chart(canvas, {
          type:'doughnut',
          data:{ labels:labels, datasets:[{data:data, backgroundColor:colors, borderColor:'#fff', borderWidth:2}] },
          options:{ plugins:{legend:{position:'bottom', labels:{color:'#333', font:{size:10}}}}, maintainAspectRatio:false }
        });
      }

      function renderTemplate(D){
        var E = computeExpireData(D);
        var customer = (document.getElementById('inp-customer').value||'').trim();
        var pmdate = (document.getElementById('inp-pmdate').value||'').trim();
        var preparedby = (document.getElementById('inp-preparedby').value||'').trim();

        var totalCores = D.hosts.reduce(function(a,h){return a+h.cores;},0);
        var totalVCPU = D.realVms.reduce(function(a,v){return a+v.cpus;},0);
        var lowDS = D.datastores.filter(function(d){return d.usedPct>=85;});
        var warnDS = D.datastores.filter(function(d){return d.usedPct>=75 && d.usedPct<85;});
        var dsOverallStatus = lowDS.length ? {cls:'bad',label:'Critical'} : (warnDS.length ? {cls:'warn',label:'Warning'} : {cls:'ok',label:'Normal'});
        var oldSnap = D.snapshots.filter(function(s){return s.ageDays!=null && s.ageDays>30;});
        var tplRatioInput = document.getElementById('simCpuRatio');
        var cpuRatio = (tplRatioInput && num(tplRatioInput.value)) || 4;

        var clusterNames = [];
        D.hosts.forEach(function(h){ if(h.cluster && clusterNames.indexOf(h.cluster)===-1) clusterNames.push(h.cluster); });
        clusterNames.sort();

        var html = '';
        html += '<h3 style="text-align:center; font-size:20px; font-weight:700; margin-bottom:2px;">VMware Preventive Maintenance Report</h3>';
        html += '<p style="text-align:center; color:#555; font-size:11.5px; margin-bottom:20px;">'+(customer?esc(customer):'[ชื่อลูกค้า]')+' — '+(pmdate?esc(pmdate):'[วันที่ตรวจ]')+' — จัดทำโดย '+(preparedby?esc(preparedby):'[ผู้จัดทำ]')+'</p>';

        html += '<h4 class="tpl-h1">1. vCenter</h4>';

        // 1.1 vCenter Information
        html += '<h5 class="tpl-h2">1.1 vCenter Information</h5>';
        html += '<table><tbody>'+
          '<tr><td style="width:28%;"><b>Hostname</b></td><td>'+(D.vCenterServer?esc(D.vCenterServer):'<i>[ไม่พบในไฟล์ RVTools — กรอกเอง]</i>')+'</td></tr>'+
          '<tr><td><b>Current Build/Patch</b></td><td>'+(D.vCenterDisplayVersion?('VMware vCenter Server '+esc(D.vCenterDisplayVersion)+(D.vCenterBuild?(' (build '+esc(D.vCenterBuild)+')'):'')):'<i>[ไม่พบคอลัมน์ VI SDK Server type / VI SDK API Version ในไฟล์นี้]</i>')+'</td></tr>'+
          '</tbody></table>';

        // 1.2 vCenter Status
        html += '<h5 class="tpl-h2">1.2 vCenter Status</h5>';
        html += '<p class="tpl-p">'+esc(D.vCenterServer||'vCenter')+' server '+(D.vms.length?'สามารถเชื่อมต่อและ export ข้อมูล RVTools ได้สำเร็จ ณ เวลาที่จัดทำไฟล์ — บ่งชี้ว่า service อยู่ในสถานะทำงานปกติ':'ไม่สามารถยืนยันสถานะจากไฟล์นี้ได้')+'</p>';
        html += '<p class="tpl-note">หมายเหตุ: RVTools ไม่ได้บันทึก system log/service status โดยตรง ควรตรวจสอบ vCenter &gt; Monitor &gt; ด้วยตนเองเพื่อยืนยัน</p>';

        // 1.3 VMware Status and VM
        html += '<h5 class="tpl-h2">1.3 VMware Status and VM</h5>';
        html += '<p class="tpl-p"><b>1.3.1 Utilization and Status on Host</b></p>';
        var hasHostUsage = D.hosts.some(function(h){ return h.cpuUsagePct!=null || h.memUsagePct!=null; });
        html += '<table><thead><tr><th>No.</th><th>Hostname</th><th class="num">Cores</th><th class="num">% CPU Usage</th><th class="num">Memory (GB)</th><th class="num">% Memory Usage</th><th>Status</th></tr></thead><tbody>';
        D.hosts.forEach(function(h, i){
          var worst = Math.max(h.cpuUsagePct||0, h.memUsagePct||0);
          var stcls = worst>=85 ? 'bad' : (worst>=75 ? 'warn' : 'ok');
          var stlabel = (h.cpuUsagePct==null && h.memUsagePct==null) ? '-' : (worst>=85?'Critical':(worst>=75?'Warning':'Normal'));
          html += '<tr><td>'+(i+1)+'</td><td>'+esc(h.name)+'</td><td class="num">'+fmt(h.cores)+'</td>'+
            '<td class="num">'+(h.cpuUsagePct!=null?fmt(h.cpuUsagePct,1):'-')+'</td>'+
            '<td class="num">'+fmt(mibToGB(h.memMiB),0)+'</td>'+
            '<td class="num">'+(h.memUsagePct!=null?fmt(h.memUsagePct,1):'-')+'</td>'+
            '<td class="'+tplStatusClass(stcls)+'">'+stlabel+'</td></tr>';
        });
        html += '</tbody></table>';
        if(!hasHostUsage){
          html += '<p class="tpl-note">หมายเหตุ: ไฟล์นี้ไม่มีข้อมูล CPU/Memory Usage % ระดับ host (ต้องเชื่อมต่อ vCenter แบบสดตอน export RVTools) แสดงเฉพาะ core/RAM ที่มีอยู่</p>';
        }

        function tplTopTable(list, field, unit, conv){
          var sorted = list.slice().sort(function(a,b){return (b[field]||0)-(a[field]||0);}).slice(0,10);
          var t = '<table><thead><tr><th>No.</th><th>VM Name</th><th class="num">'+unit+'</th></tr></thead><tbody>';
          sorted.forEach(function(v,i){ var val=v[field]||0; t += '<tr><td>'+(i+1)+'</td><td>'+esc(v.name)+'</td><td class="num">'+fmt(conv?conv(val):val,conv?0:0)+'</td></tr>'; });
          return t+'</tbody></table>';
        }
        html += '<p class="tpl-p"><b>1.3.2 Top 10 VM by vCPU (จัดสรร)</b></p>';
        html += tplTopTable(D.realVms, 'cpus', 'vCPU');
        html += '<p class="tpl-p"><b>1.3.3 Top 10 VM by Memory</b></p>';
        if(D.hasMemUsageData){
          html += tplTopTable(D.realVms.filter(function(v){return v.memActiveMiB!=null;}), 'memActiveMiB', 'Memory Usage (GB)', mibToGB);
        }else{
          html += tplTopTable(D.realVms, 'memMiB', 'RAM จัดสรร (GB)', mibToGB);
        }
        html += '<p class="tpl-note">หมายเหตุ: แสดง Top 10 แทนรายการ VM ทั้งหมด — ค่าที่ใช้เป็นค่าจัดสรร/usage ที่มีจริงในไฟล์ RVTools (RVTools ไม่บันทึก CPU usage MHz ระดับ VM)</p>';

        // 1.4 Datastore
        html += '<h5 class="tpl-h2">1.4 Datastore</h5>';
        html += '<table><thead><tr><th>Datastore Name</th><th class="num">Capacity (GB)</th><th class="num">Free (GB)</th><th class="num">Used (%)</th><th>Remark</th></tr></thead><tbody>';
        D.datastores.slice().sort(function(a,b){return b.usedPct-a.usedPct;}).forEach(function(d){
          var remark = d.usedPct>=85 ? 'Capacity will full' : (d.usedPct>=75 ? 'ควรเฝ้าระวัง' : '');
          html += '<tr><td>'+esc(d.name)+'</td><td class="num">'+fmt(mibToGB(d.capMiB),0)+'</td><td class="num">'+fmt(mibToGB(d.freeMiB),0)+'</td><td class="num">'+fmt(d.usedPct,2)+'</td><td>'+esc(remark)+'</td></tr>';
        });
        html += '</tbody></table>';
        html += '<p class="tpl-note">* Datastore usage 0-74% Normal, 75-84% Warning, 85-100% Alert</p>';

        // 2. Resource Utilization by Cluster (pie charts + N-1)
        html += '<h4 class="tpl-h1">2. Resource Utilization by Cluster</h4>';
        html += '<p class="tpl-p">คำนวณด้วย CPU Ratio (vCPU:pCPU) = '+fmt(cpuRatio,1)+':1 (ปรับค่าได้ที่แท็บ Compute Planning) — N-1 คือการจำลอง host ที่มีทรัพยากรมากที่สุดในคลัสเตอร์เสียหาย 1 ตัว</p>';
        var clusterChartIds = [];
        if(!clusterNames.length){
          html += '<p class="tpl-note">ไม่พบข้อมูล cluster ในไฟล์นี้</p>';
        }
        clusterNames.forEach(function(cName, ci){
          var base = simBaseValues(D, cName, {});
          var effCores = base.totalCores*cpuRatio;
          var cpuPct = effCores>0 ? (base.allocVCPU/effCores*100) : (base.allocVCPU>0?999:0);
          var memPct = base.totalRamGB>0 ? (base.allocVRamGB/base.totalRamGB*100) : (base.allocVRamGB>0?999:0);
          var idCpu = 'tplc'+ci+'cpu', idMem = 'tplc'+ci+'mem', idN1Cpu = 'tplc'+ci+'n1cpu', idN1Mem = 'tplc'+ci+'n1mem';
          html += '<p class="tpl-p" style="margin-top:14px;"><b>Cluster: '+esc(cName)+'</b> ('+fmt(base.hostCount)+' host, vCPU จัดสรร '+fmt(base.allocVCPU)+', vRAM จัดสรร '+fmt(base.allocVRamGB,0)+' GB)</p>';
          html += '<table style="border:none;"><tbody><tr style="border:none;">'+
            '<td style="border:none; width:25%; text-align:center;">CPU Utilization<br><div style="height:150px;"><canvas id="'+idCpu+'"></canvas></div></td>'+
            '<td style="border:none; width:25%; text-align:center;">Memory Utilization<br><div style="height:150px;"><canvas id="'+idMem+'"></canvas></div></td>';
          if(base.hostCount>=2 && (base.largestByCores||base.largestByRam)){
            var failCores = base.largestByCores?base.largestByCores.cores:0;
            var failRamGB = base.largestByRam?mibToGB(base.largestByRam.memMiB):0;
            var n1Cores = Math.max(0, base.totalCores-failCores);
            var n1RamGB = Math.max(0, base.totalRamGB-failRamGB);
            var n1EffCores = n1Cores*cpuRatio;
            var n1CpuPct = n1EffCores>0 ? (base.allocVCPU/n1EffCores*100) : (base.allocVCPU>0?999:0);
            var n1MemPct = n1RamGB>0 ? (base.allocVRamGB/n1RamGB*100) : (base.allocVRamGB>0?999:0);
            html += '<td style="border:none; width:25%; text-align:center;">CPU (N-1)<br><div style="height:150px;"><canvas id="'+idN1Cpu+'"></canvas></div></td>'+
              '<td style="border:none; width:25%; text-align:center;">Memory (N-1)<br><div style="height:150px;"><canvas id="'+idN1Mem+'"></canvas></div></td>';
            clusterChartIds.push({cpu:idCpu, mem:idMem, n1cpu:idN1Cpu, n1mem:idN1Mem, cpuPct:cpuPct, memPct:memPct, n1CpuPct:n1CpuPct, n1MemPct:n1MemPct});
          }else{
            html += '<td style="border:none; width:50%; text-align:center; color:#777; font-size:11px;" colspan="2">ต้องมี host อย่างน้อย 2 ตัวในคลัสเตอร์นี้จึงจะจำลอง N-1 ได้</td>';
            clusterChartIds.push({cpu:idCpu, mem:idMem, n1cpu:null, n1mem:null, cpuPct:cpuPct, memPct:memPct});
          }
          html += '</tr></tbody></table>';
        });

        // 3. Executive Summary
        html += '<h4 class="tpl-h1">3. Executive Summary</h4>';
        html += '<h5 class="tpl-h2">3.1 Conclusion</h5>';
        html += '<p class="tpl-p">According MFEC/ทีมงานดำเนินการตรวจสอบระบบ'+(pmdate?(' บนวันที่ <b>'+esc(pmdate)+'</b>'):'')+(customer?(' ให้กับ <b>'+esc(customer)+'</b>'):'')+' สรุปสถานะการใช้งานและความพร้อมของระบบได้ดังตารางด้านล่าง</p>';
        html += '<table><thead><tr><th colspan="3">VMware vSphere vCenter and ESXi Status</th></tr><tr><th>Details</th><th>Status</th><th>Remark</th></tr></thead><tbody>'+
          '<tr><td>CPU</td><td class="'+(totalCores>0 && totalVCPU/totalCores>4?'tpl-status-warning':'tpl-status-normal')+'">'+(totalCores>0 && totalVCPU/totalCores>4?'Warning':'Normal')+'</td><td>'+(totalCores>0?('vCPU:pCore = '+fmt(totalVCPU/totalCores,2)+':1'):'')+'</td></tr>'+
          '<tr><td>Datastore</td><td class="'+tplStatusClass(dsOverallStatus.cls)+'">'+dsOverallStatus.label+'</td><td>'+(lowDS.length?('มี '+lowDS.length+' datastore ใช้งานเกิน 85%'):(warnDS.length?('มี '+warnDS.length+' datastore ใช้งานเกิน 75%'):''))+'</td></tr>'+
          '<tr><td>Snapshot</td><td class="'+(oldSnap.length?'tpl-status-warning':'tpl-status-normal')+'">'+(oldSnap.length?'Warning':'Normal')+'</td><td>'+(oldSnap.length?(oldSnap.length+' snapshot อายุเกิน 30 วัน'):'')+'</td></tr>'+
          '</tbody></table>';

        html += '<h5 class="tpl-h2">3.2 Resource Summary</h5>';
        html += '<table><thead><tr><th>Host on vCenter '+(D.vCenterServer?esc(D.vCenterServer):'')+'</th><th class="num">Average CPU (%)</th><th class="num">Average Memory (%)</th></tr></thead><tbody>';
        D.hosts.forEach(function(h){
          html += '<tr><td>'+esc(h.name)+'</td><td class="num">'+(h.cpuUsagePct!=null?fmt(h.cpuUsagePct,2):'-')+'</td><td class="num">'+(h.memUsagePct!=null?fmt(h.memUsagePct,2):'-')+'</td></tr>';
        });
        html += '</tbody></table>';
        html += '<p class="tpl-note">** Usage: Normal = 0% - 74%, High = 75% - 85%, Critical = 86% - 100% **</p>';

        html += '<h5 class="tpl-h2">3.3 Software information (End of Support)</h5>';
        html += '<table><thead><tr><th>No</th><th>Version</th><th>End of support</th></tr></thead><tbody>';
        var swNo = 1;
        if(D.vCenterDisplayVersion){
          html += '<tr><td>'+(swNo++)+'</td><td>vCenter Server '+esc(D.vCenterDisplayVersion)+(D.vCenterBuild?(' (build '+esc(D.vCenterBuild)+')'):'')+'</td><td>'+(E.vcInfo?E.vcInfo.eogs:'ไม่พบในฐานข้อมูล')+'</td></tr>';
        }
        E.esxiRows.forEach(function(r){
          html += '<tr><td>'+(swNo++)+'</td><td>VMware ESXi '+esc(r.version)+' ('+esc(r.host)+')</td><td>'+(r.eogs||'ไม่พบในฐานข้อมูล')+'</td></tr>';
        });
        html += '</tbody></table>';

        html += '<h5 class="tpl-h2">3.4 Software version information</h5>';
        html += '<table><thead><tr><th>Product</th><th>Current version of customer</th><th>Latest version from product</th></tr></thead><tbody>'+
          '<tr><td>VMware vCenter</td><td>'+(D.vCenterDisplayVersion?(esc(D.vCenterDisplayVersion)+(D.vCenterBuild?(' (build '+esc(D.vCenterBuild)+')'):'')):'-')+'</td><td><i>[ตรวจสอบที่ Broadcom Product Lifecycle]</i></td></tr>'+
          '<tr><td>VMware ESXi</td><td>'+(E.esxiRows[0]?esc(E.esxiRows[0].version):'-')+'</td><td><i>[ตรวจสอบที่ Broadcom Product Lifecycle]</i></td></tr>'+
          '</tbody></table>';
        html += '<p class="tpl-note"><i>Note: If customers would like to upgrade the software to the latest version, MFEC recommends upgrading only when: (1) the current version has a known issue/bug impacting service, (2) customer needs new features, or (3) the current version has reached end of support.</i></p>';
        html += '<p class="tpl-note">หมายเหตุ: "Current version of customer" ของ vCenter อ้างอิงค่าจากคอลัมน์ VI SDK Server type ในชีต vInfo (fallback เป็น VI SDK API Version หากไม่พบ)</p>';

        html += '<h5 class="tpl-h2">3.5 Certificate Expire</h5>';
        html += '<table><thead><tr><th>Hostname</th><th>Expire date</th></tr></thead><tbody>'+
          '<tr><td><i>[กรอกเอง — RVTools ไม่มีข้อมูลใบรับรอง]</i></td><td></td></tr>'+
          '</tbody></table>';

        html += '<h4 class="tpl-h1">หมายเหตุ: ส่วนที่ต้องกรอกข้อมูลเพิ่มเติมด้วยตนเอง</h4>';
        html += '<p class="tpl-p">รายงานต้นฉบับ (MFEC Preventive Maintenance Report) มีหัวข้อที่ต้องใช้ข้อมูลจากแหล่งอื่นนอกเหนือจากไฟล์ RVTools เนื่องจาก RVTools เก็บเฉพาะข้อมูล inventory/config ของ vSphere เท่านั้น ได้แก่:</p>';
        html += '<table><thead><tr><th>หัวข้อ</th><th>แหล่งข้อมูลที่ต้องใช้เพิ่ม</th></tr></thead><tbody>'+
          '<tr><td>General Project Information</td><td>ข้อมูลโครงการ/ผู้ติดต่อ (กรอกเอง)</td></tr>'+
          '<tr><td>Server Preventive Maintenance (Hardware/LED status, Performance graph รายเครื่อง)</td><td>iDRAC/iLO ของแต่ละเครื่อง + เครื่องมือ monitoring (เช่น Zabbix/PRTG) สำหรับกราฟ CPU/Memory/Disk</td></tr>'+
          '<tr><td>vRealize Operations (vROPS Alerts/System Status)</td><td>Export หรือ screenshot จาก vROPS โดยตรง</td></tr>'+
          '<tr><td>Storage System (Dell EMC Unity — Pool/LUN/Performance)</td><td>Unisphere/Storage array management console</td></tr>'+
          '<tr><td>Backup Appliance (NetBackup)</td><td>NetBackup Appliance console</td></tr>'+
          '<tr><td>Network Card 10G</td><td>OS-level NIC monitoring บนแต่ละเซิร์ฟเวอร์</td></tr>'+
          '<tr><td>Network Switch (CPU/Temp/Fan, IOS version, EoS/EoL, Logging)</td><td>Switch CLI/SNMP + Cisco EoS/EoL lookup</td></tr>'+
          '<tr><td>3.5 Certificate Expire</td><td>ตรวจสอบใบรับรองบนแต่ละ endpoint โดยตรง (เช่น openssl/เครื่องมือสแกน cert)</td></tr>'+
          '</tbody></table>';
        html += '<p class="tpl-note">ส่วนด้านบนนี้ (1. vCenter, 2. Resource Utilization by Cluster, 3.1–3.4 Executive Summary) คือทั้งหมดที่สร้างจากข้อมูลจริงในไฟล์ RVTools ที่อัปโหลด</p>';

        document.getElementById('tpl-body').innerHTML = '<div class="tpl-report">'+html+'</div>';

        // instantiate cluster pie charts now that their canvases exist in the DOM
        clusterChartIds.forEach(function(c){
          renderTplPie(c.cpu, c.cpuPct);
          renderTplPie(c.mem, c.memPct);
          if(c.n1cpu) renderTplPie(c.n1cpu, c.n1CpuPct);
          if(c.n1mem) renderTplPie(c.n1mem, c.n1MemPct);
        });

        window.__tplClusterCharts = clusterChartIds;

        // ---- customer email summary (Thai + English) ----
        var totalVMs = D.realVms.length;
        var onVMs = D.realVms.filter(function(v){return v.power.indexOf('on')!==-1;}).length;
        var offVMs = D.realVms.filter(function(v){return v.power.indexOf('off')!==-1;}).length;
        var issuesTh = [], issuesEn = [];
        if(lowDS.length){ issuesTh.push('- Datastore '+lowDS.length+' แห่ง มีพื้นที่ใช้งานเกิน 85% ควรเพิ่มพื้นที่หรือย้ายข้อมูล'); issuesEn.push('- '+lowDS.length+' datastore(s) are above 85% used capacity and should be expanded or rebalanced.'); }
        else if(warnDS.length){ issuesTh.push('- Datastore '+warnDS.length+' แห่ง ใช้งานเกิน 75% ควรเฝ้าระวัง'); issuesEn.push('- '+warnDS.length+' datastore(s) are above 75% used capacity and should be monitored.'); }
        if(oldSnap.length){ issuesTh.push('- พบ Snapshot ที่มีอายุเกิน 30 วัน จำนวน '+oldSnap.length+' รายการ ควรลบหรือรวมเพื่อคืนพื้นที่และประสิทธิภาพ'); issuesEn.push('- '+oldSnap.length+' snapshot(s) older than 30 days were found and should be removed or consolidated.'); }
        var expiredEsxi = E.esxiRows.filter(function(r){return r.days!=null && r.days<0;});
        if(expiredEsxi.length){ issuesTh.push('- ESXi Host '+expiredEsxi.length+' เครื่อง หมดระยะซัพพอร์ตแล้ว ควรวางแผนอัปเกรด'); issuesEn.push('- '+expiredEsxi.length+' ESXi host(s) are past end of support and should be upgraded.'); }
        if(E.vcInfo && E.vcDays!=null && E.vcDays<0){ issuesTh.push('- vCenter Server หมดระยะซัพพอร์ตแล้ว ควรวางแผนอัปเกรด'); issuesEn.push('- vCenter Server is past end of support and should be upgraded.'); }
        if(!issuesTh.length){ issuesTh.push('- ไม่พบประเด็นผิดปกติที่เด่นชัดจากข้อมูลที่ตรวจสอบ'); issuesEn.push('- No significant issues were found in this review.'); }
        var overallTh = (lowDS.length||expiredEsxi.length||(E.vcInfo&&E.vcDays!=null&&E.vcDays<0)) ? 'ควรดำเนินการแก้ไขในบางรายการ' : 'ระบบโดยรวมอยู่ในสถานะปกติ';
        var overallEn = (lowDS.length||expiredEsxi.length||(E.vcInfo&&E.vcDays!=null&&E.vcDays<0)) ? 'Some items require action.' : 'The system is overall in normal condition.';

        var clusterUtilLinesTh = [], clusterUtilLinesEn = [];
        clusterNames.forEach(function(cName){
          var base = simBaseValues(D, cName, {});
          var effCores = base.totalCores*cpuRatio;
          var cpuPct = effCores>0 ? (base.allocVCPU/effCores*100) : (base.allocVCPU>0?999:0);
          var memPct = base.totalRamGB>0 ? (base.allocVRamGB/base.totalRamGB*100) : (base.allocVRamGB>0?999:0);
          clusterUtilLinesTh.push('- '+cName+': CPU '+fmt(cpuPct,1)+'%, Memory '+fmt(memPct,1)+'% ('+fmt(base.hostCount)+' host)');
          clusterUtilLinesEn.push('- '+cName+': CPU '+fmt(cpuPct,1)+'%, Memory '+fmt(memPct,1)+'% ('+fmt(base.hostCount)+' host(s))');
        });
        if(!clusterUtilLinesTh.length){ clusterUtilLinesTh.push('- ไม่พบข้อมูล cluster ในไฟล์นี้'); clusterUtilLinesEn.push('- No cluster data found in this file.'); }

        var emailTh = 'เรื่อง: สรุปผลการตรวจสอบระบบ VMware Infrastructure'+(pmdate?(' ประจำวันที่ '+pmdate):'')+'\n\n'+
          'เรียน '+(customer||'[ชื่อลูกค้า]')+'\n\n'+
          'ทีมงาน MFEC ได้ดำเนินการตรวจสอบระบบ VMware vSphere Infrastructure'+(pmdate?(' เมื่อวันที่ '+pmdate):'')+' สรุปผลได้ดังนี้\n\n'+
          'ภาพรวมระบบ:\n'+
          '- Virtual Machine ทั้งหมด: '+totalVMs+' เครื่อง (เปิดใช้งาน '+onVMs+', ปิดอยู่ '+offVMs+')\n'+
          '- ESXi Host: '+D.hosts.length+' เครื่อง ใน '+clusterNames.length+' Cluster\n'+
          '- Datastore: '+D.datastores.length+' แห่ง\n\n'+
          'Utilization ตาม Cluster (คำนวณด้วย CPU Ratio '+fmt(cpuRatio,1)+':1):\n'+clusterUtilLinesTh.join('\n')+'\n\n'+
          'ประเด็นที่พบ:\n'+issuesTh.join('\n')+'\n\n'+
          'สถานะโดยรวม: '+overallTh+'\n\n'+
          'รายละเอียดเพิ่มเติมตามเอกสารรายงานที่แนบมาพร้อมนี้ หากมีข้อสงสัยประการใด ทีมงานยินดีให้ข้อมูลเพิ่มเติมครับ/ค่ะ\n\n'+
          'ขอบคุณครับ/ค่ะ\n'+(preparedby||'[ผู้จัดทำ]')+'\nMFEC';

        var emailEn = 'Subject: VMware Infrastructure Health Check Summary'+(pmdate?(' — '+pmdate):'')+'\n\n'+
          'Dear '+(customer||'[Customer Name]')+',\n\n'+
          'The MFEC team has completed a health check of the VMware vSphere infrastructure'+(pmdate?(' on '+pmdate):'')+'. Summary below.\n\n'+
          'System Overview:\n'+
          '- Total Virtual Machines: '+totalVMs+' (Powered On '+onVMs+', Powered Off '+offVMs+')\n'+
          '- ESXi Hosts: '+D.hosts.length+' across '+clusterNames.length+' cluster(s)\n'+
          '- Datastores: '+D.datastores.length+'\n\n'+
          'Utilization by Cluster (calculated with CPU Ratio '+fmt(cpuRatio,1)+':1):\n'+clusterUtilLinesEn.join('\n')+'\n\n'+
          'Findings:\n'+issuesEn.join('\n')+'\n\n'+
          'Overall Status: '+overallEn+'\n\n'+
          'Please see the attached full report for details. Feel free to reach out if you have any questions.\n\n'+
          'Best regards,\n'+(preparedby||'[Prepared by]')+'\nMFEC';

        renderEmailBlock('tpl-email-block', emailTh, emailEn);
      }

      /* ===================== export to word (.docx) ===================== */
      function statusColorHex(label){
        var s = String(label||'').toLowerCase();
        if(s.indexOf('critical')!==-1 || s.indexOf('bad')!==-1) return 'C0392B';
        if(s.indexOf('warning')!==-1 || s.indexOf('warn')!==-1) return 'B8860B';
        if(s.indexOf('normal')!==-1 || s.indexOf('ok')!==-1) return '1A7A3C';
        return undefined;
      }
      function docxCellText(text, opts){
        opts = opts || {};
        var dx = window.docx;
        var thin = {style:dx.BorderStyle.SINGLE, size:2, color:'999999'};
        return new dx.TableCell({
          width: opts.width!=null ? {size:opts.width, type:dx.WidthType.PERCENTAGE} : undefined,
          shading: opts.header ? {fill:'E8E8E8'} : (opts.fill ? {fill:opts.fill} : undefined),
          margins:{top:60, bottom:60, left:100, right:100},
          borders:{top:thin,bottom:thin,left:thin,right:thin},
          children:[new dx.Paragraph({
            alignment: opts.right ? dx.AlignmentType.RIGHT : dx.AlignmentType.LEFT,
            children:[new dx.TextRun({text:String(text==null?'':text), bold:!!opts.header, italics:!!opts.italic, size:opts.size||18, color:opts.color||'111111', font:'Calibri'})]
          })]
        });
      }
      function docxTable(headerRow, rows, widths){
        var dx = window.docx;
        var thin = {style:dx.BorderStyle.SINGLE, size:2, color:'999999'};
        var trs = [new dx.TableRow({children: headerRow.map(function(h,i){ return docxCellText(h, {header:true, width: widths?widths[i]:undefined}); }), tableHeader:true})];
        rows.forEach(function(r){
          trs.push(new dx.TableRow({children: r.map(function(v,i){
            var opts = {width: widths?widths[i]:undefined};
            var textVal = v;
            if(v && typeof v === 'object'){ opts.right = v.right; opts.color = v.color; opts.italic = v.italic; textVal = v.text; }
            if(!opts.color) opts.color = statusColorHex(textVal);
            return docxCellText(textVal, opts);
          })}));
        });
        return new dx.Table({
          rows:trs, width:{size:100, type:dx.WidthType.PERCENTAGE},
          borders:{top:thin,bottom:thin,left:thin,right:thin,insideHorizontal:thin,insideVertical:thin}
        });
      }
      function docxHeading(text, level){
        var dx = window.docx;
        var opts = {
          children:[new dx.TextRun({text:text, bold:true, size:level===1?26:22, color:'111111', font:'Calibri'})],
          spacing:{before:280, after:140}
        };
        if(level===1){
          opts.border = {bottom:{style:dx.BorderStyle.SINGLE, size:8, color:'444444', space:4}};
        }
        return new dx.Paragraph(opts);
      }
      function docxPara(text, opts){
        opts = opts || {};
        var dx = window.docx;
        return new dx.Paragraph({children:[new dx.TextRun({text:text, italics:!!opts.italic, bold:!!opts.bold, size:opts.size||19, color:opts.color||'111111', font:'Calibri'})], spacing:{after:140}});
      }
      function docxImageFromCanvas(canvasId, maxWidthPx, maxHeightPx){
        var dx = window.docx;
        var canvas = document.getElementById(canvasId);
        if(!canvas) return null;
        try{
          var dataUrl = canvas.toDataURL('image/png');
          var base64 = dataUrl.split(',')[1];
          var binary = atob(base64);
          var bytes = new Uint8Array(binary.length);
          for(var i=0;i<binary.length;i++) bytes[i] = binary.charCodeAt(i);
          // preserve the canvas's actual pixel aspect ratio, scaled to fit within the max box
          var srcW = canvas.width || maxWidthPx;
          var srcH = canvas.height || maxHeightPx;
          var scale = Math.min(maxWidthPx/srcW, maxHeightPx/srcH);
          var outW = Math.max(1, Math.round(srcW*scale));
          var outH = Math.max(1, Math.round(srcH*scale));
          return new dx.ImageRun({type:'png', data:bytes, transformation:{width:outW, height:outH}});
        }catch(e){ console.error('docx image capture failed', e); return null; }
      }

      function exportToWord(D){
        if(!window.docx){ alert('ไลบรารีสร้างไฟล์ Word (docx.js) โหลดไม่สำเร็จ — ตรวจสอบการเชื่อมต่ออินเทอร์เน็ตแล้วลองใหม่'); return; }
        var dx = window.docx;
        var E = computeExpireData(D);
        var customer = (document.getElementById('inp-customer').value||'').trim() || '[ชื่อลูกค้า]';
        var pmdate = (document.getElementById('inp-pmdate').value||'').trim() || '[วันที่ตรวจ]';
        var preparedby = (document.getElementById('inp-preparedby').value||'').trim() || '[ผู้จัดทำ]';
        var totalCores = D.hosts.reduce(function(a,h){return a+h.cores;},0);
        var totalVCPU = D.realVms.reduce(function(a,v){return a+v.cpus;},0);
        var lowDS = D.datastores.filter(function(d){return d.usedPct>=85;});
        var warnDS = D.datastores.filter(function(d){return d.usedPct>=75 && d.usedPct<85;});
        var oldSnap = D.snapshots.filter(function(s){return s.ageDays!=null && s.ageDays>30;});
        var children = [];

        children.push(new dx.Paragraph({alignment:dx.AlignmentType.CENTER, spacing:{after:80}, children:[new dx.TextRun({text:'VMware Preventive Maintenance Report', bold:true, size:36, color:'111111', font:'Calibri'})]}));
        children.push(new dx.Paragraph({alignment:dx.AlignmentType.CENTER, children:[new dx.TextRun({text:customer+' — '+pmdate+' — จัดทำโดย '+preparedby, italics:true, size:18, color:'555555', font:'Calibri'})], spacing:{after:300}}));

        // 1. vCenter
        children.push(docxHeading('1. vCenter', 1));
        children.push(docxHeading('1.1 vCenter Information', 2));
        children.push(docxTable(['Field','Value'], [
          ['Hostname', D.vCenterServer || '[ไม่พบในไฟล์ RVTools]'],
          ['Current Build/Patch', D.vCenterDisplayVersion ? ('VMware vCenter Server '+D.vCenterDisplayVersion+(D.vCenterBuild?(' (build '+D.vCenterBuild+')'):'')) : '[ไม่พบข้อมูล]']
        ], [28,72]));

        children.push(docxHeading('1.2 vCenter Status', 2));
        children.push(docxPara((D.vCenterServer||'vCenter')+' server '+(D.vms.length?'สามารถเชื่อมต่อและ export ข้อมูล RVTools ได้สำเร็จ — บ่งชี้ว่า service อยู่ในสถานะทำงานปกติ':'ไม่สามารถยืนยันสถานะจากไฟล์นี้ได้')));
        children.push(docxPara('หมายเหตุ: RVTools ไม่ได้บันทึก system log/service status โดยตรง ควรตรวจสอบด้วยตนเอง', {italic:true, size:16, color:'666666'}));

        children.push(docxHeading('1.3 VMware Status and VM', 2));
        children.push(docxPara('1.3.1 Utilization and Status on Host', {size:20}));
        children.push(docxTable(['No.','Hostname','Cores','% CPU','Memory (GB)','% Memory','Status'],
          D.hosts.map(function(h,i){
            var worst = Math.max(h.cpuUsagePct||0, h.memUsagePct||0);
            var stlabel = (h.cpuUsagePct==null && h.memUsagePct==null) ? '-' : (worst>=85?'Critical':(worst>=75?'Warning':'Normal'));
            return [i+1, h.name, {text:fmt(h.cores),right:true}, {text:h.cpuUsagePct!=null?fmt(h.cpuUsagePct,1):'-',right:true}, {text:fmt(mibToGB(h.memMiB),0),right:true}, {text:h.memUsagePct!=null?fmt(h.memUsagePct,1):'-',right:true}, stlabel];
          })
        ));

        function top10Rows(list, field, conv){
          return list.slice().sort(function(a,b){return (b[field]||0)-(a[field]||0);}).slice(0,10).map(function(v,i){
            var val = v[field]||0;
            return [i+1, v.name, {text:fmt(conv?conv(val):val, conv?0:0), right:true}];
          });
        }
        children.push(docxPara('1.3.2 Top 10 VM by vCPU (จัดสรร)', {size:20}));
        children.push(docxTable(['No.','VM Name','vCPU'], top10Rows(D.realVms, 'cpus')));
        children.push(docxPara('1.3.3 Top 10 VM by Memory', {size:20}));
        if(D.hasMemUsageData){
          children.push(docxTable(['No.','VM Name','Memory Usage (GB)'], top10Rows(D.realVms.filter(function(v){return v.memActiveMiB!=null;}), 'memActiveMiB', mibToGB)));
        }else{
          children.push(docxTable(['No.','VM Name','RAM จัดสรร (GB)'], top10Rows(D.realVms, 'memMiB', mibToGB)));
        }

        children.push(docxHeading('1.4 Datastore', 2));
        children.push(docxTable(['Datastore','Capacity (GB)','Free (GB)','Used (%)','Remark'],
          D.datastores.slice().sort(function(a,b){return b.usedPct-a.usedPct;}).map(function(d){
            var remark = d.usedPct>=85 ? 'Capacity will full' : (d.usedPct>=75 ? 'ควรเฝ้าระวัง' : '');
            return [d.name, {text:fmt(mibToGB(d.capMiB),0),right:true}, {text:fmt(mibToGB(d.freeMiB),0),right:true}, {text:fmt(d.usedPct,2),right:true}, remark];
          })
        ));

        // 2. Resource Utilization by Cluster (charts captured from already-rendered canvases)
        children.push(docxHeading('2. Resource Utilization by Cluster', 1));
        var clusterCharts = window.__tplClusterCharts || [];
        if(!clusterCharts.length){
          children.push(docxPara('ไม่พบข้อมูล cluster ในไฟล์นี้'));
        }
        clusterCharts.forEach(function(c, ci){
          children.push(docxPara('Cluster #'+(ci+1)+' — CPU: '+fmt(c.cpuPct,1)+'%  |  Memory: '+fmt(c.memPct,1)+'%'+(c.n1CpuPct!=null?('  |  N-1 CPU: '+fmt(c.n1CpuPct,1)+'%  |  N-1 Memory: '+fmt(c.n1MemPct,1)+'%'):'  |  N-1: ไม่สามารถจำลองได้ (host น้อยกว่า 2)'), {size:18}));
          var imgs = [docxImageFromCanvas(c.cpu, 160,160), docxImageFromCanvas(c.mem,160,160)];
          if(c.n1cpu) imgs.push(docxImageFromCanvas(c.n1cpu,160,160));
          if(c.n1mem) imgs.push(docxImageFromCanvas(c.n1mem,160,160));
          imgs = imgs.filter(Boolean);
          if(imgs.length){
            children.push(new dx.Paragraph({children:imgs, spacing:{after:200}}));
          }
        });

        // 3. Executive Summary
        children.push(docxHeading('3. Executive Summary', 1));
        children.push(docxHeading('3.1 Conclusion', 2));
        children.push(docxPara('MFEC ดำเนินการตรวจสอบระบบบนวันที่ '+pmdate+' ให้กับ '+customer+' สรุปสถานะได้ดังตารางด้านล่าง'));
        children.push(docxTable(['Details','Status','Remark'], [
          ['CPU', (totalCores>0 && totalVCPU/totalCores>4?'Warning':'Normal'), totalCores>0?('vCPU:pCore = '+fmt(totalVCPU/totalCores,2)+':1'):''],
          ['Datastore', lowDS.length?'Critical':(warnDS.length?'Warning':'Normal'), lowDS.length?('มี '+lowDS.length+' datastore ใช้งานเกิน 85%'):(warnDS.length?('มี '+warnDS.length+' datastore ใช้งานเกิน 75%'):'')],
          ['Snapshot', oldSnap.length?'Warning':'Normal', oldSnap.length?(oldSnap.length+' snapshot อายุเกิน 30 วัน'):'']
        ]));

        children.push(docxHeading('3.2 Resource Summary', 2));
        children.push(docxTable(['Host','Average CPU (%)','Average Memory (%)'],
          D.hosts.map(function(h){ return [h.name, {text:h.cpuUsagePct!=null?fmt(h.cpuUsagePct,2):'-',right:true}, {text:h.memUsagePct!=null?fmt(h.memUsagePct,2):'-',right:true}]; })
        ));

        children.push(docxHeading('3.3 Software information (End of Support)', 2));
        var swRows = [];
        if(D.vCenterDisplayVersion) swRows.push(['vCenter Server '+D.vCenterDisplayVersion+(D.vCenterBuild?(' (build '+D.vCenterBuild+')'):''), E.vcInfo?E.vcInfo.eogs:'ไม่พบในฐานข้อมูล']);
        E.esxiRows.forEach(function(r){ swRows.push(['VMware ESXi '+r.version+' ('+r.host+')', r.eogs||'ไม่พบในฐานข้อมูล']); });
        children.push(docxTable(['Version','End of Support'], swRows));

        children.push(docxHeading('3.4 Software version information', 2));
        children.push(docxTable(['Product','Current version of customer','Latest version from product'], [
          ['VMware vCenter', D.vCenterDisplayVersion?(D.vCenterDisplayVersion+(D.vCenterBuild?(' (build '+D.vCenterBuild+')'):'')):'-', '[ตรวจสอบที่ Broadcom Product Lifecycle]'],
          ['VMware ESXi', E.esxiRows[0]?E.esxiRows[0].version:'-', '[ตรวจสอบที่ Broadcom Product Lifecycle]']
        ]));
        children.push(docxPara('หมายเหตุ: "Current version of customer" ของ vCenter อ้างอิงค่าจากคอลัมน์ VI SDK Server type ในชีต vInfo', {italic:true, size:16, color:'666666'}));

        children.push(docxHeading('3.5 Certificate Expire', 2));
        children.push(docxTable(['Hostname','Expire date'], [['[กรอกเอง — RVTools ไม่มีข้อมูลใบรับรอง]', '']]));

        children.push(docxHeading('หมายเหตุ: ส่วนที่ต้องกรอกข้อมูลเพิ่มเติมด้วยตนเอง', 1));
        children.push(docxTable(['หัวข้อ','แหล่งข้อมูลที่ต้องใช้เพิ่ม'], [
          ['General Project Information','ข้อมูลโครงการ/ผู้ติดต่อ (กรอกเอง)'],
          ['Server Preventive Maintenance (Hardware/LED, Performance graph)','iDRAC/iLO + เครื่องมือ monitoring (Zabbix/PRTG)'],
          ['vRealize Operations (vROPS)','Export/screenshot จาก vROPS โดยตรง'],
          ['Storage System (Dell EMC Unity)','Unisphere/Storage array management console'],
          ['Backup Appliance (NetBackup)','NetBackup Appliance console'],
          ['Network Card 10G','OS-level NIC monitoring บนแต่ละเซิร์ฟเวอร์'],
          ['Network Switch','Switch CLI/SNMP + Cisco EoS/EoL lookup'],
          ['Certificate Expire','ตรวจสอบใบรับรองบนแต่ละ endpoint โดยตรง']
        ]));

        var doc = new dx.Document({
          styles:{ default:{ document:{ run:{font:'Calibri', size:19} } } },
          sections:[{ properties:{}, children:children }]
        });
        dx.Packer.toBlob(doc).then(function(blob){
          var url = URL.createObjectURL(blob);
          var a = document.createElement('a');
          a.href = url;
          a.download = 'VMware_Preventive_Maintenance_Report.docx';
          document.body.appendChild(a);
          a.click();
          document.body.removeChild(a);
          setTimeout(function(){ URL.revokeObjectURL(url); }, 5000);
        }).catch(function(err){
          console.error(err);
          alert('สร้างไฟล์ Word ไม่สำเร็จ: '+err.message);
        });
      }

      function fetchLiveAdvisories(D){
        var el = document.getElementById('sec-live');
        var tag = document.getElementById('sec-live-tag');
        el.innerHTML = '<div class="empty-note">กำลังตรวจสอบข้อมูลล่าสุดจาก NVD...</div>';
        tag.textContent = '';

        var since = new Date(Date.now() - 120*86400000); // last 120 days (NVD API max range per request)
        var pad = function(n){ return String(n).padStart(2,'0'); };
        var toIso = function(d){ return d.getFullYear()+'-'+pad(d.getMonth()+1)+'-'+pad(d.getDate())+'T00:00:00.000'; };
        var url = 'https://services.nvd.nist.gov/rest/json/cves/2.0?keywordSearch=VMware%20ESXi%20OR%20VMware%20vCenter&pubStartDate='+encodeURIComponent(toIso(since))+'&pubEndDate='+encodeURIComponent(toIso(new Date()))+'&resultsPerPage=30';

        fetch(url).then(function(res){
          if(!res.ok) throw new Error('NVD ตอบกลับสถานะ '+res.status);
          return res.json();
        }).then(function(data){
          var items = (data.vulnerabilities||[]).map(function(x){
            var c = x.cve;
            var id = c.id;
            var desc = (c.descriptions||[]).filter(function(d){return d.lang==='en';})[0];
            var descText = desc ? desc.value : '';
            var metrics = c.metrics||{};
            var cvssData = (metrics.cvssMetricV31||metrics.cvssMetricV30||metrics.cvssMetricV2||[])[0];
            var score = cvssData ? cvssData.cvssData.baseScore : null;
            var sevText = cvssData ? (cvssData.cvssData.baseSeverity||'') : '';
            var isVmwareRelevant = /vmware|esxi|vcenter/i.test(descText) || /vmware|esxi|vcenter/i.test(id);
            return {id:id, published:c.published, desc:descText, score:score, sev:sevText, relevant:isVmwareRelevant};
          }).filter(function(x){ return x.relevant; })
            .sort(function(a,b){ return new Date(b.published) - new Date(a.published); });

          if(!items.length){
            el.innerHTML = '<div class="empty-note">ไม่พบ CVE ใหม่ที่เกี่ยวกับ VMware ESXi/vCenter ใน NVD ช่วง 120 วันที่ผ่านมา (หรือ NVD ยังไม่ได้จัดทำดัชนีคำอธิบายที่ระบุคำเหล่านี้ชัดเจน)</div>';
            tag.textContent = 'อัปเดตล่าสุด: '+new Date().toLocaleString('th-TH');
            return;
          }
          el.innerHTML = '<div class="table-wrap"><table><thead><tr><th>CVE</th><th>เผยแพร่</th><th class="num">CVSS</th><th>ความรุนแรง</th><th>คำอธิบาย</th></tr></thead><tbody>'+
            items.slice(0,20).map(function(x){
              var sevCls = /CRITICAL/i.test(x.sev)?'bad':(/HIGH/i.test(x.sev)?'warn':'');
              return '<tr><td><a href="https://nvd.nist.gov/vuln/detail/'+esc(x.id)+'" target="_blank" rel="noopener">'+esc(x.id)+'</a></td>'+
                '<td>'+(x.published?esc(x.published.slice(0,10)):'-')+'</td>'+
                '<td class="num">'+(x.score!=null?fmt(x.score,1):'-')+'</td>'+
                '<td><span class="pill '+sevCls+'">'+esc(x.sev||'-')+'</span></td>'+
                '<td style="max-width:420px;">'+esc(x.desc.length>220?(x.desc.slice(0,220)+'…'):x.desc)+'</td></tr>';
            }).join('') + '</tbody></table></div>'+
            '<div class="empty-note">รายการนี้ดึงจาก NVD ตามคำค้น "VMware ESXi"/"VMware vCenter" ในช่วง 120 วันล่าสุด เป็นข้อมูลเสริมเพื่อแจ้งเตือน — ยังไม่ได้ผนวกเข้ากับตารางจับคู่เวอร์ชันด้านบน โปรดตรวจสอบ VMSA ที่เกี่ยวข้องบน Broadcom Support Portal ด้วยตนเอง</div>';
          tag.textContent = 'อัปเดตล่าสุด: '+new Date().toLocaleString('th-TH');
        }).catch(function(err){
          console.error(err);
          el.innerHTML = '<div class="empty-note">ไม่สามารถดึงข้อมูลล่าสุดจาก NVD ได้ตอนนี้ (อาจถูกจำกัดโดยเครือข่าย/CORS/rate limit ของ NVD) — ใช้ฐานข้อมูล VMSA ที่มีอยู่ในเครื่องมือนี้แทนไปก่อน (ปรับปรุงล่าสุด ก.ย. 2569) รายละเอียด: '+esc(err.message)+'</div>';
          tag.textContent = 'ดึงข้อมูลไม่สำเร็จ';
        });
      }

      /* ===================== capacity simulator ===================== */
      var simState = { D:null, hostDelta:0, wired:false, cluster:'ALL', hostOverride:{} };

      function simEffectiveHosts(D, clusterFilter, hostOverride){
        if(!clusterFilter || clusterFilter==='ALL') return D.hosts;
        return D.hosts.filter(function(h){
          if(hostOverride.hasOwnProperty(h.name)) return hostOverride[h.name];
          return h.cluster===clusterFilter;
        });
      }

      function simBaseValues(D, clusterFilter, hostOverride){
        hostOverride = hostOverride || {};
        var hostsIn = simEffectiveHosts(D, clusterFilter, hostOverride);
        var vmsIn = clusterFilter && clusterFilter!=='ALL' ? D.realVms.filter(function(v){return v.cluster===clusterFilter;}) : D.realVms;
        var totalCores = hostsIn.reduce(function(a,h){return a+h.cores;},0);
        var totalRamGB = mibToGB(hostsIn.reduce(function(a,h){return a+h.memMiB;},0));
        var totalStorageTB = mibToTB(D.datastores.reduce(function(a,d){return a+d.capMiB;},0));
        var usedStorageTB = mibToTB(D.datastores.reduce(function(a,d){return a+d.usedMiB;},0));
        var allocVCPU = vmsIn.reduce(function(a,v){return a+v.cpus;},0);
        var allocVRamGB = mibToGB(vmsIn.reduce(function(a,v){return a+v.memMiB;},0));
        var hostCount = hostsIn.length;
        var avgCores = hostCount>0 ? (totalCores/hostCount) : 32;
        var avgRamGB = hostCount>0 ? (totalRamGB/hostCount) : 256;
        // largest host tracked separately per resource — a host may be biggest by cores but not by RAM, or vice versa
        var largestByCores = null, largestByRam = null;
        hostsIn.forEach(function(h){
          if(!largestByCores || h.cores>largestByCores.cores) largestByCores = h;
          if(!largestByRam || h.memMiB>largestByRam.memMiB) largestByRam = h;
        });
        return {totalCores:totalCores, totalRamGB:totalRamGB, totalStorageTB:totalStorageTB, usedStorageTB:usedStorageTB,
                allocVCPU:allocVCPU, allocVRamGB:allocVRamGB, hostCount:hostCount, avgCores:avgCores, avgRamGB:avgRamGB,
                largestByCores:largestByCores, largestByRam:largestByRam, hostsIn:hostsIn};
      }

      function simPieData(usedPct){
        var used = Math.max(0, Math.min(100, usedPct));
        var free = Math.max(0, 100-used);
        var over = Math.max(0, usedPct-100);
        return {used:used, free:free, over:over};
      }

      function renderSimPie(canvasId, usedPct, label){
        var d = simPieData(usedPct);
        killChart(canvasId);
        var labels = ['ใช้งาน/จัดสรรแล้ว','ว่าง'];
        var data = [d.used, d.free];
        var colors = [usedPct>=90?'#E2596B':(usedPct>=75?'#F0A857':'#3ED2A0'),'#2C3742'];
        if(d.over>0){ labels = ['เกินขีดความสามารถ (Over-provisioned)','ใช้งานอยู่ในขอบเขต']; data=[Math.min(d.over,100),100-Math.min(d.over,100)]; colors=['#E2596B','#5A2A32']; }
        CHARTS[canvasId] = new Chart(document.getElementById(canvasId), {
          type:'doughnut',
          data:{ labels:labels, datasets:[{data:data, backgroundColor:colors, borderColor:'#1A222B', borderWidth:2}] },
          options:{ plugins:{legend:{position:'bottom', labels:{color:'#8B96A3', font:{size:10.5}}}}, maintainAspectRatio:false }
        });
      }

      function populateSimClusterSelect(D){
        var sel = document.getElementById('simClusterSelect');
        var clusters = [];
        D.hosts.forEach(function(h){ if(h.cluster && clusters.indexOf(h.cluster)===-1) clusters.push(h.cluster); });
        clusters.sort();
        var prev = simState.cluster;
        sel.innerHTML = '<option value="ALL">ทั้งหมด (All Clusters)</option>' +
          clusters.map(function(c){ return '<option value="'+esc(c)+'">'+esc(c)+'</option>'; }).join('');
        // keep previous selection if it still exists, else default to ALL
        sel.value = clusters.indexOf(prev)!==-1 ? prev : 'ALL';
        simState.cluster = sel.value;
      }

      function populateSimHostChecklist(D){
        var wrap = document.getElementById('simHostChecklistWrap');
        var box = document.getElementById('simHostChecklist');
        if(simState.cluster==='ALL'){
          wrap.style.display = 'none';
          box.innerHTML = '';
          return;
        }
        wrap.style.display = 'block';
        var cluster = simState.cluster;
        var hostsSorted = D.hosts.slice().sort(function(a,b){
          var aIn = a.cluster===cluster, bIn = b.cluster===cluster;
          if(aIn!==bIn) return aIn ? -1 : 1;
          return a.name.localeCompare(b.name);
        });
        box.innerHTML = hostsSorted.map(function(h){
          var checked = simState.hostOverride.hasOwnProperty(h.name) ? simState.hostOverride[h.name] : (h.cluster===cluster);
          var tagTxt = h.cluster===cluster ? '' : ' <span style="color:var(--muted);">(ย้ายจาก '+esc(h.cluster)+')</span>';
          return '<label style="display:flex; align-items:center; gap:8px; padding:4px 0; cursor:pointer;">'+
            '<input type="checkbox" class="sim-host-cb" data-host="'+esc(h.name)+'" '+(checked?'checked':'')+'>'+
            '<span>'+esc(h.name)+' — '+fmt(h.cores)+' cores, '+fmt(mibToGB(h.memMiB),0)+' GB'+tagTxt+'</span>'+
            '</label>';
        }).join('') || '<div class="empty-note">ไม่พบ host ในไฟล์นี้</div>';
        box.querySelectorAll('.sim-host-cb').forEach(function(cb){
          cb.addEventListener('change', function(){
            simState.hostOverride[cb.dataset.host] = cb.checked;
            recomputeSimulator();
          });
        });
      }

      function recomputeSimulator(){
        var D = simState.D;
        if(!D) return;
        var clusterFilter = simState.cluster;
        var base = simBaseValues(D, clusterFilter, simState.hostOverride);
        var coresPerHost = num(document.getElementById('simCoresPerHost').value) || base.avgCores;
        var ramPerHost = num(document.getElementById('simRamPerHost').value) || base.avgRamGB;
        var storageDeltaTB = num(document.getElementById('simStorageDeltaTB').value);
        var cpuRatio = num(document.getElementById('simCpuRatio').value) || 1;
        var hostDelta = simState.hostDelta;

        document.getElementById('simHostDeltaLabel').textContent = (hostDelta>=0?'+':'')+hostDelta;

        var newCores = Math.max(0, base.totalCores + hostDelta*coresPerHost);
        var newRamGB = Math.max(0, base.totalRamGB + hostDelta*ramPerHost);
        var newStorageTB = Math.max(0, base.totalStorageTB + storageDeltaTB);
        var newHostCount = Math.max(0, base.hostCount + hostDelta);

        var effectiveCoresCur = base.totalCores * cpuRatio;
        var effectiveCoresNew = newCores * cpuRatio;

        var cpuPct = effectiveCoresNew>0 ? (base.allocVCPU/effectiveCoresNew*100) : (base.allocVCPU>0?999:0);
        var memPct = newRamGB>0 ? (base.allocVRamGB/newRamGB*100) : (base.allocVRamGB>0?999:0);
        var stPct = newStorageTB>0 ? (base.usedStorageTB/newStorageTB*100) : (base.usedStorageTB>0?999:0);

        var curCpuPct = effectiveCoresCur>0 ? (base.allocVCPU/effectiveCoresCur*100) : 0;
        var curMemPct = base.totalRamGB>0 ? (base.allocVRamGB/base.totalRamGB*100) : 0;
        var curStPct = base.totalStorageTB>0 ? (base.usedStorageTB/base.totalStorageTB*100) : 0;

        renderSimPie('chart-sim-cpu', cpuPct, 'CPU');
        renderSimPie('chart-sim-mem', memPct, 'Memory');
        renderSimPie('chart-sim-storage', stPct, 'Storage');

        document.getElementById('sim-cpu-tag').textContent = fmt(cpuPct,1)+'%';
        document.getElementById('sim-mem-tag').textContent = fmt(memPct,1)+'%';
        document.getElementById('sim-storage-tag').textContent = fmt(stPct,1)+'%';

        // ---- N-1 host failure simulation: use the highest-resource host, independently per resource ----
        var n1Note = document.getElementById('sim-n1-note');
        var n1Charts = document.getElementById('sim-n1-charts');
        var n1Tag = document.getElementById('sim-n1-tag');
        var n1CpuPct = null, n1MemPct = null;
        if(newHostCount < 2 || (!base.largestByCores && !base.largestByRam)){
          n1Note.style.display = 'block';
          n1Note.textContent = newHostCount < 2
            ? 'ต้องมี host อย่างน้อย 2 ตัวขึ้นไปในคลัสเตอร์นี้ (หลังปรับจำนวนแล้ว) จึงจะจำลอง N-1 ได้'
            : 'ไม่พบข้อมูล host จริงในคลัสเตอร์นี้สำหรับใช้จำลอง host ที่ล่ม';
          n1Charts.style.display = 'none';
          n1Tag.textContent = '';
        }else{
          n1Charts.style.display = '';
          n1Note.style.display = 'none';
          var failedCoresHost = base.largestByCores;
          var failedRamHost = base.largestByRam;
          var n1Cores = Math.max(0, newCores - (failedCoresHost?failedCoresHost.cores:0));
          var n1RamGB = Math.max(0, newRamGB - (failedRamHost?mibToGB(failedRamHost.memMiB):0));
          var n1EffectiveCores = n1Cores * cpuRatio;
          n1CpuPct = n1EffectiveCores>0 ? (base.allocVCPU/n1EffectiveCores*100) : (base.allocVCPU>0?999:0);
          n1MemPct = n1RamGB>0 ? (base.allocVRamGB/n1RamGB*100) : (base.allocVRamGB>0?999:0);
          renderSimPie('chart-sim-n1-cpu', n1CpuPct, 'CPU N-1');
          renderSimPie('chart-sim-n1-mem', n1MemPct, 'Memory N-1');
          var n1Bad = n1CpuPct>100 || n1MemPct>100;
          var sameHost = failedCoresHost && failedRamHost && failedCoresHost.name===failedRamHost.name;
          var whoTxt = sameHost
            ? ('จำลองเสีย: '+esc(failedCoresHost.name)+' ('+fmt(failedCoresHost.cores)+' cores, '+fmt(mibToGB(failedCoresHost.memMiB),0)+' GB) — ใหญ่สุดทั้ง CPU และ Memory')
            : ('จำลองเสีย (CPU): '+esc(failedCoresHost?failedCoresHost.name:'-')+' ('+fmt(failedCoresHost?failedCoresHost.cores:0)+' cores) · จำลองเสีย (Memory): '+esc(failedRamHost?failedRamHost.name:'-')+' ('+fmt(failedRamHost?mibToGB(failedRamHost.memMiB):0,0)+' GB)');
          n1Tag.innerHTML = '<span class="pill '+(n1Bad?'bad':'ok')+'">'+(n1Bad?'เกินขีดความสามารถหาก host ล่ม':'รองรับได้หาก host ล่ม 1 ตัว')+'</span> '+whoTxt;
        }

        function deltaTxt(cur,next,unit,d){
          var diff = next-cur;
          var sign = diff>=0?'+':'';
          return fmt(cur,d)+' → '+fmt(next,d)+' '+unit+' <span style="color:'+(diff===0?'var(--muted)':(diff>0?'var(--ok)':'var(--warn)'))+';">('+sign+fmt(diff,d)+')</span>';
        }
        var rows = [
          ['Cluster', clusterFilter==='ALL' ? 'ทั้งหมด (All Clusters)' : esc(clusterFilter)],
          ['จำนวน Host', fmt(base.hostCount)+' → '+fmt(newHostCount)+' <span style="color:var(--muted);">('+(hostDelta>=0?'+':'')+hostDelta+')</span>'],
          ['Physical Cores', deltaTxt(base.totalCores, newCores, '', 0)],
          ['CPU Ratio (vCPU:pCPU)', fmt(cpuRatio,1)+' : 1'],
          ['Effective CPU Capacity (vCPU)', deltaTxt(effectiveCoresCur, effectiveCoresNew, '', 0)],
          ['vCPU จัดสรร', fmt(base.allocVCPU)+' (คงที่)'],
          ['CPU Utilization', fmt(curCpuPct,1)+'% → '+fmt(cpuPct,1)+'%'],
          ['Physical RAM (GB)', deltaTxt(base.totalRamGB, newRamGB, '', 0)],
          ['vRAM จัดสรร (GB)', fmt(base.allocVRamGB,0)+' (คงที่)'],
          ['Memory Utilization', fmt(curMemPct,1)+'% → '+fmt(memPct,1)+'%'],
          ['Storage Capacity (TB)', deltaTxt(base.totalStorageTB, newStorageTB, '', 2)],
          ['Storage Used (TB)', fmt(base.usedStorageTB,2)+' (คงที่)'],
          ['Storage Utilization', fmt(curStPct,1)+'% → '+fmt(stPct,1)+'%'],
          ['N-1 CPU Utilization (host CPU สูงสุดล่ม)', n1CpuPct==null?'—':fmt(n1CpuPct,1)+'%'],
          ['N-1 Memory Utilization (host Memory สูงสุดล่ม)', n1MemPct==null?'—':fmt(n1MemPct,1)+'%']
        ];
        document.getElementById('tbl-sim-summary').innerHTML =
          '<thead><tr><th>Metric</th><th>ปัจจุบัน → หลังจำลอง</th></tr></thead><tbody>'+
          rows.map(function(r){ return '<tr><td>'+r[0]+'</td><td>'+r[1]+'</td></tr>'; }).join('')+
          '</tbody>';

        // ---- BOM / pricing request email (Thai + English) ----
        var clusterLabelTh = clusterFilter==='ALL' ? 'ทุกคลัสเตอร์' : clusterFilter;
        var clusterLabelEn = clusterFilter==='ALL' ? 'all clusters' : clusterFilter;
        var addHostTh = hostDelta>0 ? ('- เพิ่ม Host ใหม่จำนวน '+hostDelta+' เครื่อง (CPU '+fmt(coresPerHost,0)+' core/เครื่อง, RAM '+fmt(ramPerHost,0)+' GB/เครื่อง)\n') : (hostDelta<0 ? ('- ลด/ปลดระวาง Host จำนวน '+(-hostDelta)+' เครื่อง\n') : '');
        var addHostEn = hostDelta>0 ? ('- Add '+hostDelta+' new host(s) (CPU '+fmt(coresPerHost,0)+' cores/host, RAM '+fmt(ramPerHost,0)+' GB/host)\n') : (hostDelta<0 ? ('- Decommission '+(-hostDelta)+' host(s)\n') : '');
        var addStorageTh = storageDeltaTB!==0 ? ('- '+(storageDeltaTB>0?'เพิ่ม':'ลด')+' Storage Capacity '+fmt(Math.abs(storageDeltaTB),2)+' TB\n') : '';
        var addStorageEn = storageDeltaTB!==0 ? ('- '+(storageDeltaTB>0?'Add':'Reduce')+' storage capacity by '+fmt(Math.abs(storageDeltaTB),2)+' TB\n') : '';

        var simEmailTh = 'เรื่อง: ขอใบเสนอราคาสำหรับการขยายระบบ Virtualization ('+clusterLabelTh+')\n\n'+
          'เรียน ทีมงานฝ่ายขาย\n\n'+
          'บริษัทมีแผนขยายทรัพยากร Virtualization Infrastructure สำหรับ '+clusterLabelTh+' ดังรายละเอียดต่อไปนี้\n\n'+
          'การเปลี่ยนแปลงที่วางแผนไว้:\n'+addHostTh+addStorageTh+
          '- อัตราส่วน vCPU:pCPU ที่ใช้ออกแบบ: '+fmt(cpuRatio,1)+':1\n\n'+
          'ผลลัพธ์ที่คาดการณ์หลังขยาย:\n'+
          '- จำนวน Host รวม: '+fmt(newHostCount)+' เครื่อง\n'+
          '- CPU Utilization: '+fmt(cpuPct,1)+'%\n'+
          '- Memory Utilization: '+fmt(memPct,1)+'%\n'+
          '- Storage Utilization: '+fmt(stPct,1)+'%\n'+
          (n1CpuPct!=null?('- CPU Utilization กรณี Host ล่ม 1 ตัว (N-1): '+fmt(n1CpuPct,1)+'%\n'):'')+
          (n1MemPct!=null?('- Memory Utilization กรณี Host ล่ม 1 ตัว (N-1): '+fmt(n1MemPct,1)+'%\n'):'')+
          '\nรบกวนขอใบเสนอราคาและ Bill of Materials (BOM) พร้อมระยะเวลาส่งมอบ (Lead Time) สำหรับอุปกรณ์ข้างต้นด้วยครับ/ค่ะ\n\n'+
          'ขอบคุณครับ/ค่ะ';

        var simEmailEn = 'Subject: Quotation Request for Virtualization Infrastructure Expansion ('+clusterLabelEn+')\n\n'+
          'Dear Sales Team,\n\n'+
          'We are planning to expand our virtualization infrastructure for '+clusterLabelEn+' as detailed below.\n\n'+
          'Planned changes:\n'+addHostEn+addStorageEn+
          '- Design vCPU:pCPU ratio: '+fmt(cpuRatio,1)+':1\n\n'+
          'Expected results after expansion:\n'+
          '- Total hosts: '+fmt(newHostCount)+'\n'+
          '- CPU Utilization: '+fmt(cpuPct,1)+'%\n'+
          '- Memory Utilization: '+fmt(memPct,1)+'%\n'+
          '- Storage Utilization: '+fmt(stPct,1)+'%\n'+
          (n1CpuPct!=null?('- CPU Utilization if 1 host fails (N-1): '+fmt(n1CpuPct,1)+'%\n'):'')+
          (n1MemPct!=null?('- Memory Utilization if 1 host fails (N-1): '+fmt(n1MemPct,1)+'%\n'):'')+
          '\nCould you please provide a quotation and Bill of Materials (BOM) with lead time for the above requirements?\n\n'+
          'Best regards,';

        renderEmailBlock('sim-email-block', simEmailTh, simEmailEn);
      }

      function simResetInputsToDefaults(){
        var b = simBaseValues(simState.D, simState.cluster, simState.hostOverride);
        document.getElementById('simCoresPerHost').value = Math.round(b.avgCores);
        document.getElementById('simRamPerHost').value = Math.round(b.avgRamGB);
        document.getElementById('simStorageDeltaTB').value = 0;
        document.getElementById('simCpuRatio').value = 4;
      }

      function renderSimulator(D){
        simState.D = D;
        simState.hostDelta = 0;
        simState.hostOverride = {};
        populateSimClusterSelect(D);
        populateSimHostChecklist(D);
        simResetInputsToDefaults();

        if(!simState.wired){
          simState.wired = true;
          document.getElementById('simHostPlus').addEventListener('click', function(){ simState.hostDelta++; recomputeSimulator(); });
          document.getElementById('simHostMinus').addEventListener('click', function(){
            var b = simBaseValues(simState.D, simState.cluster, simState.hostOverride);
            var minDelta = -b.hostCount;
            if(simState.hostDelta > minDelta) simState.hostDelta--;
            recomputeSimulator();
          });
          document.getElementById('simReset').addEventListener('click', function(){
            simState.hostDelta = 0;
            simState.hostOverride = {};
            populateSimHostChecklist(simState.D);
            simResetInputsToDefaults();
            recomputeSimulator();
          });
          document.getElementById('simClusterSelect').addEventListener('change', function(e){
            simState.cluster = e.target.value;
            simState.hostDelta = 0;
            simState.hostOverride = {};
            populateSimHostChecklist(simState.D);
            simResetInputsToDefaults();
            recomputeSimulator();
          });
          ['simCoresPerHost','simRamPerHost','simStorageDeltaTB','simCpuRatio'].forEach(function(id){
            document.getElementById(id).addEventListener('input', recomputeSimulator);
          });
        }
        recomputeSimulator();
      }

      /* ===================== new sizing calculator ===================== */
      var szState = { wired:false, serverCount:3 };
      var upgState = { wired:false };

      function recomputeSizing(){
        var reqVcpu = num(document.getElementById('szReqVcpu').value);
        var reqMemGB = num(document.getElementById('szReqMem').value);
        var reqDiskGB = num(document.getElementById('szReqDisk').value);
        var coresPerServer = num(document.getElementById('szCoresPerServer').value) || 1;
        var ramPerServer = num(document.getElementById('szRamPerServer').value) || 1;
        var storageTotalGB = num(document.getElementById('szStorageTotal').value);
        var cpuRatio = num(document.getElementById('szCpuRatio').value) || 1;
        var serverCount = szState.serverCount;

        document.getElementById('szServerCountLabel').textContent = serverCount;

        var totalCores = serverCount*coresPerServer;
        var totalRamGB = serverCount*ramPerServer;
        var effCores = totalCores*cpuRatio;

        var cpuPct = effCores>0 ? (reqVcpu/effCores*100) : (reqVcpu>0?999:0);
        var memPct = totalRamGB>0 ? (reqMemGB/totalRamGB*100) : (reqMemGB>0?999:0);
        var stPct = storageTotalGB>0 ? (reqDiskGB/storageTotalGB*100) : (reqDiskGB>0?999:0);

        renderSimPie('chart-sz-cpu', cpuPct);
        renderSimPie('chart-sz-mem', memPct);
        renderSimPie('chart-sz-storage', stPct);
        document.getElementById('sz-cpu-tag').textContent = fmt(cpuPct,1)+'%';
        document.getElementById('sz-mem-tag').textContent = fmt(memPct,1)+'%';
        document.getElementById('sz-storage-tag').textContent = fmt(stPct,1)+'%';

        var n1Note = document.getElementById('sz-n1-note');
        var n1Charts = document.getElementById('sz-n1-charts');
        var n1Tag = document.getElementById('sz-n1-tag');
        var n1CpuPct = null, n1MemPct = null;
        if(serverCount < 2){
          n1Note.style.display = 'block';
          n1Note.textContent = 'ต้องมีอย่างน้อย 2 เครื่องขึ้นไปจึงจะจำลอง N-1 ได้';
          n1Charts.style.display = 'none';
          n1Tag.textContent = '';
        }else{
          n1Charts.style.display = '';
          n1Note.style.display = 'none';
          var n1Cores = totalCores - coresPerServer;
          var n1RamGB = totalRamGB - ramPerServer;
          var n1EffCores = n1Cores*cpuRatio;
          n1CpuPct = n1EffCores>0 ? (reqVcpu/n1EffCores*100) : (reqVcpu>0?999:0);
          n1MemPct = n1RamGB>0 ? (reqMemGB/n1RamGB*100) : (reqMemGB>0?999:0);
          renderSimPie('chart-sz-n1-cpu', n1CpuPct);
          renderSimPie('chart-sz-n1-mem', n1MemPct);
          var n1Bad = n1CpuPct>100 || n1MemPct>100;
          n1Tag.innerHTML = '<span class="pill '+(n1Bad?'bad':'ok')+'">'+(n1Bad?'ไม่พอหากเครื่องล่ม 1 ตัว':'รองรับได้หากเครื่องล่ม 1 ตัว')+'</span>';
        }

        var rows = [
          ['จำนวนเครื่อง', fmt(serverCount)],
          ['Physical Cores รวม', fmt(totalCores)],
          ['CPU Ratio (vCPU:pCPU)', fmt(cpuRatio,1)+' : 1'],
          ['Effective CPU Capacity (vCPU)', fmt(effCores,0)],
          ['vCPU ที่ต้องการ', fmt(reqVcpu)],
          ['CPU Utilization', fmt(cpuPct,1)+'%'],
          ['Physical RAM รวม (GB)', fmt(totalRamGB,0)],
          ['vMemory ที่ต้องการ (GB)', fmt(reqMemGB,0)],
          ['Memory Utilization', fmt(memPct,1)+'%'],
          ['Storage รวม (GB)', fmt(storageTotalGB,0)],
          ['vDisk ที่ต้องการ (GB)', fmt(reqDiskGB,0)],
          ['Storage Utilization', fmt(stPct,1)+'%'],
          ['N-1 CPU Utilization', n1CpuPct==null?'—':fmt(n1CpuPct,1)+'%'],
          ['N-1 Memory Utilization', n1MemPct==null?'—':fmt(n1MemPct,1)+'%']
        ];
        document.getElementById('tbl-sz-summary').innerHTML =
          '<thead><tr><th>Metric</th><th>ค่า</th></tr></thead><tbody>'+
          rows.map(function(r){ return '<tr><td>'+r[0]+'</td><td>'+r[1]+'</td></tr>'; }).join('')+
          '</tbody>';

        // ---- BOM / pricing request email (Thai + English) ----
        var szEmailTh = 'เรื่อง: ขอใบเสนอราคาและ BOM สำหรับจัดหาเซิร์ฟเวอร์ใหม่\n\n'+
          'เรียน ทีมงานฝ่ายขาย\n\n'+
          'บริษัทมีความต้องการจัดหาเซิร์ฟเวอร์เพื่อรองรับ Workload ใหม่ ดังรายละเอียดต่อไปนี้\n\n'+
          'ความต้องการ Workload:\n'+
          '- vCPU: '+fmt(reqVcpu)+'\n'+
          '- vMemory: '+fmt(reqMemGB,0)+' GB\n'+
          '- vDisk: '+fmt(reqDiskGB,0)+' GB\n\n'+
          'สเปกเซิร์ฟเวอร์ที่ต้องการ (โดยประมาณ):\n'+
          '- จำนวน: '+fmt(serverCount)+' เครื่อง\n'+
          '- CPU: '+fmt(coresPerServer,0)+' core ต่อเครื่อง\n'+
          '- Memory: '+fmt(ramPerServer,0)+' GB ต่อเครื่อง\n'+
          '- Storage รวม: '+fmt(storageTotalGB,0)+' GB\n'+
          '- อัตราส่วน vCPU:pCPU ที่ใช้ออกแบบ: '+fmt(cpuRatio,1)+':1\n\n'+
          'ผลลัพธ์ที่คาดการณ์: CPU Utilization '+fmt(cpuPct,1)+'%, Memory Utilization '+fmt(memPct,1)+'%, Storage Utilization '+fmt(stPct,1)+'%\n\n'+
          'รบกวนขอใบเสนอราคาและ Bill of Materials (BOM) พร้อมข้อมูลด้าน compatibility และระยะเวลาส่งมอบ (Lead Time) ด้วยครับ/ค่ะ\n\n'+
          'ขอบคุณครับ/ค่ะ';

        var szEmailEn = 'Subject: Quotation and BOM Request for New Server Procurement\n\n'+
          'Dear Sales Team,\n\n'+
          'We are looking to procure servers to support a new workload with the following requirements.\n\n'+
          'Workload Requirements:\n'+
          '- vCPU: '+fmt(reqVcpu)+'\n'+
          '- vMemory: '+fmt(reqMemGB,0)+' GB\n'+
          '- vDisk: '+fmt(reqDiskGB,0)+' GB\n\n'+
          'Proposed Server Specification (estimate):\n'+
          '- Quantity: '+fmt(serverCount)+' unit(s)\n'+
          '- CPU: '+fmt(coresPerServer,0)+' cores per server\n'+
          '- Memory: '+fmt(ramPerServer,0)+' GB per server\n'+
          '- Total storage: '+fmt(storageTotalGB,0)+' GB\n'+
          '- Design vCPU:pCPU ratio: '+fmt(cpuRatio,1)+':1\n\n'+
          'Expected result: CPU Utilization '+fmt(cpuPct,1)+'%, Memory Utilization '+fmt(memPct,1)+'%, Storage Utilization '+fmt(stPct,1)+'%\n\n'+
          'Could you please provide a quotation and Bill of Materials (BOM), including compatibility notes and lead time?\n\n'+
          'Best regards,';

        renderEmailBlock('sz-email-block', szEmailTh, szEmailEn);
      }

      function renderSizing(D){
        if(D && D.hosts && D.hosts.length){
          var avgCores = D.hosts.reduce(function(a,h){return a+h.cores;},0)/D.hosts.length;
          var avgRamGB = mibToGB(D.hosts.reduce(function(a,h){return a+h.memMiB;},0))/D.hosts.length;
          var totalStorageGB = mibToGB(D.datastores.reduce(function(a,d){return a+d.capMiB;},0));
          if(!szState.wired){
            document.getElementById('szCoresPerServer').value = Math.round(avgCores) || 32;
            document.getElementById('szRamPerServer').value = Math.round(avgRamGB) || 256;
            if(totalStorageGB>0) document.getElementById('szStorageTotal').value = Math.round(totalStorageGB);
          }
        }
        if(!szState.wired){
          szState.wired = true;
          document.getElementById('szServerPlus').addEventListener('click', function(){ szState.serverCount++; recomputeSizing(); });
          document.getElementById('szServerMinus').addEventListener('click', function(){ if(szState.serverCount>0) szState.serverCount--; recomputeSizing(); });
          ['szReqVcpu','szReqMem','szReqDisk','szCoresPerServer','szRamPerServer','szStorageTotal','szCpuRatio'].forEach(function(id){
            document.getElementById(id).addEventListener('input', recomputeSizing);
          });
        }
        recomputeSizing();
      }

      /* ===================== upgrade plan ===================== */
      var HW_COMPAT_RULES = [
        {re:/xeon\s*(5500|5600|e5540|e5620|e5645|x56|l56|w56)/i, dropFrom:'7.0', note:'Xeon 5500/5600 series (Nehalem/Westmere) — Broadcom/VMware เลิกรองรับตั้งแต่ ESXi 7.0'},
        {re:/xeon\s*e[35]-\d{4}\s*v1\b/i, dropFrom:'7.0', note:'Xeon E3/E5 v1 (Sandy Bridge-EP รุ่นแรก) มักไม่รองรับ ESXi 7.0 ขึ้นไป'},
        {re:/xeon\s*e5-\d{4}\s*v2\b/i, dropFrom:'8.0', note:'Xeon E5 v2 (Ivy Bridge-EP) เป็นรุ่นท้ายๆ ที่รองรับ ESXi 7.0 — มักไม่รองรับ ESXi 8.0'},
        {re:/opteron/i, dropFrom:'7.0', note:'AMD Opteron รุ่นเก่าหลายรุ่นไม่รองรับ ESXi 7.0 ขึ้นไป'},
        {re:/xeon\s*e3-\d{4}\s*v[12]\b/i, dropFrom:'7.0', note:'Xeon E3 v1/v2 อาจไม่รองรับ ESXi 7.0 ขึ้นไป'}
      ];
      function verCompare(a,b){
        var pa = String(a).split('.').map(Number), pb = String(b).split('.').map(Number);
        for(var i=0;i<Math.max(pa.length,pb.length);i++){
          var x=pa[i]||0, y=pb[i]||0;
          if(x!==y) return x-y;
        }
        return 0;
      }
      function checkHwCompat(cpuModel, targetVersion){
        if(!cpuModel || !targetVersion) return null;
        for(var i=0;i<HW_COMPAT_RULES.length;i++){
          var r = HW_COMPAT_RULES[i];
          if(r.re.test(cpuModel) && verCompare(targetVersion, r.dropFrom) >= 0) return r.note;
        }
        return null;
      }

      function populateVersionSelect(selectEl, currentBranch){
        var keys = Object.keys(VSPHERE_EOL).sort(verCompare);
        selectEl.innerHTML = keys.map(function(k){ return '<option value="'+k+'">'+k+'</option>'; }).join('');
        // default: pick the highest version that is >= current branch (or the latest overall)
        var pick = keys[keys.length-1];
        if(currentBranch){
          var candidates = keys.filter(function(k){ return verCompare(k, currentBranch) >= 0; });
          if(candidates.length) pick = candidates[0]===currentBranch && candidates.length>1 ? candidates[candidates.length-1] : candidates[candidates.length-1];
        }
        selectEl.value = pick;
      }

      /* ===================== network diagram ===================== */
      var diagState = { wired:false };

      function buildHostDiagramSVG(D, hostName){
        var sw = D.switches.filter(function(s){return s.host===hostName;});
        var pgs = D.portgroups.filter(function(p){return p.host===hostName;});
        var nics = D.pnics.filter(function(n){return n.host===hostName;});
        if(!sw.length && !pgs.length && !nics.length){ return null; }

        var nicBoxW=100, nicBoxH=32, nicGapY=14;
        var pgBoxW=190, pgBoxH=42, pgGapY=10;
        var switchHeaderH=30, switchGapY=26;
        var leftX=24, rightX=280;
        var padTop=24;
        function nicKey(n){ return String(n||'').trim().toLowerCase(); }

        // group portgroups by switch (preserve switches with zero portgroups, and switches only referenced by NICs/portgroups)
        var switchNames = sw.map(function(s){return s.name;});
        pgs.forEach(function(p){ if(p.switch && switchNames.indexOf(p.switch)===-1) switchNames.push(p.switch); });
        nics.forEach(function(n){ if(n.switch && switchNames.indexOf(n.switch)===-1) switchNames.push(n.switch); });
        var byswitch = {};
        switchNames.forEach(function(sn){ byswitch[sn] = pgs.filter(function(p){return p.switch===sn;}); });

        var totalNicsH = nics.length ? (nics.length*(nicBoxH+nicGapY)-nicGapY) : 0;
        var totalSwitchH = 0;
        switchNames.forEach(function(sn, i){
          var rows = byswitch[sn].length || 1;
          totalSwitchH += switchHeaderH + rows*(pgBoxH+pgGapY) - pgGapY;
          if(i<switchNames.length-1) totalSwitchH += switchGapY;
        });

        var svgH = Math.max(totalNicsH, totalSwitchH) + padTop*2;
        var svgW = rightX + pgBoxW + 40;

        var nicPos = {};
        var svg = '';
        // NICs column
        var nicY = padTop + Math.max(0,(svgH-padTop*2-totalNicsH)/2);
        nics.forEach(function(n){
          nicPos[nicKey(n.name)] = {x:leftX, y:nicY, w:nicBoxW, h:nicBoxH, cy:nicY+nicBoxH/2};
          nicY += nicBoxH+nicGapY;
        });

        // switches + portgroups column
        var pgBoxes = [];
        var switchPos = {};
        var swY = padTop + Math.max(0,(svgH-padTop*2-totalSwitchH)/2);
        switchNames.forEach(function(sn){
          var rows = byswitch[sn];
          var headerY = swY;
          var blockH = switchHeaderH + Math.max(1,rows.length)*(pgBoxH+pgGapY) - pgGapY;
          switchPos[sn] = {x:rightX, y:headerY, w:pgBoxW, h:switchHeaderH, cy:headerY+switchHeaderH/2};
          svg += '<rect x="'+rightX+'" y="'+headerY+'" width="'+pgBoxW+'" height="'+switchHeaderH+'" rx="3" fill="#233042" stroke="#3A4A5E"/>';
          svg += '<text x="'+(rightX+pgBoxW/2)+'" y="'+(headerY+19)+'" text-anchor="middle" font-size="11" fill="#E7ECF2" font-family="monospace" font-weight="600">'+esc(sn)+'</text>';
          var rowY = headerY + switchHeaderH + 6;
          if(!rows.length){
            svg += '<text x="'+(rightX+pgBoxW/2)+'" y="'+(rowY+16)+'" text-anchor="middle" font-size="9.5" fill="#5C6B7A" font-family="monospace">(ไม่มี Port Group)</text>';
          }
          rows.forEach(function(p){
            pgBoxes.push({p:p, x:rightX, y:rowY, w:pgBoxW, h:pgBoxH, cy:rowY+pgBoxH/2});
            rowY += pgBoxH+pgGapY;
          });
          swY += blockH + switchGapY;
        });

        // draw NIC boxes
        nics.forEach(function(n){
          var pos = nicPos[nicKey(n.name)];
          svg += '<rect x="'+pos.x+'" y="'+pos.y+'" width="'+pos.w+'" height="'+pos.h+'" rx="3" fill="#1A222B" stroke="#3A4A5E"/>';
          svg += '<text x="'+(pos.x+pos.w/2)+'" y="'+(pos.y+14)+'" text-anchor="middle" font-size="10.5" fill="#E7ECF2" font-family="monospace" font-weight="600">'+esc(n.name)+'</text>';
          svg += '<text x="'+(pos.x+pos.w/2)+'" y="'+(pos.y+26)+'" text-anchor="middle" font-size="8.5" fill="#8B96A3" font-family="monospace">'+esc(n.speed||'-')+'</text>';
        });

        var lineSvg = '';
        var lineAttempts = 0, lineMatched = 0, unmatchedNames = [];

        // ---- primary connection: NIC -> Switch, using vNIC's own "Switch"/"Uplink port" fields ----
        var nicToSwitchDrawn = false;
        nics.forEach(function(n){
          if(!n.switch) return;
          nicToSwitchDrawn = true;
          lineAttempts++;
          var np = nicPos[nicKey(n.name)];
          var sp = switchPos[n.switch];
          if(!np || !sp){ if(unmatchedNames.indexOf(n.switch)===-1) unmatchedNames.push(n.switch); return; }
          lineMatched++;
          var x1=np.x+np.w, y1=np.cy, x2=sp.x, y2=sp.cy;
          var mx=(x1+x2)/2;
          var d = 'M'+x1+','+y1+' C'+mx+','+y1+' '+mx+','+y2+' '+x2+','+y2;
          lineSvg += '<path d="'+d+'" fill="none" stroke="#5AA9E6" stroke-width="1.8" opacity="0.9"/>';
          if(n.uplinkPort){
            lineSvg += '<text x="'+((x1+x2)/2)+'" y="'+((y1+y2)/2-4)+'" text-anchor="middle" font-size="8" fill="#5AA9E6" font-family="monospace">'+esc(n.uplinkPort)+'</text>';
          }
        });

        // ---- secondary: per-portgroup Active/Standby/Unused override lines, when this RVTools export includes them ----
        pgBoxes.forEach(function(pb){
          var p = pb.p;
          function drawLine(nicName, cls){
            lineAttempts++;
            var np = nicPos[nicKey(nicName)];
            if(!np){ if(unmatchedNames.indexOf(nicName)===-1) unmatchedNames.push(nicName); return; }
            lineMatched++;
            var x1=np.x+np.w, y1=np.cy, x2=pb.x, y2=pb.cy;
            var mx=(x1+x2)/2;
            var d = 'M'+x1+','+y1+' C'+mx+','+y1+' '+mx+','+y2+' '+x2+','+y2;
            var stroke = cls==='active'?'#3ED2A0':(cls==='standby'?'#F0A857':'#4A5560');
            var dash = cls==='standby' ? ' stroke-dasharray="5,4"' : (cls==='unused' ? ' stroke-dasharray="2,3"' : '');
            lineSvg += '<path d="'+d+'" fill="none" stroke="'+stroke+'" stroke-width="1.6"'+dash+' opacity="'+(cls==='unused'?'0.5':'0.9')+'"/>';
          }
          p.activeNics.forEach(function(n){ drawLine(n,'active'); });
          p.standbyNics.forEach(function(n){ drawLine(n,'standby'); });
          p.unusedNics.forEach(function(n){ drawLine(n,'unused'); });
        });

        // draw portgroup boxes (on top of lines)
        var pgSvg = '';
        pgBoxes.forEach(function(pb){
          var p = pb.p;
          pgSvg += '<rect x="'+pb.x+'" y="'+pb.y+'" width="'+pb.w+'" height="'+pb.h+'" rx="3" fill="#1A222B" stroke="#3A4A5E"/>';
          pgSvg += '<text x="'+(pb.x+8)+'" y="'+(pb.y+16)+'" font-size="10" fill="#E7ECF2" font-family="monospace" font-weight="600">'+esc((p.name||'').length>22?p.name.slice(0,22)+'…':p.name)+'</text>';
          pgSvg += '<text x="'+(pb.x+8)+'" y="'+(pb.y+30)+'" font-size="8.5" fill="#8B96A3" font-family="monospace">VLAN '+esc(p.vlan!=null&&p.vlan!==''?p.vlan:'-')+'</text>';
        });

        var full = '<svg viewBox="0 0 '+svgW+' '+svgH+'" width="'+svgW+'" height="'+svgH+'" xmlns="http://www.w3.org/2000/svg">'+
          lineSvg + svg + pgSvg + '</svg>';
        return {svg:full, lineAttempts:lineAttempts, lineMatched:lineMatched, unmatchedNames:unmatchedNames, pgCount:pgBoxes.length, nicCount:nics.length, nicToSwitchDrawn:nicToSwitchDrawn};
      }

      function renderDiagram(D){
        var sel = document.getElementById('diagHostSelect');
        if(!diagState.wired){
          var hostsWithNet = D.hosts.map(function(h){return h.name;});
          sel.innerHTML = hostsWithNet.map(function(h){ return '<option value="'+esc(h)+'">'+esc(h)+'</option>'; }).join('');
          diagState.wired = true;
          sel.addEventListener('change', function(){ drawSelectedHostDiagram(D); });
        }
        drawSelectedHostDiagram(D);

        // debug info: which sheets/headers were actually found in this file
        var nd = D.netDiag || {};
        var dbg = '<b>ชื่อชีตทั้งหมดในไฟล์:</b> '+esc((nd.allSheetNames||[]).join(', '))+'<br><br>';
        function fmtHeaders(label, headers){
          return '<b>'+label+':</b> '+(headers ? esc(headers.join(', ')) : '<span style="color:var(--bad);">ไม่พบชีตนี้ (หรือไม่มีข้อมูล)</span>')+'<br>';
        }
        dbg += fmtHeaders('vSwitch headers', nd.vSwitchHeaders);
        dbg += fmtHeaders('vPort headers', nd.vPortHeaders);
        dbg += fmtHeaders('dvSwitch headers', nd.dvSwitchHeaders);
        dbg += fmtHeaders('dvPort headers', nd.dvPortHeaders);
        dbg += fmtHeaders('vNIC headers', nd.vNicHeaders);
        dbg += fmtHeaders('vSC_VMK headers', nd.vSCVMKHeaders);
        dbg += '<br>จำนวนที่ parse ได้: switches='+D.switches.length+', portgroups='+D.portgroups.length+', pnics='+D.pnics.length+', vmkernels='+D.vmkernels.length;
        document.getElementById('diag-debug-info').innerHTML = dbg;
      }

      function drawSelectedHostDiagram(D){
        var hostName = document.getElementById('diagHostSelect').value;
        document.getElementById('diag-host-tag').textContent = hostName;
        var result = buildHostDiagramSVG(D, hostName);
        var wrap = document.getElementById('diag-svg-wrap');
        var diagNote = document.getElementById('diag-diagnostic-note');
        if(!result){
          wrap.innerHTML = '<div class="empty-note">ไม่พบข้อมูล vSwitch/vPort/vNIC สำหรับ host นี้ในไฟล์ RVTools (ต้องมีชีต vSwitch, vPort, vNIC)</div>';
          diagNote.innerHTML = '';
        }else{
          wrap.innerHTML = result.svg;
          if(!result.nicToSwitchDrawn && result.lineMatched===0){
            diagNote.innerHTML = '<div class="empty-note" style="color:var(--warn);">⚠ ไม่พบคอลัมน์ "Switch" ในชีต vNIC สำหรับ host นี้ (ใช้บอกว่า NIC ไหนอัปลิงก์เข้า switch ไหน) และไม่พบข้อมูล Active/Standby NIC ระดับ Port Group เช่นกัน — เครื่องมือจึงวาดเส้นเชื่อมไม่ได้ ตรวจสอบหัวคอลัมน์จริงได้ที่กล่อง "ข้อมูลดิบสำหรับ Debug" ด้านล่าง</div>';
          }else if(result.lineAttempts>result.lineMatched){
            diagNote.innerHTML = '<div class="empty-note">จับคู่ NIC ได้ '+result.lineMatched+'/'+result.lineAttempts+' รายการ — ที่จับคู่ไม่ได้ (ชื่อ switch/NIC ไม่ตรงกัน): '+result.unmatchedNames.slice(0,10).map(esc).join(', ')+'</div>';
          }else{
            diagNote.innerHTML = '';
          }
        }
        var nics = D.pnics.filter(function(n){return n.host===hostName;});
        document.getElementById('tbl-diag-nic').innerHTML = nics.length ?
          '<thead><tr><th>NIC</th><th>Driver</th><th>Speed</th><th>Switch</th><th>Uplink Port</th><th>MAC</th></tr></thead><tbody>'+
          nics.map(function(n){ return '<tr><td>'+esc(n.name)+'</td><td>'+esc(n.driver||'-')+'</td><td>'+esc(n.speed||'-')+'</td><td>'+esc(n.switch||'-')+'</td><td>'+esc(n.uplinkPort||'-')+'</td><td>'+esc(n.mac||'-')+'</td></tr>'; }).join('')+'</tbody>'
          : '';
        if(!nics.length) document.getElementById('tbl-diag-nic').parentElement.innerHTML = '<div class="empty-note">ไม่พบข้อมูล (ไม่มีชีต vNIC)</div>';

        var vmk = D.vmkernels.filter(function(k){return k.host===hostName;});
        document.getElementById('tbl-diag-vmk').innerHTML = vmk.length ?
          '<thead><tr><th>Device</th><th>Port Group</th><th>IP Address</th></tr></thead><tbody>'+
          vmk.map(function(k){ return '<tr><td>'+esc(k.device||'-')+'</td><td>'+esc(k.portgroup||'-')+'</td><td>'+esc(k.ip||'-')+'</td></tr>'; }).join('')+'</tbody>'
          : '';
        if(!vmk.length) document.getElementById('tbl-diag-vmk').parentElement.innerHTML = '<div class="empty-note">ไม่พบข้อมูล (ไม่มีชีต vSC_VMK)</div>';
      }

      function renderUpgrade(D){
        var E = computeExpireData(D);
        var esxiBranches = E.esxiRows.map(function(r){ return r.vkey; }).filter(Boolean);
        var lowestEsxiBranch = esxiBranches.sort(verCompare)[0];
        var vcBranch = D.vCenterApiVersion ? versionKey(D.vCenterApiVersion) : null;

        var esxiSel = document.getElementById('upgEsxiTarget');
        var vcSel = document.getElementById('upgVcenterTarget');
        if(!upgState.wired){
          populateVersionSelect(esxiSel, lowestEsxiBranch);
          populateVersionSelect(vcSel, vcBranch);
        }

        // current versions table
        var rows = '';
        E.esxiRows.forEach(function(r){
          rows += '<tr><td>ESXi</td><td>'+esc(r.host)+'</td><td>'+esc(r.version)+'</td><td>'+esc(D.hosts.filter(function(h){return h.name===r.host;})[0].esxBuild||'-')+'</td><td>'+(r.eogs||'-')+'</td></tr>';
        });
        if(D.vCenterApiVersion){
          rows += '<tr><td>vCenter</td><td>'+esc(D.vCenterServer||'-')+'</td><td>'+esc(D.vCenterApiVersion)+'</td><td>'+esc(D.vCenterBuild||'-')+'</td><td>'+(E.vcInfo?E.vcInfo.eogs:'-')+'</td></tr>';
        }
        document.getElementById('tbl-upg-current').innerHTML =
          '<thead><tr><th>Product</th><th>Host/Server</th><th>Version</th><th>Build</th><th>End of Support</th></tr></thead><tbody>'+rows+'</tbody>';

        if(!upgState.wired){
          upgState.wired = true;
          esxiSel.addEventListener('change', function(){ renderUpgradePlanBody(D); });
          vcSel.addEventListener('change', function(){ renderUpgradePlanBody(D); });
        }
        renderUpgradePlanBody(D);
      }

      function renderUpgradePlanBody(D){
        var E = computeExpireData(D);
        var esxiTarget = document.getElementById('upgEsxiTarget').value;
        var vcTarget = document.getElementById('upgVcenterTarget').value;

        // hardware compatibility
        var hwRows = D.hosts.map(function(h){
          var note = checkHwCompat(h.cpuModel, esxiTarget);
          return {host:h.name, cpu:h.cpuModel, ok: !note, note: note};
        });
        var hwBad = hwRows.filter(function(r){return !r.ok;});
        document.getElementById('upg-hw-tag').innerHTML = hwBad.length ? ('<span class="pill bad">'+hwBad.length+' host อาจไม่รองรับ</span>') : '<span class="pill ok">ไม่พบปัญหาจากการตรวจเบื้องต้น</span>';
        document.getElementById('tbl-upg-hw').innerHTML =
          '<thead><tr><th>Host</th><th>CPU Model</th><th>ผลตรวจสอบ (ESXi '+esc(esxiTarget)+')</th></tr></thead><tbody>'+
          hwRows.map(function(r){
            return '<tr><td>'+esc(r.host)+'</td><td>'+esc(r.cpu||'-')+'</td><td>'+(r.ok?'<span class="pill ok">ไม่พบปัญหา</span>':'<span class="pill bad">'+esc(r.note)+'</span>')+'</td></tr>';
          }).join('') + '</tbody>';

        // action plan
        var hostCount = D.hosts.length;
        var vmCount = D.realVms.length;
        var vcNeedsUpgrade = D.vCenterApiVersion && verCompare(vcTarget, versionKey(D.vCenterApiVersion)||'0.0') > 0;
        var esxiNeedsUpgrade = E.esxiRows.some(function(r){ return r.vkey && verCompare(esxiTarget, r.vkey) > 0; });
        var majorJump = D.vCenterApiVersion && Math.floor(parseFloat(vcTarget)) - Math.floor(parseFloat(versionKey(D.vCenterApiVersion)||'0')) >= 1;

        var esxiHours = Math.max(1, hostCount) * 1.5;
        var vmToolsHours = Math.ceil(vmCount/20) * 1; // batch ~20 VMs/hour for tools+hw upgrade staged rollout

        var tasks = [];
        tasks.push(['1', 'Pre-upgrade Assessment', 'ตรวจสอบ Interoperability Matrix, VMware Compatibility Guide, และสำรองข้อมูล (vCenter file-based backup, ESXi host profile/config backup)', '4 ชม.']);
        tasks.push(['2', 'Snapshot / Backup Validation', 'ทดสอบ restore backup เพื่อยืนยันความสมบูรณ์ก่อนเริ่มอัปเกรดจริง', '2 ชม.']);
        if(vcNeedsUpgrade){
          tasks.push(['3', 'Upgrade vCenter Server → '+vcTarget, 'อัปเกรดผ่าน VAMI (Update Manager) พร้อม snapshot ก่อนดำเนินการ'+(majorJump?' — ข้ามหลาย major version ควรตรวจสอบ upgrade path ทีละขั้นตามคำแนะนำ VMware':''), '3-4 ชม.']);
          tasks.push(['4', 'Post-vCenter Validation', 'ตรวจสอบ service, ตรวจ log, login vSphere Client, ตรวจการเชื่อมต่อ host/plugin ทั้งหมด', '2 ชม.']);
        }
        if(esxiNeedsUpgrade){
          tasks.push([String(tasks.length+1), 'Upgrade ESXi Host → '+esxiTarget+' (Rolling, '+hostCount+' host)', 'ทยอยอัปเกรดทีละ host: ย้าย VM ออกด้วย vMotion/DRS → Maintenance Mode → อัปเกรด → ตรวจสอบ → นำกลับเข้า cluster', fmt(esxiHours,1)+' ชม. (~1.5 ชม./host × '+hostCount+' host)']);
        }
        tasks.push([String(tasks.length+1), 'Upgrade VMware Tools & VM Hardware Version', 'ทยอยอัปเดตเป็นชุด (batch) ตามช่วง Maintenance Window เพื่อลดผลกระทบต่อ VM ที่ใช้งานจริง ('+vmCount+' VM)', fmt(vmToolsHours,0)+' ชม. (ประเมินแบบ batch ~20 VM/ชม.)']);
        tasks.push([String(tasks.length+1), 'Post-upgrade Smoke Test', 'ทดสอบ VM/Application หลัก, ตรวจ HA/DRS/vMotion, ตรวจ performance และ event log ผิดปกติ', '4-8 ชม.']);
        tasks.push([String(tasks.length+1), 'Documentation & Handover', 'จัดทำเอกสารสรุปผลการอัปเกรด และส่งมอบให้ทีมงานที่เกี่ยวข้อง', '2 ชม.']);

        var totalLow = tasks.length; // just for tag count
        document.getElementById('upg-plan-tag').textContent = tasks.length+' tasks';
        document.getElementById('tbl-upg-plan').innerHTML =
          '<thead><tr><th>No.</th><th>Task</th><th>รายละเอียด</th><th>เวลาโดยประมาณ</th></tr></thead><tbody>'+
          tasks.map(function(t){ return '<tr><td>'+t[0]+'</td><td><b>'+t[1]+'</b></td><td>'+t[2]+'</td><td class="num">'+t[3]+'</td></tr>'; }).join('')+
          '</tbody>';

        if(!vcNeedsUpgrade && !esxiNeedsUpgrade){
          document.getElementById('tbl-upg-plan').insertAdjacentHTML('afterend', '<div class="empty-note">เวอร์ชันปลายทางที่เลือกไม่สูงกว่าเวอร์ชันปัจจุบัน — เลือกเวอร์ชันที่สูงกว่าเพื่อสร้างแผนอัปเกรด</div>');
        }
      }

      function renderAll(D){
        renderOverview(D);
        renderCompute(D);
        renderStorage(D);
        renderVMs(D);
        renderSnapshots(D);
        renderTools(D);
        renderExpire(D);
        renderSecurity(D);
        renderSimulator(D);
        renderSizing(D);
        renderUpgrade(D);
        renderDiagram(D);
        renderTemplate(D);
        fetchLiveAdvisories(D);
      }

      /* ===================== file handling ===================== */
      var dropzone = document.getElementById('dropzone');
      var fileInput = document.getElementById('fileInput');
      var reportEl = document.getElementById('report');
      var fileinfo = document.getElementById('fileinfo');

      document.getElementById('btnPick').addEventListener('click', function(){ fileInput.click(); });
      document.getElementById('btnReplace').addEventListener('click', function(){ fileInput.click(); });
      document.getElementById('btnPrint').addEventListener('click', function(){
        // switch to the template section so print output matches it, then print
        document.querySelectorAll('.nav-item').forEach(function(n){ n.classList.remove('active'); });
        document.querySelectorAll('.section').forEach(function(n){ n.classList.remove('active'); });
        var navTpl = document.querySelector('.nav-item[data-id="template"]');
        if(navTpl) navTpl.classList.add('active');
        document.getElementById('sec-template').classList.add('active');
        setTimeout(function(){ window.print(); }, 50);
      });
      document.getElementById('btnExportWord').addEventListener('click', function(){
        if(window.__RV__) exportToWord(window.__RV__);
      });
      ['inp-customer','inp-pmdate','inp-preparedby'].forEach(function(id){
        document.getElementById(id).addEventListener('input', function(){
          if(window.__RV__) renderTemplate(window.__RV__);
        });
      });
      document.getElementById('btnRefreshCve').addEventListener('click', function(){
        if(window.__RV__) fetchLiveAdvisories(window.__RV__);
      });

      ['dragenter','dragover'].forEach(function(ev){
        dropzone.addEventListener(ev, function(e){ e.preventDefault(); dropzone.classList.add('drag'); });
      });
      ['dragleave','drop'].forEach(function(ev){
        dropzone.addEventListener(ev, function(e){ e.preventDefault(); dropzone.classList.remove('drag'); });
      });
      dropzone.addEventListener('drop', function(e){
        var f = e.dataTransfer.files[0];
        if(f) handleFile(f);
      });
      fileInput.addEventListener('change', function(e){
        if(e.target.files[0]) handleFile(e.target.files[0]);
      });

      function handleFile(file){
        fileinfo.innerHTML = 'กำลังประมวลผล <b>'+esc(file.name)+'</b> ...';
        var reader = new FileReader();
        reader.onload = function(e){
          try{
            var data = new Uint8Array(e.target.result);
            var wb = XLSX.read(data, {type:'array', cellDates:true});
            var D = processWorkbook(wb, file.name);
            if(!D.vms.length){
              fileinfo.innerHTML = '<span style="color:#E2596B">ไม่พบชีต vInfo ที่อ่านได้ในไฟล์นี้ — โปรดตรวจสอบว่าเป็นไฟล์ export จาก RVTools</span>';
              return;
            }
            window.__RV__ = D;
            renderAll(D);
            loaded = true;
            renderNav();
            dropzone.style.display = 'none';
            reportEl.style.display = 'block';
            document.getElementById('btnReplace').style.display = 'inline-block';
            document.getElementById('btnExportWord').style.display = 'inline-block';
            document.getElementById('btnPrint').style.display = 'inline-block';
            fileinfo.innerHTML = 'ไฟล์: <b>'+esc(file.name)+'</b> &nbsp;·&nbsp; สร้างรายงานเมื่อ '+new Date().toLocaleString('th-TH');
          }catch(err){
            console.error(err);
            fileinfo.innerHTML = '<span style="color:#E2596B">เกิดข้อผิดพลาดขณะอ่านไฟล์: '+esc(err.message)+'</span>';
          }
        };
        reader.readAsArrayBuffer(file);
      }

    })();
    </script>
    <div class="no-print" style="position:fixed; right:14px; bottom:10px; font-size:10.5px; color:#5C6B7A; font-family:var(--mono); z-index:50; pointer-events:none;">Inspire by Aphinat Tiyasuwan</div>

    </body>
    </html>
